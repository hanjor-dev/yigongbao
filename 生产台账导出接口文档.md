# 生产产品台账导出接口文档

## 接口信息

**接口名称：** 导出生产产品台账Excel

**接口路径：** `POST /api/production/record/product-ledger/export`

**接口说明：** 导出生产产品台账数据为Excel文件，支持按多种条件筛选或导出全部数据。系统会根据当前登录用户的角色自动过滤数据权限（医院角色只能导出本医院订单数据，加工中心角色只能导出分配给自己的数据）。

**响应类型：** 文件下载（application/vnd.openxmlformats-officedocument.spreadsheetml.sheet）

---

## 请求参数

**请求方式：** POST

**Content-Type：** application/json

**请求体（ProductLedgerExportDTO）：**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| recordNo | String | 否 | 流转卡编号（支持模糊查询） | "LC202606220001" |
| orderCode | String | 否 | 订单编号（支持模糊查询） | "ORD202606220001" |
| productNo | String | 否 | 产品编号（支持模糊查询） | "PRD202606220001" |
| startTime | String | 否 | 创建时间起（ISO 8601格式） | "2026-06-01T00:00:00" |
| endTime | String | 否 | 创建时间止（ISO 8601格式） | "2026-06-22T23:59:59" |

**注意：**
- 所有参数均为可选，支持全空参数导出全部数据（受数据权限限制）
- 时间参数格式：`yyyy-MM-ddTHH:mm:ss`，例如 `2026-06-22T08:00:00`
- 模糊查询参数支持部分匹配
- `hospitalIds` 和 `centerIds` 参数由后端根据用户权限自动填充，前端无需传递

---

## 请求示例

### 示例1：导出全部数据（空参数）

```bash
curl -X POST "http://localhost:8080/api/production/record/product-ledger/export" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{}'
```

```json
{}
```

### 示例2：按流转卡编号导出

```json
{
  "recordNo": "LC202606220001"
}
```

### 示例3：按时间范围导出

```json
{
  "startTime": "2026-06-01T00:00:00",
  "endTime": "2026-06-22T23:59:59"
}
```

### 示例4：组合条件导出

```json
{
  "orderCode": "ORD",
  "productNo": "PRD",
  "startTime": "2026-06-01T00:00:00",
  "endTime": "2026-06-22T23:59:59"
}
```

---

## 响应说明

### 成功响应（200 OK）

**响应头：**
```
Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
Content-Disposition: attachment; filename=%E7%94%9F%E4%BA%A7%E4%BA%A7%E5%93%81%E5%8F%B0%E8%B4%A6_2026-06-22.xlsx
```

**响应体：** Excel文件二进制数据

**Excel文件说明：**
- 文件名格式：`生产产品台账_yyyy-MM-dd.xlsx`
- 包含表头和数据行
- 最多导出10000条数据
- 如果查询结果超过10000条，Excel中会显示总数和实际导出数

---

### 错误响应

#### 1. 时间范围错误（400）

```json
{
  "code": 400,
  "message": "开始时间不能晚于结束时间",
  "data": null,
  "timestamp": 1719043200000
}
```

**原因：** `startTime` 晚于 `endTime`

---

#### 2. 无数据（404）

```json
{
  "code": 404,
  "message": "未查询到符合条件的数据",
  "data": null,
  "timestamp": 1719043200000
}
```

**原因：** 查询条件没有匹配到任何数据

---

#### 3. 无权限（403）

```json
{
  "code": 403,
  "message": "无权导出生产台账：当前用户未绑定医院或加工中心",
  "data": null,
  "timestamp": 1719043200000
}
```

**原因：** 用户角色不是医院角色或加工中心角色，或未绑定对应机构

---

#### 4. 系统错误（500）

```json
{
  "code": 500,
  "message": "Excel生成失败：...",
  "data": null,
  "timestamp": 1719043200000
}
```

**原因：** 服务器内部错误，如Excel生成失败

---

## 数据权限说明

接口会根据当前登录用户的角色自动过滤数据：

| 用户角色 | 数据范围 | 说明 |
|---------|---------|------|
| **医院角色** | 仅本医院订单相关的生产数据 | 系统自动查询用户绑定的医院ID，并过滤 `order_main.hospital_id` |
| **加工中心角色** | 仅分配给本中心的生产数据 | 系统自动查询用户绑定的加工中心ID，并过滤 `production_product.center_id` |
| **全部数据权限** | 所有生产数据 | 不添加医院或加工中心过滤条件 |
| **其他角色** | 拒绝导出 | 返回403错误 |

**注意：** 即使不传任何筛选条件，也只会导出用户有权限查看的数据，不会导出全系统数据。

---

## 业务规则

1. **模糊查询：** `recordNo`、`orderCode`、`productNo` 三个字段支持部分匹配（LIKE查询）
2. **时间范围：** `startTime` 和 `endTime` 基于产品创建时间（`production_product.create_time`）过滤
3. **数据上限：** 单次最多导出10000条数据，超出部分不会包含在Excel中
4. **空参数：** 支持全空参数请求，此时导出当前用户权限范围内的所有数据
5. **条件组合：** 多个条件之间为AND关系（同时满足）

---

## 前端实现建议

### JavaScript (Fetch API)

```javascript
async function exportProductLedger(params) {
  try {
    const response = await fetch('/api/production/record/product-ledger/export', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${localStorage.getItem('token')}`
      },
      body: JSON.stringify(params)
    });

    if (!response.ok) {
      const error = await response.json();
      throw new Error(error.message);
    }

    // 获取文件名
    const disposition = response.headers.get('Content-Disposition');
    const filename = disposition 
      ? decodeURIComponent(disposition.split('filename=')[1])
      : `生产产品台账_${new Date().toISOString().split('T')[0]}.xlsx`;

    // 下载文件
    const blob = await response.blob();
    const url = window.URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = filename;
    document.body.appendChild(a);
    a.click();
    window.URL.revokeObjectURL(url);
    document.body.removeChild(a);

    return { success: true };
  } catch (error) {
    console.error('导出失败:', error);
    return { success: false, message: error.message };
  }
}

// 使用示例
// 导出全部
exportProductLedger({});

// 按条件导出
exportProductLedger({
  startTime: '2026-06-01T00:00:00',
  endTime: '2026-06-22T23:59:59',
  recordNo: 'LC2026'
});
```

---

### JavaScript (Axios)

```javascript
import axios from 'axios';

async function exportProductLedger(params) {
  try {
    const response = await axios.post(
      '/api/production/record/product-ledger/export',
      params,
      {
        responseType: 'blob',
        headers: {
          'Content-Type': 'application/json'
        }
      }
    );

    // 获取文件名
    const disposition = response.headers['content-disposition'];
    const filename = disposition 
      ? decodeURIComponent(disposition.split('filename=')[1])
      : `生产产品台账_${new Date().toISOString().split('T')[0]}.xlsx`;

    // 下载文件
    const url = window.URL.createObjectURL(new Blob([response.data]));
    const link = document.createElement('a');
    link.href = url;
    link.setAttribute('download', filename);
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
    window.URL.revokeObjectURL(url);

    return { success: true };
  } catch (error) {
    console.error('导出失败:', error);
    return { 
      success: false, 
      message: error.response?.data?.message || error.message 
    };
  }
}
```

---

## 测试用例

### 测试场景1：全量导出

**请求：**
```json
{}
```

**预期：** 导出当前用户权限范围内的所有生产产品数据

---

### 测试场景2：时间范围筛选

**请求：**
```json
{
  "startTime": "2026-06-01T00:00:00",
  "endTime": "2026-06-30T23:59:59"
}
```

**预期：** 导出6月份创建的产品数据

---

### 测试场景3：时间范围错误

**请求：**
```json
{
  "startTime": "2026-06-30T00:00:00",
  "endTime": "2026-06-01T23:59:59"
}
```

**预期：** 返回400错误，提示"开始时间不能晚于结束时间"

---

### 测试场景4：模糊查询

**请求：**
```json
{
  "recordNo": "LC2026",
  "orderCode": "ORD"
}
```

**预期：** 导出流转卡编号包含"LC2026"且订单编号包含"ORD"的数据

---

### 测试场景5：无数据

**请求：**
```json
{
  "productNo": "NOT_EXIST"
}
```

**预期：** 返回404错误，提示"未查询到符合条件的数据"

---

## 常见问题

### Q1：为什么空参数请求没有返回所有数据？

**A：** 接口会根据用户角色自动过滤数据。如果你是医院角色，只能导出本医院订单的数据；如果是加工中心角色，只能导出分配给本中心的数据。

---

### Q2：为什么导出的数据只有10000条？

**A：** 为了防止单次导出数据量过大导致系统性能问题，接口限制单次最多导出10000条。如果需要导出更多数据，建议使用时间范围或其他条件分批导出。

---

### Q3：时间参数的时区是什么？

**A：** 时间参数使用服务器本地时区。建议使用ISO 8601格式（`yyyy-MM-ddTHH:mm:ss`），系统会自动解析。

---

### Q4：如何判断导出是否成功？

**A：** 
- HTTP状态码为200且返回Excel文件 = 成功
- HTTP状态码为4xx/5xx且返回JSON错误信息 = 失败

---

### Q5：文件名中的中文乱码怎么办？

**A：** 服务端已使用UTF-8编码处理文件名。如果浏览器下载时仍然乱码，建议使用 `decodeURIComponent()` 解码 `Content-Disposition` 头中的文件名。

---

**文档版本：** v1.0  
**最后更新：** 2026-06-22  
**维护人员：** 后端开发团队
