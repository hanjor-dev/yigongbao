# 医工宝订单状态流转完整分析

> **最后更新**: 2026-06-04  
> **版本**: v2.0（移除USER_CONFIRM后）

## 一、系统架构概述

### 核心组件
- **FlowPhaseEnum**: 7个阶段（ORDER → DESIGN → PRINT → POST_PROCESSING → QC → WAREHOUSE → COMPLETED）
- **FlowStatusEnum**: 18个状态（按阶段分段：10xx, 20xx, 30xx...）
- **FlowActionEnum**: 21个动作触发状态转换
- **FlowStatusTransitionRules**: 阶段内状态转换规则
- **FlowPhaseTransitionRules**: 跨阶段转换规则
- **FlowFacade**: 流程模块对外统一入口

### 订单分类
- **needsPhysicalDelivery**: 是否需要实体交付（1=需要，0=不需要）
- **orderType**: 订单类型（1=医疗器械，2=非医疗器械）

---

## 二、完整状态流转图

### 路径A：需要实体交付 + 医疗器械（needsPhysicalDelivery=1, orderType=1）
```
草稿(1010) 
  → [SUBMIT_ORDER] 
  → 数据待审核(1020) 
  → [DATA_AUDIT_PASS] 
  → 数据审核通过(1030) → 跨阶段 → 待设计(2010)
  → [START_DESIGN] 
  → 设计中(2020)
  → [COMPLETE_DESIGN]
  → 设计完成(2030) → 跨阶段 → 待打印(3010)
  → [START_PRINT]
  → 打印中(3020)
  → [COMPLETE_PRINT]
  → 打印完成(3030) → 跨阶段 → 后处理中(4010)
  → [COMPLETE_POST_PROCESSING]
  → 跨阶段 → 质检中(5010)
  → [QC_PASS]
  → 质检合格(5020) → 跨阶段 → 入库中(6010)
  → [COMPLETE_WAREHOUSE_IN]
  → 已入库(6020) → 跨阶段 → 已完成(8010)
```

### 路径B：需要实体交付 + 非医疗器械（needsPhysicalDelivery=1, orderType=2）
```
草稿(1010) 
  → ... (同路径A到打印完成)
  → 打印完成(3030) → 跨阶段 → 质检中(5010) [跳过后处理]
  → [QC_PASS]
  → ... (同路径A后续流程)
```

### 路径C：不需要实体交付（needsPhysicalDelivery=0）
```
草稿(1010)
  → [SUBMIT_ORDER]
  → 数据待审核(1020)
  → [DATA_AUDIT_PASS]
  → 数据审核通过(1030) → 跨阶段 → 待设计(2010)
  → [START_DESIGN]
  → 设计中(2020)
  → [COMPLETE_DESIGN]
  → 设计完成(2030) [停留，等待手动完成]
  → [COMPLETE] 手动操作
  → 已完成(8010)
```

**重要变更（v2.0）**：
- ❌ 移除了CONFIRM阶段（phase=70）和AWAITING_CONFIRM状态（7010）
- ❌ 移除了USER_CONFIRM动作
- ✅ 不需要实体交付的订单在DESIGN_COMPLETED(2030)停留，等待业务人员线下确认后使用COMPLETE动作手动完成

---

## 三、各阶段详细分析

### 阶段1：订单阶段（ORDER, phase=10）

**状态列表**：
- 1010: 草稿（DRAFT）
- 1020: 数据待审核（PENDING_DATA_AUDIT）
- 1030: 数据审核通过（DATA_AUDIT_PASSED）
- 1040: 数据审核不通过（DATA_AUDIT_REJECTED）

**状态转换**：
```
DRAFT(1010) → PENDING_DATA_AUDIT(1020) [SUBMIT_ORDER]
PENDING_DATA_AUDIT(1020) → DATA_AUDIT_PASSED(1030) [DATA_AUDIT_PASS]
PENDING_DATA_AUDIT(1020) → DATA_AUDIT_REJECTED(1040) [DATA_AUDIT_REJECT]
DATA_AUDIT_PASSED(1030) → PENDING_DATA_AUDIT(1020) [WITHDRAW]
DATA_AUDIT_REJECTED(1040) → PENDING_DATA_AUDIT(1020) [RESUBMIT]
```

**跨阶段转换**：
- DATA_AUDIT_PASSED(1030) → 自动推进到 DESIGN 阶段，状态变为 PENDING_DESIGN(2010)

**触发点（接口/功能）**：
- **创建订单**: `OrderMainController.create()` → 初始状态 = DRAFT(1010)
- **提交订单**: `OrderMainController.submitOrder()` → SUBMIT_ORDER 动作
- **审核通过**: `DataAuditController.pass()` → DATA_AUDIT_PASS 动作
- **审核驳回**: `DataAuditController.reject()` → DATA_AUDIT_REJECT 动作
- **撤回**: `OrderMainController.withdraw()` → WITHDRAW 动作
- **重新提交**: `OrderMainController.resubmit()` → RESUBMIT 动作

---

### 阶段2：设计阶段（DESIGN, phase=20）

**状态列表**：
- 2010: 待设计（PENDING_DESIGN）
- 2020: 设计中（DESIGN_IN_PROGRESS）
- 2030: 设计完成（DESIGN_COMPLETED）

**状态转换**：
```
PENDING_DESIGN(2010) → DESIGN_IN_PROGRESS(2020) [START_DESIGN]
DESIGN_IN_PROGRESS(2020) → DESIGN_COMPLETED(2030) [COMPLETE_DESIGN]
DESIGN_COMPLETED(2030) → COMPLETED(8010) [COMPLETE] (仅needsPhysicalDelivery=0)
```

**跨阶段转换**：
- DESIGN_COMPLETED(2030) → 根据 needsPhysicalDelivery 分支：
  - needsPhysicalDelivery=1: 推进到 PRINT 阶段，状态 = PENDING_PRINT(3010)
  - needsPhysicalDelivery=0: **不自动推进**，停留在 DESIGN_COMPLETED(2030)，等待手动完成

**触发点（接口/功能）**：
- **开始设计**: `DesignWorkorderController.startDesign()` → 
  - 调用 `DesignWorkorderServiceImpl.startDesign()`
  - 执行 `flowFacade.executeFlow(orderId, FlowActionEnum.START_DESIGN, operator)`
  
- **完成设计**: `DesignWorkorderController.completeDesign()` →
  - 调用 `DesignWorkorderServiceImpl.completeDesign()`
  - 校验逻辑：
    - needsPhysicalDelivery=1: 校验数据包、打印信息、指令单、图纸
    - needsPhysicalDelivery=0: 只校验STL模型
  - 执行 `flowFacade.executeFlow(orderId, FlowActionEnum.COMPLETE_DESIGN, operator)`
  - 发布事件：`DesignCompletedEvent`

- **手动完成订单**: `OrderMainController.manualComplete()` →
  - 调用 `OrderMainServiceImpl.manualCompleteOrder()`
  - 前置条件：needsPhysicalDelivery=0 且 status=DESIGN_COMPLETED(2030)
  - 执行 `flowFacade.executeFlow(orderId, FlowActionEnum.COMPLETE, operator)`

**关键逻辑**：
- 完成设计后，生产模块监听 `DesignCompletedEvent`，为需要实体交付的订单创建生产流转卡
- 不需要实体交付的订单停留在 DESIGN_COMPLETED，业务人员线下与客户确认后手动完成

---

### 阶段3：打印阶段（PRINT, phase=30）

**状态列表**：
- 3010: 待打印（PENDING_PRINT）
- 3020: 打印中（PRINTING）
- 3030: 打印完成（PRINT_COMPLETED）
- 3040: 打印失败（PRINT_FAILED）

**状态转换**：
```
PENDING_PRINT(3010) → PRINTING(3020) [START_PRINT]
PRINTING(3020) → PRINT_COMPLETED(3030) [COMPLETE_PRINT]
```

**跨阶段转换**：
- PRINT_COMPLETED(3030) → 根据 orderType 分支：
  - orderType=1（医疗器械）: 推进到 POST_PROCESSING 阶段，状态 = POST_PROCESSING(4010)
  - orderType=2（非医疗器械）: 推进到 QC 阶段，状态 = QC_IN_PROGRESS(5010)

**触发点（接口/功能）**：
- **创建生产流转卡**: 监听 `DesignCompletedEvent` →
  - `ProductionRecordServiceImpl.createProductionRecords()`
  - 为需要实体交付的订单创建流转卡
  - 订单状态从 DESIGN_COMPLETED(2030) → PENDING_PRINT(3010)
  
- **开始打印**: `ProductionRecordController.startPrint()` →
  - 调用 `ProductionRecordServiceImpl.startPrint()`
  - 执行 `flowFacade.executeFlow(orderId, FlowActionEnum.START_PRINT, operator)`
  
- **完成打印**: 设备自动触发或手动操作 →
  - `DeviceStatusListener` 监听设备状态
  - 或 `ProductionRecordServiceImpl.triggerFlowIfAllReach()`
  - 执行 `flowFacade.executeFlow(orderId, FlowActionEnum.COMPLETE_PRINT, operator)`

**关键逻辑**：
- 订单可能包含多个产品，每个产品对应一个生产流转卡
- 只有当所有流转卡都达到目标状态时，才触发订单级别的状态流转
- `triggerFlowIfAllReach()` 方法负责聚合判断

---

### 阶段4：后处理阶段（POST_PROCESSING, phase=40）

**状态列表**：
- 4010: 后处理中（POST_PROCESSING）

**状态转换**：
```
POST_PROCESSING(4010) → QC_IN_PROGRESS(5010) [COMPLETE_POST_PROCESSING]
```

**跨阶段转换**：
- POST_PROCESSING(4010) + COMPLETE_POST_PROCESSING → 推进到 QC 阶段，状态 = QC_IN_PROGRESS(5010)

**触发点（接口/功能）**：
- **完成后处理**: `ProductionProcessController.completePostProcessing()` →
  - 调用 `ProductionProcessServiceImpl.completePostProcessing()`
  - 调用 `ProductionRecordServiceImpl.triggerFlowIfAllReach(orderId, POST_PROCESSING, COMPLETE_POST_PROCESSING)`
  - 执行 `flowFacade.executeFlow(orderId, FlowActionEnum.COMPLETE_POST_PROCESSING, operator)`

**注意**：
- 仅医疗器械订单（orderType=1）会经过此阶段
- 非医疗器械从打印直接进入质检

---

### 阶段5：质检阶段（QC, phase=50）

**状态列表**：
- 5010: 质检中（QC_IN_PROGRESS）
- 5020: 质检合格（QC_PASSED）
- 5030: 质检不合格（QC_FAILED）
- 5040: 返工（REWORK）

**状态转换**：
```
QC_IN_PROGRESS(5010) → QC_PASSED(5020) [QC_PASS]
QC_IN_PROGRESS(5010) → QC_FAILED(5030) [QC_FAIL]
QC_IN_PROGRESS(5010) → PENDING_PRINT(3010) [REWORK_TO_PRINT - 直接回退到打印]
QC_FAILED(5030) → REWORK(5040) [REWORK]
REWORK(5040) → QC_IN_PROGRESS(5010) [REWORK_COMPLETE]
```

**跨阶段转换**：
- QC_PASSED(5020) → 推进到 WAREHOUSE 阶段，状态 = WAREHOUSE_IN(6010)
- QC_FAILED + REWORK_TO_PRINT → 回退到 PRINT 阶段，状态 = PENDING_PRINT(3010)

**触发点（接口/功能）**：
- **质检合格**: `ProductionQcController.pass()` →
  - 调用 `ProductionQcServiceImpl.qcPass()`
  - 调用 `ProductionRecordServiceImpl.triggerFlowIfAllReach(orderId, QC_PASSED, QC_PASS)`
  - 执行 `flowFacade.executeFlow(orderId, FlowActionEnum.QC_PASS, operator)`
  
- **质检不合格**: `ProductionQcController.fail()` →
  - 调用 `ProductionQcServiceImpl.qcFail()`
  - 执行 `flowFacade.executeFlow(orderId, FlowActionEnum.QC_FAIL, operator)`
  
- **返工**: `ProductionQcController.rework()` →
  - 执行 `flowFacade.executeFlow(orderId, FlowActionEnum.REWORK, operator)`
  
- **返工完成**: `ProductionQcController.reworkComplete()` →
  - 执行 `flowFacade.executeFlow(orderId, FlowActionEnum.REWORK_COMPLETE, operator)`
  
- **回退到打印**: `ProductionQcController.reworkToPrint()` →
  - 执行 `flowFacade.executeFlow(orderId, FlowActionEnum.REWORK_TO_PRINT, operator)`
  - 订单阶段从 QC(50) 回退到 PRINT(30)

**特殊逻辑**：
- 质检不合格可回退到打印阶段（REWORK_TO_PRINT），支持跨阶段回退
- 返工流程：QC_FAILED → REWORK → QC_IN_PROGRESS（在质检阶段内循环）

---

### 阶段6：仓储阶段（WAREHOUSE, phase=60）

**状态列表**：
- 6010: 入库中（WAREHOUSE_IN）
- 6020: 已入库（WAREHOUSED）

**状态转换**：
```
WAREHOUSE_IN(6010) → WAREHOUSED(6020) [COMPLETE_WAREHOUSE_IN]
```

**跨阶段转换**：
- WAREHOUSED(6020) → 推进到 COMPLETED 阶段，状态 = COMPLETED(8010)

**触发点（接口/功能）**：
- **完成入库**: `ProductionPackController.completeWarehouse()` →
  - 调用 `ProductionPackServiceImpl.completeWarehouse()`
  - 调用 `ProductionRecordServiceImpl.triggerFlowIfAllReach(orderId, WAREHOUSED, COMPLETE_WAREHOUSE_IN)`
  - 执行 `flowFacade.executeFlow(orderId, FlowActionEnum.COMPLETE_WAREHOUSE_IN, operator)`

---

### 阶段7：已完成（COMPLETED, phase=80）

**状态列表**：
- 8010: 已完成（COMPLETED）

**状态转换**：
- 终态，无后续转换

**到达方式**：
- 需要实体交付订单：WAREHOUSED(6020) → 自动推进 → COMPLETED(8010)
- 不需要实体交付订单：DESIGN_COMPLETED(2030) → [COMPLETE手动完成] → COMPLETED(8010)

---

### 全局状态：已取消（CANCELLED, phase=90）

**状态**：
- 9010: 已取消（CANCELLED）

**触发**：
- 任何非终态状态都可以执行 CANCEL 动作 → CANCELLED(9010)
- 触发点：`OrderMainController.cancel()` → `flowFacade.executeFlow(orderId, FlowActionEnum.CANCEL, operator)`

---

## 四、动作汇总表

| 动作编码 | 动作名称 | 适用阶段 | 触发接口/服务 |
|---------|---------|---------|--------------|
| CREATE | 创建订单 | 全局 | OrderMainController.create() |
| SUBMIT_ORDER | 提交订单 | ORDER | OrderMainController.submitOrder() |
| DATA_AUDIT_PASS | 审核通过 | ORDER | DataAuditController.pass() |
| DATA_AUDIT_REJECT | 审核驳回 | ORDER | DataAuditController.reject() |
| WITHDRAW | 撤回 | ORDER | OrderMainController.withdraw() |
| RESUBMIT | 重新提交 | ORDER | OrderMainController.resubmit() |
| START_DESIGN | 开始设计 | DESIGN | DesignWorkorderController.startDesign() |
| COMPLETE_DESIGN | 完成设计 | DESIGN | DesignWorkorderController.completeDesign() |
| START_PRINT | 开始打印 | PRINT | ProductionRecordController.startPrint() |
| COMPLETE_PRINT | 完成打印 | PRINT | DeviceStatusListener / triggerFlowIfAllReach() |
| COMPLETE_POST_PROCESSING | 完成后处理 | POST_PROCESSING | ProductionProcessController.completePostProcessing() |
| QC_PASS | 质检合格 | QC | ProductionQcController.pass() |
| QC_FAIL | 质检不合格 | QC | ProductionQcController.fail() |
| REWORK | 返工 | QC | ProductionQcController.rework() |
| REWORK_COMPLETE | 返工完成 | QC | ProductionQcController.reworkComplete() |
| REWORK_TO_PRINT | 回退到打印 | QC | ProductionQcController.reworkToPrint() |
| COMPLETE_WAREHOUSE_IN | 完成入库 | WAREHOUSE | ProductionPackController.completeWarehouse() |
| COMPLETE | 手动完成 | DESIGN | OrderMainController.manualComplete() |
| CANCEL | 取消订单 | 全局 | OrderMainController.cancel() |

---

## 五、关键设计模式

### 1. 状态机模式
- **FlowStateMachineService**: 状态机核心服务
- **FlowStatusTransitionRules**: 定义阶段内状态转换规则
- **FlowPhaseTransitionRules**: 定义跨阶段转换规则
- **TransitionResult**: 封装转换结果（包含阶段变更、状态变更等信息）

### 2. 门面模式
- **FlowFacade**: 作为flow模块的唯一对外入口
- 封装状态机操作，提供统一接口
- 支持乐观锁版本控制（审核动作）

### 3. 事件驱动
- **DesignCompletedEvent**: 设计完成事件
- 生产模块监听事件，解耦模块依赖
- 异步创建生产流转卡

### 4. 聚合触发
- **triggerFlowIfAllReach()**: 聚合判断方法
- 订单包含多个产品（流转卡）时，只有全部达到目标状态才触发订单级别状态流转
- 支持分布式场景下的状态同步

---

## 六、v2.0 重要变更说明

### 移除的功能
1. **CONFIRM阶段（phase=70）**
   - 移除 AWAITING_CONFIRM 状态（7010）
   - 不需要实体交付的订单不再进入此阶段

2. **USER_CONFIRM动作**
   - 移除客户确认动作
   - 改为业务人员线下确认后手动完成

### 新的业务流程
**不需要实体交付的订单**：
```
设计完成(2030) → [停留] → 业务人员线下与客户确认 → [手动完成] → 已完成(8010)
```

**核心逻辑**：
- 设计完成后，订单停留在 DESIGN_COMPLETED(2030)
- FlowStatusTransitionRules.getAvailableActions() 返回 [COMPLETE] 动作
- 业务人员通过 OrderMainController.manualComplete() 手动完成订单
- 前置条件：needsPhysicalDelivery=0 且 status=2030

### 代码变更
1. **FlowActionEnum.java**
   - 删除 USER_CONFIRM 枚举定义

2. **FlowStatusTransitionRules.java**
   - 移除 USER_CONFIRM 的动作映射
   - DESIGN_COMPLETED + needsPhysicalDelivery=0 → 可执行 COMPLETE 动作

3. **FlowPhaseTransitionRules.java**
   - 移除 USER_CONFIRM 的阶段推进判断
   - DESIGN_COMPLETED + needsPhysicalDelivery=0 → 不自动推进阶段

4. **测试文件**
   - 删除所有 USER_CONFIRM 相关测试用例

---

## 七、总结

### 系统优点
1. ✅ 清晰的阶段和状态分离设计
2. ✅ 灵活的双路径支持（实体交付/非实体交付）
3. ✅ 医疗器械特殊处理（后处理阶段）
4. ✅ 事件驱动的模块解耦
5. ✅ 状态机保证转换合法性
6. ✅ 支持跨阶段回退（质检回退到打印）
7. ✅ 聚合触发机制处理多产品订单

### v2.0 改进
1. ✅ 简化了不需要实体交付订单的流程
2. ✅ 移除了多余的CONFIRM阶段
3. ✅ 业务逻辑更加清晰：线下确认 + 系统手动完成

### 注意事项
1. **不需要实体交付订单的完成**：
   - 必须由业务人员手动触发（OrderMainController.manualComplete）
   - 不会自动推进到COMPLETED状态

2. **生产流转卡的聚合逻辑**：
   - 订单包含多个产品时，所有产品必须达到目标状态才触发订单级别流转
   - triggerFlowIfAllReach() 方法负责聚合判断

3. **跨阶段回退**：
   - 仅质检阶段支持回退到打印阶段（REWORK_TO_PRINT）
   - 其他阶段不支持回退

---

## 八、测试验证

### 测试覆盖
- ✅ FlowStatusTransitionRulesTest: 118个测试全部通过
- ✅ FlowPhaseTransitionRulesTest: 40个测试全部通过
- ✅ FlowStateMachineServiceImplTest: 集成测试全部通过

### 关键场景验证
1. ✅ 需要实体交付 + 医疗器械：完整流程通过
2. ✅ 需要实体交付 + 非医疗器械：跳过后处理，流程通过
3. ✅ 不需要实体交付：停留在DESIGN_COMPLETED，手动完成通过
4. ✅ USER_CONFIRM动作已完全移除，无残留引用

---

**文档完成日期**: 2026-06-04  
**维护者**: hanjor  
**版本**: v2.0
