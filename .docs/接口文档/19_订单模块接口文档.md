# 19_订单模块接口文档

## 基本信息

| 项目 | 说明 |
|------|------|
| 所属模块 | order（订单管理） |
| Controller | OrderController |
| 请求前缀 | /order |
| 认证要求 | 需要登录认证（SaToken） |

---

## 接口列表

### 草稿接口

- [19.1 分页查询我的草稿列表](#191-分页查询我的草稿列表)
- [19.2 查询草稿详情](#192-查询草稿详情)
- [19.3 创建或更新草稿](#193-创建或更新草稿)
- [19.4 删除草稿](#194-删除草稿)
- [19.5 提交草稿转正式订单](#195-提交草稿转正式订单)

### 订单接口

- [19.6 直接创建订单（直提）](#196-直接创建订单直提)
- [19.7 分页查询订单列表](#197-分页查询订单列表)
- [19.8 查询订单详情](#198-查询订单详情)
- [19.9 提交订单](#199-提交订单)
- [19.10 撤回订单](#1910-撤回订单)
- [19.11 审核通过](#1911-审核通过)
- [19.12 审核驳回](#1912-审核驳回)
- [19.13 查询可执行的动作](#1913-查询可执行的动作)
- [19.14 删除订单](#1914-删除订单)

### 列配置接口

- [19.15 获取当前用户列配置](#1915-获取当前用户列配置)
- [19.16 保存用户列配置](#1916-保存用户列配置)
- [19.17 重置用户列配置](#1917-重置用户列配置)

### 导出接口

- [19.18 导出订单列表](#1918-导出订单列表)

---

## 草稿接口

### 19.1 分页查询我的草稿列表

**接口地址**：`GET /order/draft/list`

**是否需要认证**：是

**接口说明**：分页查询当前登录用户的草稿列表，仅返回未提交的草稿（已提交草稿不展示）。

**请求参数**（Query String）：

| 参数名 | 类型 | 必填 | 说明 | 默认值 |
|--------|------|------|------|--------|
| pageNum | Integer | 否 | 页码 | 1 |
| pageSize | Integer | 否 | 每页条数 | 10 |
| hospitalId | Long | 否 | 医院ID（筛选条件） | - |
| status | Integer | 否 | 草稿状态：1-有效，3-已过期 | - |

**请求示例**：
```
GET /order/draft/list?pageNum=1&pageSize=10
Authorization: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
```

**响应参数**：

| 参数名 | 类型 | 说明 |
|--------|------|------|
| code | Integer | 状态码，200表示成功 |
| message | String | 响应消息 |
| data | Object | 分页数据 |
| data.current | Long | 当前页码 |
| data.size | Long | 每页条数 |
| data.total | Long | 总记录数 |
| data.records | Array | 草稿列表 |
| data.records[].id | Long | 草稿ID |
| data.records[].orderType | Integer | 订单类型：1-医疗器械，2-非医疗器械 |
| data.records[].orderTypeName | String | 订单类型名称 |
| data.records[].needsPhysicalDelivery | Integer | 是否需要实体交付：0-否，1-是 |
| data.records[].businessType | String | 业务类型（dict_code，如 "11.1"） |
| data.records[].orgName | String | 提单机构名称 |
| data.records[].hospitalId | Long | 医院ID |
| data.records[].hospitalName | String | 医院名称 |
| data.records[].patientName | String | 患者姓名 |
| data.records[].status | Integer | 草稿状态：1-有效，2-已提交，3-已过期 |
| data.records[].statusName | String | 草稿状态名称 |
| data.records[].itemCount | Integer | 重建项目数量 |
| data.records[].expiresAt | String | 过期时间（yyyy-MM-dd HH:mm:ss） |
| data.records[].createTime | String | 创建时间 |

**响应示例**：
```json
{
    "code": 200,
    "message": "操作成功",
    "timestamp": 1744000000000,
    "data": {
        "current": 1,
        "size": 10,
        "total": 2,
        "records": [
            {
                "id": 100,
                "orderType": 1,
                "orderTypeName": "医疗器械",
                "needsPhysicalDelivery": 1,
                "businessType": "11.1",
                "orgName": "XX医疗科技有限公司",
                "hospitalId": 5,
                "hospitalName": "北京协和医院",
                "patientName": "张三",
                "status": 1,
                "statusName": "有效",
                "itemCount": 2,
                "expiresAt": "2026-05-07 10:00:00",
                "createTime": "2026-04-07 10:00:00"
            }
        ]
    }
}
```

---

### 19.2 查询草稿详情

**接口地址**：`GET /order/draft/{id}`

**是否需要认证**：是

**接口说明**：查询指定草稿的完整详情，包含重建项目列表。只能查询属于当前用户的草稿。

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 草稿ID |

**请求示例**：
```
GET /order/draft/100
Authorization: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
```

**响应参数**：

| 参数名 | 类型 | 说明 |
|--------|------|------|
| data | Object | 草稿详情 |
| data.id | Long | 草稿ID |
| data.operatorId | Long | 操作员ID |
| data.operatorName | String | 操作员姓名 |
| data.operatorPhone | String | 操作员电话 |
| data.orderType | Integer | 订单类型：1-医疗器械，2-非医疗器械 |
| data.orderTypeName | String | 订单类型名称 |
| data.needsPhysicalDelivery | Integer | 是否需要实体交付：0-否，1-是 |
| data.needsPhysicalDeliveryName | String | 是否需要实体交付名称 |
| data.businessType | String | 业务类型（dict_code） |
| data.businessTypeName | String | 业务类型名称 |
| data.orgId | Long | 提单机构ID |
| data.orgName | String | 提单机构名称 |
| data.hospitalId | Long | 医院ID |
| data.hospitalName | String | 医院名称 |
| data.deptId | Long | 科室ID |
| data.deptName | String | 科室名称 |
| data.doctorId | Long | 医生ID |
| data.doctorName | String | 医生姓名 |
| data.doctorPhone | String | 医生电话 |
| data.patientName | String | 患者姓名 |
| data.patientAge | Integer | 患者年龄 |
| data.patientGender | String | 患者性别（dict_code：12.1-男，12.2-女） |
| data.patientGenderName | String | 患者性别名称 |
| data.isUrgent | Integer | 是否加急：0-否，1-是 |
| data.isPostal | Integer | 是否邮寄：0-否，1-是 |
| data.postalAddress | String | 邮寄地址 |
| data.expectedDeliveryDate | String | 期望交付时间 |
| data.status | Integer | 草稿状态：1-有效，2-已提交，3-已过期 |
| data.statusName | String | 草稿状态名称 |
| data.expiresAt | String | 过期时间 |
| data.itemCount | Integer | 重建项目数量 |
| data.items | Array | 重建项目明细列表 |
| data.items[].id | Long | 明细ID |
| data.items[].bodyPartId | Long | 部位ID |
| data.items[].bodyPartName | String | 部位名称 |
| data.items[].projectId | Long | 重建项目ID |
| data.items[].projectName | String | 重建项目名称 |
| data.items[].projectEstimatedHours | Number | 预计耗时（小时，支持小数） |
| data.items[].projectDesc | String | 项目说明 |
| data.items[].formingRequirement | String | 成形需求 |
| data.items[].otherRequirement | String | 其他要求 |
| data.items[].sortOrder | Integer | 排序序号 |
| data.createTime | String | 创建时间 |
| data.updateTime | String | 更新时间 |

**响应示例**：
```json
{
    "code": 200,
    "message": "操作成功",
    "timestamp": 1744000000000,
    "data": {
        "id": 100,
        "operatorId": 1,
        "operatorName": "李四",
        "operatorPhone": "13800138000",
        "orderType": 1,
        "orderTypeName": "医疗器械",
        "needsPhysicalDelivery": 1,
        "needsPhysicalDeliveryName": "需要",
        "businessType": "11.1",
        "businessTypeName": "业务",
        "orgId": 2,
        "orgName": "XX医疗科技有限公司",
        "hospitalId": 5,
        "hospitalName": "北京协和医院",
        "deptId": 10,
        "deptName": "骨科",
        "doctorId": 20,
        "doctorName": "王医生",
        "doctorPhone": "13900139000",
        "patientName": "张三",
        "patientAge": 45,
        "patientGender": "12.1",
        "patientGenderName": "男",
        "isUrgent": 0,
        "isPostal": 1,
        "postalAddress": "北京市朝阳区xxx路xxx号",
        "expectedDeliveryDate": "2026-04-20 10:00:00",
        "status": 1,
        "statusName": "有效",
        "expiresAt": "2026-05-07 10:00:00",
        "itemCount": 1,
        "items": [
            {
                "id": 200,
                "bodyPartId": 3,
                "bodyPartName": "颅骨",
                "projectId": 15,
                "projectName": "颅骨三维重建",
                "projectEstimatedHours": 4.5,
                "projectDesc": "需要高精度重建",
                "formingRequirement": "钛合金",
                "otherRequirement": null,
                "sortOrder": 1
            }
        ],
        "createTime": "2026-04-07 10:00:00",
        "updateTime": "2026-04-07 10:00:00"
    }
}
```

**错误码**：

| 错误码 | 说明 |
|--------|------|
| 676 | 草稿不存在 |
| 679 | 只能查询自己的草稿 |

---

### 19.3 创建或更新草稿

**接口地址**：`POST /order/draft`

**是否需要认证**：是

**接口说明**：创建新草稿或更新已有草稿。传入 `id` 字段时为更新，不传则为新增。草稿默认30天后过期（可通过系统配置 `order.draft.expire.days` 调整）。

**请求参数**（JSON Body）：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 否 | 草稿ID，更新时传入 |
| orderType | Integer | 是 | 订单类型：1-医疗器械，2-非医疗器械 |
| needsPhysicalDelivery | Integer | 是 | 是否需要实体交付：0-不需要，1-需要 |
| businessType | String | 是 | 业务类型（dict_code：11.1-业务，11.2-测试，11.3-试用，11.4-代理） |
| orgId | Long | 是 | 提单机构ID |
| orgName | String | 否 | 提单机构名称 |
| operatorName | String | 否 | 操作员姓名 |
| operatorPhone | String | 否 | 操作员电话（格式：1[3-9]xxxxxxxxx） |
| hospitalId | Long | 是 | 医院ID |
| hospitalName | String | 否 | 医院名称 |
| deptId | Long | 否 | 科室ID |
| deptName | String | 否 | 科室名称 |
| doctorId | Long | 否 | 医生ID（有则自动填充医生信息） |
| doctorName | String | 否 | 医生姓名（doctorId为空时可填，系统自动创建医生记录） |
| doctorPhone | String | 否 | 医生电话（格式：1[3-9]xxxxxxxxx） |
| patientName | String | 是 | 患者姓名 |
| patientAge | Integer | 否 | 患者年龄（0~150） |
| patientGender | String | 否 | 患者性别（dict_code：12.1-男，12.2-女） |
| isUrgent | Integer | 否 | 是否加急：0-否，1-是 |
| isPostal | Integer | 否 | 是否邮寄：0-否，1-是 |
| postalAddress | String | 否 | 邮寄地址 |
| expectedDeliveryDate | String | 否 | 期望交付时间（yyyy-MM-dd HH:mm:ss） |
| items | Array | 否 | 重建项目明细列表（草稿模式可为空） |
| items[].bodyPartId | Long | 否 | 部位ID |
| items[].bodyPartName | String | 否 | 部位名称 |
| items[].projectId | Long | 否 | 重建项目ID |
| items[].projectName | String | 否 | 重建项目名称 |
| items[].projectEstimatedHours | Number | 否 | 预计耗时（小时） |
| items[].projectDesc | String | 否 | 项目说明 |
| items[].formingRequirement | String | 否 | 成形需求 |
| items[].otherRequirement | String | 否 | 其他要求 |
| items[].sortOrder | Integer | 否 | 排序序号（未填则自动赋值） |

**请求示例**：
```json
POST /order/draft
Content-Type: application/json
Authorization: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

{
    "orderType": 1,
    "needsPhysicalDelivery": 1,
    "businessType": "11.1",
    "orgId": 2,
    "hospitalId": 5,
    "patientName": "张三",
    "patientAge": 45,
    "patientGender": "12.1",
    "items": [
        {
            "bodyPartId": 3,
            "projectId": 15,
            "formingRequirement": "钛合金",
            "sortOrder": 1
        }
    ]
}
```

**响应参数**：

| 参数名 | 类型 | 说明 |
|--------|------|------|
| data | Long | 草稿ID |

**响应示例**：
```json
{
    "code": 200,
    "message": "操作成功",
    "timestamp": 1744000000000,
    "data": 100
}
```

**错误码**：

| 错误码 | 说明 |
|--------|------|
| 676 | 草稿不存在（更新时） |
| 679 | 只能修改自己的草稿 |
| 680 | 草稿已提交，不能修改 |

---

### 19.4 删除草稿

**接口地址**：`DELETE /order/draft/{id}`

**是否需要认证**：是

**接口说明**：删除指定草稿（逻辑删除）。只能删除属于当前用户且未提交的草稿。

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 草稿ID |

**请求示例**：
```
DELETE /order/draft/100
Authorization: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
```

**响应示例**：
```json
{
    "code": 200,
    "message": "操作成功",
    "timestamp": 1744000000000,
    "data": null
}
```

**错误码**：

| 错误码 | 说明 |
|--------|------|
| 676 | 草稿不存在 |
| 679 | 只能删除自己的草稿 |
| 680 | 草稿已提交，不能删除 |

---

### 19.5 提交草稿转正式订单

**接口地址**：`POST /order/draft/{id}/submit`

**是否需要认证**：是

**接口说明**：将草稿提交为正式订单。提交前会进行严格校验（机构、医院、患者、明细必填；若系统配置要求则影像文件也必填）。提交成功后草稿状态变为"已提交"，不可再修改。

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 草稿ID |

**请求示例**：
```
POST /order/draft/100/submit
Authorization: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
```

**响应参数**：

| 参数名 | 类型 | 说明 |
|--------|------|------|
| data | Long | 新创建的订单ID |

**响应示例**：
```json
{
    "code": 200,
    "message": "操作成功",
    "timestamp": 1744000000000,
    "data": 1001
}
```

**错误码**：

| 错误码 | 说明 |
|--------|------|
| 676 | 草稿不存在 |
| 679 | 只能提交自己的草稿 |
| 680 | 草稿已提交 |
| 681 | 订单明细不能为空 |
| 682 | 影像文件不能为空（系统配置开启时） |

---

## 订单接口

### 19.6 直接创建订单（直提）

**接口地址**：`POST /order`

**是否需要认证**：是

**接口说明**：直接创建正式订单，跳过草稿步骤。适用于业务员信息填写完整时的快捷提交场景。与草稿提交的区别在于：影像文件通过 `imageDataFileIds`/`imageReportFileIds` 直接传入（文件需提前上传至文件服务）。

**请求参数**（JSON Body）：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| orderType | Integer | 是 | 订单类型：1-医疗器械，2-非医疗器械 |
| needsPhysicalDelivery | Integer | 是 | 是否需要实体交付：0-不需要，1-需要 |
| businessType | String | 是 | 业务类型（dict_code：11.1/11.2/11.3/11.4） |
| orgId | Long | 是 | 提单机构ID |
| orgName | String | 否 | 提单机构名称 |
| operatorName | String | 否 | 操作员姓名 |
| operatorPhone | String | 否 | 操作员电话 |
| hospitalId | Long | 是 | 医院ID |
| hospitalName | String | 否 | 医院名称 |
| deptId | Long | 否 | 科室ID |
| deptName | String | 否 | 科室名称 |
| doctorId | Long | 否 | 医生ID |
| doctorName | String | 否 | 医生姓名 |
| doctorPhone | String | 否 | 医生电话 |
| patientName | String | 是 | 患者姓名 |
| patientAge | Integer | 否 | 患者年龄（0~150） |
| patientGender | String | 否 | 患者性别（dict_code：12.1-男，12.2-女） |
| isUrgent | Integer | 否 | 是否加急：0-否，1-是 |
| isPostal | Integer | 否 | 是否邮寄：0-否，1-是 |
| postalAddress | String | 否 | 邮寄地址 |
| expectedDeliveryDate | String | 否 | 期望交付时间 |
| items | Array | 是 | 重建项目明细列表（至少1条） |
| items[].bodyPartId | Long | 是 | 部位ID |
| items[].projectId | Long | 是 | 重建项目ID |
| items[].projectEstimatedHours | Number | 否 | 预计耗时（小时） |
| items[].projectDesc | String | 否 | 项目说明 |
| items[].formingRequirement | String | 否 | 成形需求 |
| items[].otherRequirement | String | 否 | 其他要求 |
| items[].sortOrder | Integer | 否 | 排序序号 |
| imageDataFileIds | Array | 否 | 影像数据文件ID列表（CT/MRI等，dict_code=10.1） |
| imageReportFileIds | Array | 否 | 影像报告文件ID列表（dict_code=10.2） |

**请求示例**：
```json
POST /order
Content-Type: application/json
Authorization: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

{
    "orderType": 1,
    "needsPhysicalDelivery": 1,
    "businessType": "11.1",
    "orgId": 2,
    "hospitalId": 5,
    "deptId": 10,
    "doctorId": 20,
    "patientName": "张三",
    "patientAge": 45,
    "patientGender": "12.1",
    "isUrgent": 0,
    "isPostal": 1,
    "postalAddress": "北京市朝阳区xxx路xxx号",
    "expectedDeliveryDate": "2026-04-20 10:00:00",
    "items": [
        {
            "bodyPartId": 3,
            "projectId": 15,
            "formingRequirement": "钛合金",
            "sortOrder": 1
        }
    ],
    "imageDataFileIds": ["file-uuid-001", "file-uuid-002"]
}
```

**响应参数**：

| 参数名 | 类型 | 说明 |
|--------|------|------|
| data | Long | 新创建的订单ID |

**响应示例**：
```json
{
    "code": 200,
    "message": "操作成功",
    "timestamp": 1744000000000,
    "data": 1001
}
```

**错误码**：

| 错误码 | 说明 |
|--------|------|
| 681 | 订单明细不能为空 |
| 682 | 影像文件不能为空（系统配置开启时） |

---

### 19.7 分页查询订单列表

**接口地址**：`POST /order/page`

**是否需要认证**：是

**接口说明**：分页查询订单列表，支持多维度筛选。查询结果受当前用户的数据权限（dataScopeType）控制：ALL-全部可见，ORG-仅本机构，HOSPITALS-仅关联医院，SELF-仅自己创建的。当前仅返回订单阶段（phase=1）的数据。

**请求参数**（JSON Body）：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| pageNum | Integer | 否 | 页码（默认1，最小1） |
| pageSize | Integer | 否 | 每页条数（默认10，最大100） |
| orderCode | String | 否 | 订单编号（模糊查询） |
| hospitalId | Long | 否 | 医院ID（必须在用户权限范围内，否则返回空列表） |
| areaId | Long | 否 | 地区ID（精确匹配） |
| doctorName | String | 否 | 医生姓名（模糊查询） |
| patientName | String | 否 | 患者姓名（模糊查询） |
| businessType | String | 否 | 业务类型（精确匹配，如 "11.1"） |
| createTimeStart | String | 否 | 创建时间起始（闭区间，yyyy-MM-dd HH:mm:ss） |
| createTimeEnd | String | 否 | 创建时间结束（闭区间，yyyy-MM-dd HH:mm:ss） |
| bodyPartIds | Array | 否 | 身体部位ID列表（通过 order_item 子查询过滤） |
| projectIds | Array | 否 | 重建项目ID列表（通过 order_item 子查询过滤） |
| operatorId | Long | 否 | 操作员ID（精确匹配） |
| status | Integer | 否 | 订单状态码（精确匹配） |
| sortField | String | 否 | 排序字段（默认 createTime）。允许值：createTime、updateTime、orderCode、patientName、doctorName、hospitalName、areaName、businessType、estimatedCost、expectedDeliveryDate、status、isUrgent |
| sortOrder | String | 否 | 排序方向（默认 DESC，不区分大小写）。允许值：ASC、DESC |

**请求示例**：
```json
POST /order/page
Content-Type: application/json
Authorization: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

{
    "pageNum": 1,
    "pageSize": 10,
    "hospitalId": 5,
    "patientName": "张",
    "sortField": "createTime",
    "sortOrder": "DESC"
}
```

**响应参数**：

| 参数名 | 类型 | 说明 |
|--------|------|------|
| data.current | Long | 当前页码 |
| data.size | Long | 每页条数 |
| data.total | Long | 总记录数 |
| data.records | Array | 订单列表 |
| data.records[].id | Long | 订单ID |
| data.records[].orderCode | String | 订单编号（ORDER_NO格式） |
| data.records[].orderType | Integer | 订单类型：1-医疗器械，2-非医疗器械 |
| data.records[].orderTypeName | String | 订单类型名称 |
| data.records[].needsPhysicalDelivery | Integer | 是否需要实体交付：0-否，1-是 |
| data.records[].businessType | String | 业务类型（dict_code） |
| data.records[].businessTypeName | String | 业务类型名称 |
| data.records[].orgId | Long | 提单机构ID |
| data.records[].orgName | String | 提单机构名称 |
| data.records[].operatorId | Long | 操作员ID |
| data.records[].operatorName | String | 操作员姓名 |
| data.records[].hospitalId | Long | 医院ID |
| data.records[].hospitalName | String | 医院名称 |
| data.records[].areaId | Long | 地区ID |
| data.records[].areaName | String | 地区名称 |
| data.records[].fullAreaName | String | 完整地区路径 |
| data.records[].deptId | Long | 科室ID |
| data.records[].deptName | String | 科室名称 |
| data.records[].doctorId | Long | 医生ID |
| data.records[].doctorName | String | 医生姓名 |
| data.records[].patientName | String | 患者姓名 |
| data.records[].patientAge | Integer | 患者年龄 |
| data.records[].patientGender | String | 患者性别（dict_code） |
| data.records[].patientGenderName | String | 患者性别名称 |
| data.records[].isUrgent | Integer | 是否加急：0-否，1-是 |
| data.records[].isPostal | Integer | 是否邮寄：0-否，1-是 |
| data.records[].designerId | Long | 设计师ID |
| data.records[].designerName | String | 设计师姓名 |
| data.records[].phase | Integer | 当前阶段（1-8） |
| data.records[].status | Integer | 当前状态码 |
| data.records[].estimatedCost | Number | 预计费用（元） |
| data.records[].expectedDeliveryDate | String | 期望交付时间 |
| data.records[].createTime | String | 创建时间 |
| data.records[].rebuildProjectList | Array | 重建项目摘要列表 |
| data.records[].rebuildProjectList[].projectName | String | 项目名称 |
| data.records[].rebuildProjectList[].bodyPartName | String | 部位名称 |
| data.records[].rebuildProjectList[].categoryCode | String | 项目分类编码（字典 dict_code=13） |
| data.records[].rebuildProjectList[].categoryName | String | 项目分类名称 |

**响应示例**：
```json
{
    "code": 200,
    "message": "操作成功",
    "timestamp": 1744000000000,
    "data": {
        "current": 1,
        "size": 10,
        "total": 1,
        "records": [
            {
                "id": 1001,
                "orderCode": "ORDER-20260407-0001",
                "orderType": 1,
                "orderTypeName": "医疗器械",
                "needsPhysicalDelivery": 1,
                "businessType": "11.1",
                "businessTypeName": "业务",
                "orgId": 2,
                "orgName": "XX医疗科技有限公司",
                "operatorId": 1,
                "operatorName": "李四",
                "hospitalId": 5,
                "hospitalName": "北京协和医院",
                "areaId": 110105,
                "areaName": "朝阳区",
                "fullAreaName": "中国,北京,北京市,朝阳区",
                "deptId": 10,
                "deptName": "骨科",
                "doctorId": 20,
                "doctorName": "王医生",
                "patientName": "张三",
                "patientAge": 45,
                "patientGender": "12.1",
                "patientGenderName": "男",
                "isUrgent": 0,
                "isPostal": 1,
                "designerId": null,
                "designerName": null,
                "phase": 1,
                "status": 10,
                "estimatedCost": null,
                "expectedDeliveryDate": "2026-04-20 10:00:00",
                "createTime": "2026-04-07 10:00:00",
                "rebuildProjectList": [
                    {
                        "projectName": "颅骨三维重建",
                        "bodyPartName": "颅骨",
                        "categoryCode": "13.1",
                        "categoryName": "模型"
                    }
                ]
            }
        ]
    }
}
```

---

### 19.8 查询订单详情

**接口地址**：`GET /order/{id}`

**是否需要认证**：是

**接口说明**：查询指定订单的完整详情，包含订单明细、文件列表和当前可执行动作。同样受数据权限控制，无权限访问时返回错误。

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 订单ID |

**请求示例**：
```
GET /order/1001
Authorization: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
```

**响应参数**：

> 在 19.7 订单列表字段基础上，详情额外包含以下字段：

| 参数名 | 类型 | 说明 |
|--------|------|------|
| data.currentHandlerId | Long | 当前处理人ID |
| data.currentHandlerName | String | 当前处理人姓名 |
| data.designerId | Long | 设计师ID |
| data.producerId | Long | 生产员ID |
| data.auditRemark | String | 审核备注（驳回原因等） |
| data.designReviewRemark | String | 设计审核备注 |
| data.dataEvaluationOpinion | String | 数据评估意见 |
| data.designStartTime | String | 设计开始时间 |
| data.designSubmitTime | String | 设计提交时间 |
| data.actualCompleteTime | String | 实际完成时间 |
| data.updateTime | String | 更新时间 |
| data.itemCount | Integer | 明细数量 |
| data.availableActions | Array | 当前可执行动作列表（如 ["SUBMIT_ORDER", "WITHDRAW"]） |
| data.items | Array | 订单明细列表 |
| data.items[].id | Long | 明细ID |
| data.items[].bodyPartId | Long | 部位ID |
| data.items[].bodyPartName | String | 部位名称 |
| data.items[].projectId | Long | 重建项目ID |
| data.items[].projectName | String | 重建项目名称 |
| data.items[].categoryCode | String | 项目分类编码（字典 dict_code=13，如 13.1=模型） |
| data.items[].categoryName | String | 项目分类名称 |
| data.items[].projectEstimatedHours | Number | 预计耗时（小时） |
| data.items[].projectDesc | String | 项目说明 |
| data.items[].formingRequirement | String | 成形需求 |
| data.items[].otherRequirement | String | 其他要求 |
| data.items[].sortOrder | Integer | 排序序号 |
| data.imageDataFiles | Array | 影像数据文件列表（dict_code=10.1） |
| data.imageReportFiles | Array | 影像报告文件列表（dict_code=10.2） |
| data.imageDataFiles[].fileId | String | 文件ID |
| data.imageDataFiles[].fileName | String | 文件名称 |
| data.imageDataFiles[].fileUrl | String | 公开访问URL |
| data.imageDataFiles[].thUrl | String | 缩略图URL |
| data.imageDataFiles[].fileSize | Long | 文件大小（字节） |
| data.imageDataFiles[].fileSizeText | String | 格式化文件大小（如 "2.35 MB"） |
| data.imageDataFiles[].fileExt | String | 文件扩展名 |

**响应示例**：
```json
{
    "code": 200,
    "message": "操作成功",
    "timestamp": 1744000000000,
    "data": {
        "id": 1001,
        "orderCode": "ORDER-20260407-0001",
        "orderType": 1,
        "orderTypeName": "医疗器械",
        "needsPhysicalDelivery": 1,
        "needsPhysicalDeliveryName": "需要",
        "businessType": "11.1",
        "orgId": 2,
        "orgName": "XX医疗科技有限公司",
        "operatorId": 1,
        "operatorName": "李四",
        "operatorPhone": "13800138000",
        "hospitalId": 5,
        "hospitalName": "北京协和医院",
        "deptId": 10,
        "deptName": "骨科",
        "doctorId": 20,
        "doctorName": "王医生",
        "doctorPhone": "13900139000",
        "patientName": "张三",
        "patientAge": 45,
        "patientGender": "12.1",
        "patientGenderName": "男",
        "isUrgent": 0,
        "isPostal": 1,
        "postalAddress": "北京市朝阳区xxx路xxx号",
        "expectedDeliveryDate": "2026-04-20 10:00:00",
        "phase": 1,
        "status": 10,
        "currentHandlerId": null,
        "currentHandlerName": null,
        "designerId": null,
        "producerId": null,
        "auditRemark": null,
        "designReviewRemark": null,
        "estimatedCost": null,
        "dataEvaluationOpinion": null,
        "designStartTime": null,
        "designSubmitTime": null,
        "actualCompleteTime": null,
        "createTime": "2026-04-07 10:00:00",
        "updateTime": "2026-04-07 10:00:00",
        "itemCount": 1,
        "availableActions": ["SUBMIT_ORDER"],
        "items": [
            {
                "id": 501,
                "bodyPartId": 3,
                "bodyPartName": "颅骨",
                "projectId": 15,
                "projectName": "颅骨三维重建",
                "categoryCode": "13.1",
                "categoryName": "模型",
                "projectEstimatedHours": 4.5,
                "projectDesc": "需要高精度重建",
                "formingRequirement": "钛合金",
                "otherRequirement": null,
                "sortOrder": 1
            }
        ],
        "imageDataFiles": [
            {
                "fileId": "file-uuid-001",
                "fileName": "CT扫描数据.dcm",
                "fileUrl": "https://oss.example.com/files/file-uuid-001.dcm",
                "thUrl": null,
                "fileSize": 52428800,
                "fileSizeText": "50.00 MB",
                "fileExt": "dcm"
            }
        ],
        "imageReportFiles": []
    }
}
```

**错误码**：

| 错误码 | 说明 |
|--------|------|
| 675 | 订单不存在 |
| 无权限错误码 | 无权查看该订单（数据权限不足） |

---

### 19.9 提交订单

**接口地址**：`POST /order/{id}/submit`

**是否需要认证**：是

**接口说明**：将订单从草稿状态提交审核，触发状态流转（SUBMIT_ORDER 动作）。

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 订单ID |

**请求示例**：
```
POST /order/1001/submit
Authorization: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
```

**响应示例**：
```json
{
    "code": 200,
    "message": "操作成功",
    "timestamp": 1744000000000,
    "data": null
}
```

**错误码**：

| 错误码 | 说明 |
|--------|------|
| 675 | 订单不存在 |
| 678 | 当前状态不允许执行该操作 |

---

### 19.10 撤回订单

**接口地址**：`POST /order/{id}/withdraw`

**是否需要认证**：是

**接口说明**：将待审核的订单撤回到草稿状态，触发状态流转（WITHDRAW 动作）。

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 订单ID |

**请求示例**：
```
POST /order/1001/withdraw
Authorization: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
```

**响应示例**：
```json
{
    "code": 200,
    "message": "操作成功",
    "timestamp": 1744000000000,
    "data": null
}
```

**错误码**：

| 错误码 | 说明 |
|--------|------|
| 675 | 订单不存在 |
| 678 | 当前状态不允许执行该操作 |

---

### 19.11 审核通过

**接口地址**：`POST /order/{id}/audit-pass`

**是否需要认证**：是

**接口说明**：审核员对待审核订单执行审核通过操作，触发状态流转（DATA_AUDIT_PASS 动作）。

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 订单ID |

**请求参数**（JSON Body）：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| remark | String | 否 | 审核备注 |

**请求示例**：
```json
POST /order/1001/audit-pass
Content-Type: application/json
Authorization: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

{
    "remark": "数据完整，审核通过"
}
```

**响应示例**：
```json
{
    "code": 200,
    "message": "操作成功",
    "timestamp": 1744000000000,
    "data": null
}
```

**错误码**：

| 错误码 | 说明 |
|--------|------|
| 675 | 订单不存在 |
| 678 | 当前状态不允许执行该操作 |

---

### 19.12 审核驳回

**接口地址**：`POST /order/{id}/audit-reject`

**是否需要认证**：是

**接口说明**：审核员对待审核订单执行驳回操作，触发状态流转（DATA_AUDIT_REJECT 动作）。驳回时必须填写驳回原因。

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 订单ID |

**请求参数**（JSON Body）：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| remark | String | 是 | 驳回原因（必填） |

**请求示例**：
```json
POST /order/1001/audit-reject
Content-Type: application/json
Authorization: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

{
    "remark": "影像数据质量不合格，请重新上传"
}
```

**响应示例**：
```json
{
    "code": 200,
    "message": "操作成功",
    "timestamp": 1744000000000,
    "data": null
}
```

**错误码**：

| 错误码 | 说明 |
|--------|------|
| 675 | 订单不存在 |
| 678 | 当前状态不允许执行该操作 |
| 700 | 驳回原因不能为空 |
| 704 | 审核驳回次数超限 |

---

### 19.13 查询可执行的动作

**接口地址**：`GET /order/{id}/actions`

**是否需要认证**：是

**接口说明**：查询当前订单状态下可执行的动作列表，前端据此控制按钮显示/隐藏。

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 订单ID |

**请求示例**：
```
GET /order/1001/actions
Authorization: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
```

**响应参数**：

| 参数名 | 类型 | 说明 |
|--------|------|------|
| data | Array | 可执行的动作名称列表（FlowActionEnum 枚举名） |

**响应示例**：
```json
{
    "code": 200,
    "message": "操作成功",
    "timestamp": 1744000000000,
    "data": ["SUBMIT_ORDER", "WITHDRAW"]
}
```

**常见动作值说明**：

| 动作名称 | 说明 |
|---------|------|
| SUBMIT_ORDER | 提交订单 |
| WITHDRAW | 撤回订单 |
| DATA_AUDIT_PASS | 审核通过 |
| DATA_AUDIT_REJECT | 审核驳回 |

**错误码**：

| 错误码 | 说明 |
|--------|------|
| 675 | 订单不存在 |

---

### 19.14 删除订单

**接口地址**：`DELETE /order/{id}`

**是否需要认证**：是

**接口说明**：删除指定订单（逻辑删除）。仅允许删除处于草稿/初始状态的订单。

**路径参数**：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 订单ID |

**请求示例**：
```
DELETE /order/1001
Authorization: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
```

**响应示例**：
```json
{
    "code": 200,
    "message": "操作成功",
    "timestamp": 1744000000000,
    "data": null
}
```

**错误码**：

| 错误码 | 说明 |
|--------|------|
| 675 | 订单不存在 |
| 677 | 当前状态不允许删除 |

---

## 列配置接口

### 19.15 获取当前用户列配置

**接口地址**：`GET /order/column-config`

**是否需要认证**：是

**接口说明**：获取当前用户在订单列表的列显示配置，优先返回用户个人配置，不存在则返回系统默认配置。

**请求示例**：
```
GET /order/column-config
Authorization: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
```

**响应参数**：

| 参数名 | 类型 | 说明 |
|--------|------|------|
| data | Object | 列配置 VO |
| data.columns | Array | 列定义列表 |
| data.columns[].key | String | 列标识 |
| data.columns[].label | String | 列标题 |
| data.columns[].visible | Boolean | 是否显示 |
| data.columns[].width | Integer | 列宽（px） |

**响应示例**：
```json
{
    "code": 200,
    "message": "操作成功",
    "timestamp": 1744000000000,
    "data": {
        "columns": [
            {"key": "orderCode", "label": "订单编号", "visible": true, "width": 160},
            {"key": "patientName", "label": "患者姓名", "visible": true, "width": 100},
            {"key": "hospitalName", "label": "医院名称", "visible": true, "width": 180},
            {"key": "status", "label": "状态", "visible": true, "width": 100},
            {"key": "createTime", "label": "创建时间", "visible": true, "width": 160}
        ]
    }
}
```

---

### 19.16 保存用户列配置

**接口地址**：`PUT /order/column-config`

**是否需要认证**：是

**接口说明**：保存当前用户的订单列表列配置。

**请求参数**（JSON Body）：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| columns | Array | 是 | 列定义列表 |
| columns[].key | String | 是 | 列标识 |
| columns[].visible | Boolean | 是 | 是否显示 |
| columns[].width | Integer | 否 | 列宽（px） |

**请求示例**：
```json
PUT /order/column-config
Content-Type: application/json
Authorization: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

{
    "columns": [
        {"key": "orderCode", "visible": true, "width": 160},
        {"key": "patientName", "visible": true, "width": 100},
        {"key": "hospitalName", "visible": false, "width": 180}
    ]
}
```

**响应示例**：
```json
{
    "code": 200,
    "message": "操作成功",
    "timestamp": 1744000000000,
    "data": null
}
```

---

### 19.17 重置用户列配置

**接口地址**：`DELETE /order/column-config`

**是否需要认证**：是

**接口说明**：删除当前用户的个人列配置，恢复系统默认列配置。

**请求示例**：
```
DELETE /order/column-config
Authorization: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
```

**响应示例**：
```json
{
    "code": 200,
    "message": "操作成功",
    "timestamp": 1744000000000,
    "data": null
}
```

---

## 导出接口

### 19.18 导出订单列表

**接口地址**：`POST /order/export`

**是否需要认证**：是

**接口说明**：将订单列表导出为 Excel 文件，支持与分页查询（19.7）相同的筛选条件和排序参数。最多导出10000条，超出部分截断。直接写入响应流，响应类型为 `application/octet-stream`。

**请求参数**（JSON Body）：

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| orderCode | String | 否 | 订单编号（模糊查询） |
| hospitalId | Long | 否 | 医院ID |
| areaId | Long | 否 | 地区ID |
| doctorName | String | 否 | 医生姓名（模糊查询） |
| patientName | String | 否 | 患者姓名（模糊查询） |
| businessType | String | 否 | 业务类型（dict_code） |
| createTimeStart | String | 否 | 创建时间起始 |
| createTimeEnd | String | 否 | 创建时间结束 |
| status | Integer | 否 | 订单状态码 |
| sortField | String | 否 | 排序字段（同 19.7，默认 createTime） |
| sortOrder | String | 否 | 排序方向（同 19.7，默认 DESC） |

**请求示例**：
```json
POST /order/export
Content-Type: application/json
Authorization: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

{
    "createTimeStart": "2026-04-01 00:00:00",
    "createTimeEnd": "2026-04-07 23:59:59"
}
```

**响应**：Excel 文件流（`Content-Type: application/octet-stream`），前端需处理文件下载。

---

## 数据结构说明

### 订单阶段与状态对照

| 阶段（phase） | 阶段名称 | 常见状态码（status） | 状态说明 |
|-------------|---------|-------------------|---------|
| 1 | 订单阶段（ORDER） | 10 | 草稿（待提交） |
| 1 | 订单阶段（ORDER） | 11 | 待审核 |
| 1 | 订单阶段（ORDER） | 12 | 审核通过 |
| 1 | 订单阶段（ORDER） | 13 | 审核驳回 |
| 2 | 设计阶段（DESIGN） | 20+ | 设计相关状态 |
| 3-8 | 后续阶段 | 30-80 | 打印/后处理/质检/仓储/确认/完成 |

> 完整的阶段与状态定义参见 `.docs/技术实现/order/00_订单状态机设计方案.md`。

### 字典值说明

| 字典分类 | dict_code | 含义 |
|---------|-----------|------|
| 订单类型 | - | 1=医疗器械，2=非医疗器械（非字典，枚举值） |
| 业务类型 | 11.1 | 业务 |
| 业务类型 | 11.2 | 测试 |
| 业务类型 | 11.3 | 试用 |
| 业务类型 | 11.4 | 代理 |
| 患者性别 | 12.1 | 男 |
| 患者性别 | 12.2 | 女 |
| 文件类别 | 10.1 | 影像数据（CT/MRI等） |
| 文件类别 | 10.2 | 影像报告 |
| 文件类别 | 10.3 | 附件 |
| 文件类别 | 10.4 | 打印包 |
| 文件类别 | 10.5 | 设计报告 |
| 文件类别 | 10.6 | 可视化模型 |
| 重建项目分类 | 13.1 | 模型 |
| 重建项目分类 | 13.2 | 导板 |
| 重建项目分类 | 13.3 | 假体 |
| 重建项目分类 | 13.4 | 其他 |

---

## 错误码汇总

| 错误码 | 说明 |
|--------|------|
| 675 | 订单不存在 |
| 676 | 草稿不存在 |
| 677 | 订单状态异常，不允许该操作 |
| 678 | 状态流转失败（不合法的状态转换） |
| 679 | 只能操作自己的草稿 |
| 680 | 草稿已提交，不能再修改/删除 |
| 681 | 订单明细不能为空 |
| 682 | 影像文件不能为空（系统配置 order.image.required=true 时） |
| 700 | 审核驳回时驳回原因不能为空 |
| 701 | 修改申请不存在 |
| 702 | 修改申请已审核 |
| 703 | 超过可修改时间窗口（默认提交后10分钟） |
| 704 | 审核驳回次数超限（FlowContext 保护） |
| 705 | 返工次数超限（FlowContext 保护） |
| 706 | 设计驳回次数超限（FlowContext 保护） |

---

## 变更记录

| 版本 | 日期 | 修改内容 | 作者 |
|------|------|---------|------|
| 1.0 | 2026-04-07 | 初始版本：订单模块完整接口文档 | hanjor |
| 1.1 | 2026-04-07 | 新增动态排序参数（19.7 和 19.18）：sortField、sortOrder | hanjor |
| 1.2 | 2026-04-08 | 订单明细/草稿明细 category 字段拆分为 categoryCode + categoryName，新增字典 dict_code=13 说明 | hanjor |
