# 08_鉴权功能实现方案

## 一、模块概述

鉴权模块是医工宝系统安全体系的核心模块，负责处理用户的登录认证、Token 管理、登出操作和密码修改等功能。该模块整合了 Sa-Token 框架作为认证基础设施，结合系统的用户管理、资源管理和系统配置模块，构建起完整的身份认证和权限控制体系。

### 1.1 核心能力

- 三种登录方式：账号密码（PASSWORD）、手机验证码（PHONE）、邮箱验证码（EMAIL）
- 图形验证码校验（PASSWORD 登录时必填）
- 短信/邮件验证码发送与校验
- Token 管理与自动续期
- 用户登出
- 当前用户信息获取（含菜单树和权限列表）
- 密码修改
- 忘记密码：发送验证码 + 重置密码
- 登录失败锁定机制
- 登录日志记录（含登录方式）
- 公共接口的认证放行

### 1.2 模块位置

```
yigongbao-module-system/
└── src/main/java/com/yigongbao/module/system/auth/
    ├── controller/AuthController.java           # 认证接口
    ├── service/
    │   ├── AuthService.java                  # 认证服务接口
    │   ├── impl/AuthServiceImpl.java         # 认证服务实现
    │   ├── CaptchaService.java              # 验证码服务接口
    │   ├── impl/CaptchaServiceImpl.java     # 验证码服务实现
    │   ├── SmsService.java                  # 短信发送服务接口
    │   ├── impl/MockSmsServiceImpl.java     # 短信发送（非生产环境 Mock 实现）
    │   └── MailService.java                 # 邮件发送服务接口
    ├── mapper/
    │   └── LoginLogMapper.java              # 登录日志Mapper
    ├── entity/
    │   └── LoginLogEntity.java              # 登录日志实体
    ├── dto/
    │   ├── LoginDTO.java                   # 登录请求参数
    │   ├── SendCaptchaDTO.java             # 发送验证码请求参数
    │   ├── ForgotPasswordResetDTO.java     # 忘记密码重置请求参数
    │   └── ChangePasswordDTO.java         # 修改密码请求参数
    ├── enums/
    │   ├── LoginTypeEnum.java              # 登录方式枚举（PASSWORD/PHONE/EMAIL）
    │   ├── CaptchaTypeEnum.java            # 验证码类型枚举（PHONE/EMAIL）
    │   └── CaptchaSceneEnum.java           # 验证码场景枚举（LOGIN/FORGOT）
    └── vo/
        ├── GraphicCaptchaVO.java          # 图形验证码响应
        ├── LoginVO.java                   # 登录响应视图对象
        └── LoginLogVO.java                # 登录日志视图对象

yigongbao-framework/
└── src/main/java/com/yigongbao/framework/
    ├── config/
    │   ├── SaTokenConfig.java             # Sa-Token 配置
    │   ├── SecurityConfig.java            # 安全配置（密码编码器）
    │   └── WebMvcConfig.java              # Web MVC 配置
    ├── handler/
    │   └── GlobalExceptionHandler.java    # 全局异常处理器
    └── aspect/
        └── PermissionAspect.java          # 权限校验切面（预留）

yigongbao-common/
└── src/main/java/com/yigongbao/common/
    ├── constant/StatusConstants.java      # 状态常量
    ├── exception/BusinessException.java   # 业务异常
    ├── enums/ErrorCodeEnum.java          # 错误码枚举
    └── result/Result.java                # 统一响应封装
```

## 二、数据库设计

### 2.1 登录日志表（sys_login_log）

```sql
CREATE TABLE sys_login_log (
    id              BIGINT          NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    user_id         BIGINT          COMMENT '用户ID',
    username        VARCHAR(64)     COMMENT '用户名',
    login_type      VARCHAR(16)     COMMENT '登录方式（PASSWORD/PHONE/EMAIL）',
    ip              VARCHAR(64)     COMMENT '登录IP',
    user_agent      VARCHAR(512)    COMMENT 'User-Agent（浏览器/设备信息）',
    login_time      TIMESTAMP       NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '登录时间',
    login_status    TINYINT         NOT NULL COMMENT '登录结果（1=成功，0=失败）',
    fail_reason     VARCHAR(256)    COMMENT '失败原因',

    PRIMARY KEY (id),
    KEY idx_login_user_id (user_id),
    KEY idx_login_time (login_time)
);
```

## 三、API 接口设计

### 3.1 接口列表

| 方法 | 路径 | 说明 | 认证 |
|-----|------|------|------|
| GET | `/api/system/auth/graphic-captcha` | 获取图形验证码 | 无需认证 |
| POST | `/api/system/auth/login` | 用户登录（支持三种方式） | 无需认证 |
| POST | `/api/system/auth/captcha` | 发送登录验证码（PHONE/EMAIL） | 无需认证 |
| POST | `/api/system/auth/forgot-password/captcha` | 发送忘记密码验证码 | 无需认证 |
| POST | `/api/system/auth/forgot-password/reset` | 忘记密码重置 | 无需认证 |
| POST | `/api/system/auth/logout` | 用户登出 | 需要认证 |
| GET | `/api/system/auth/info` | 获取当前用户信息 | 需要认证 |
| PUT | `/api/system/auth/password` | 修改密码 | 需要认证 |

### 3.2 请求/响应示例

**获取图形验证码**

```
GET /api/system/auth/graphic-captcha
```

响应：
```json
{
  "code": 200,
  "data": {
    "captchaId": "d5a9f3e2-...",
    "imageBase64": "data:image/png;base64,iVBOR..."
  }
}
```

**账号密码登录**

```json
POST /api/system/auth/login
{
  "loginType": "PASSWORD",
  "principal": "admin",
  "credential": "123456",
  "captchaId": "d5a9f3e2-...",
  "captchaCode": "k7mx"
}
```

**手机验证码登录**

```json
POST /api/system/auth/login
{
  "loginType": "PHONE",
  "principal": "13800000001",
  "credential": "123456"
}
```

**邮箱验证码登录**

```json
POST /api/system/auth/login
{
  "loginType": "EMAIL",
  "principal": "user@example.com",
  "credential": "123456"
}
```

**LoginDTO 字段说明**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| loginType | String | 是 | 登录方式：PASSWORD / PHONE / EMAIL |
| principal | String | 是 | 登录主体：USERNAME（账号密码）/ 手机号 / 邮箱 |
| credential | String | 是 | 凭据：密码 / 短信验证码 / 邮箱验证码 |
| captchaId | String | 否 | 图形验证码ID（PASSWORD 登录时必填） |
| captchaCode | String | 否 | 图形验证码（PASSWORD 登录时必填） |

**登录成功响应（LoginVO）**

```json
{
  "code": 200,
  "data": {
    "token": "xxxx-xxxx-xxxx-xxxx"
  }
}
```

> 注意：`login()` 仅返回 token。菜单、权限等完整信息通过 `GET /system/auth/info` 获取。

**发送登录/忘记密码验证码（SendCaptchaDTO）**

```json
POST /api/system/auth/captcha
{
  "captchaType": "PHONE",
  "target": "13800000001"
}
```

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| captchaType | String | 是 | PHONE 或 EMAIL |
| target | String | 是 | 手机号或邮箱 |

**忘记密码重置（ForgotPasswordResetDTO）**

```json
POST /api/system/auth/forgot-password/reset
{
  "captchaType": "PHONE",
  "target": "13800000001",
  "captcha": "123456",
  "newPassword": "new_pass"
}
```

**修改密码请求**

```json
PUT /api/system/auth/password
Authorization: xxxx-xxxx-xxxx-xxxx

{
  "oldPassword": "123456",
  "newPassword": "654321"
}
```

## 四、登录认证流程

### 4.1 账号密码登录（PASSWORD）

```
login(dto) → loginType=PASSWORD
  │
  ├─0. 获取客户端 IP / User-Agent
  │
  ├─1. 校验图形验证码
  │    └─ verifyGraphicCaptcha(captchaId, captchaCode)
  │         ├─ Redis GET graphic:captcha:{captchaId}
  │         ├─ key 不存在 → 抛出 CAPTCHA_GRAPHIC_EXPIRED(773)
  │         ├─ Redis DEL（一次性使用）
  │         └─ 大小写不敏感比对
  │              └─ 不匹配 → 抛出 CAPTCHA_GRAPHIC_ERROR(774)
  │
  ├─2. 查询用户（按用户名）
  │    └─ userMapper.selectByUsername(principal)
  │         └─ null → 记录失败日志 → 抛出 USERNAME_OR_PASSWORD_ERROR(608)
  │
  ├─3. 校验用户状态
  │    └─ status=0 → 记录失败日志 → 抛出 USER_DISABLED(602)
  │
  ├─4. 校验账户是否锁定
  │    └─ isAccountLocked(user) → 抛出 ACCOUNT_LOCKED(609)
  │
  ├─5. 校验密码
  │    └─ passwordEncoder.matches(credential, user.password)
  │         └─ 不匹配 → handleLoginFailure → 抛出 PASSWORD_ERROR(605)
  │
  └─6. 登录成功 → buildLoginSuccess(user, ip, ua, PASSWORD)
```

### 4.2 手机/邮箱验证码登录（PHONE / EMAIL）

```
login(dto) → loginType=PHONE 或 EMAIL
  │
  ├─0. 获取客户端 IP / User-Agent
  │
  ├─1. 校验验证码
  │    └─ captchaService.verifyCaptcha(LOGIN, type, principal, credential)
  │         ├─ key 不存在或已过期 → 抛出 CAPTCHA_EXPIRED(639)
  │         ├─ 尝试次数超过 5 次 → 抛出 CAPTCHA_EXPIRED(639)（作废）
  │         └─ 不匹配 → 记录失败次数 → 抛出 CAPTCHA_ERROR(640)
  │
  ├─2. 查询用户（按手机号/邮箱）
  │    └─ userMapper.selectByPhone/selectByEmail(principal)
  │         └─ null → 记录失败日志 → 抛出 USERNAME_OR_PASSWORD_ERROR(608)
  │
  ├─3. 校验用户状态
  │    └─ status=0 → 记录失败日志 → 抛出 USER_DISABLED(602)
  │
  └─4. 登录成功 → buildLoginSuccess(user, ip, ua, PHONE/EMAIL)
```

### 4.3 登录成功处理（buildLoginSuccess）

```
buildLoginSuccess(user, ip, ua, loginType)
  │
  ├─1. 重置登录失败计数（PASSWORD 登录路径才有此步）
  │    └─ loginFailCount=0, lockTime=null → updateById
  │
  ├─2. Sa-Token 登录
  │    └─ StpUtil.login(userId)
  │
  ├─3. 记录登录成功日志
  │    └─ saveLoginLog(userId, username, ip, ua, loginType, 1, null)
  │
  └─4. 返回 LoginVO { token }
```

### 4.4 图形验证码生成流程

```
getGraphicCaptcha()
  │
  ├─1. Hutool 生成 LineCaptcha（4位，含干扰线）
  ├─2. UUID 生成 captchaId
  ├─3. 存入 Redis：graphic:captcha:{captchaId} = 验证码文本（小写），TTL 300s
  └─4. 返回 GraphicCaptchaVO { captchaId, imageBase64 }
```

### 4.5 登录失败处理流程（handleLoginFailure）

```
handleLoginFailure(user, ip, ua, loginType, failReason)
  │
  ├─1. 记录失败日志
  │
  ├─2. 递增失败计数
  │    └─ newFailCount = user.loginFailCount + 1
  │
  ├─3. 判断是否达到锁定阈值（login.max.failures，默认 5）
  │    ├─ >= 阈值 → 设置 lockTime=now，更新用户
  │    └─ < 阈值 → 仅更新失败计数
  │
  └─4. 抛出 PASSWORD_ERROR(605)
```

### 4.6 账户锁定与自动解锁

```
isAccountLocked(user)
  │
  ├─ lockTime=null → 返回 false（未锁定）
  └─ lockTime + lockDuration > now → 返回 true（仍在锁定）
       否则 → 返回 false（锁定已过期，自动解锁）

锁定时长：login.lock.duration 系统配置（默认 15 分钟）
```

## 五、验证码服务（CaptchaService）

### 5.1 Redis 键结构

| 键模板 | 说明 | TTL |
|--------|------|-----|
| `captcha:{scene}:{type}:{target}` | 验证码正文 | captcha.expire.seconds（默认 300s） |
| `captcha:cooldown:{scene}:{type}:{target}` | 发送冷却 | captcha.cooldown.seconds（默认 60s） |
| `captcha:daily:{scene}:{type}:{target}:{yyyyMMdd}` | 今日发送次数 | 至次日 0 点 |
| `captcha:attempts:{scene}:{type}:{target}` | 校验失败次数 | 同验证码 TTL |
| `graphic:captcha:{captchaId}` | 图形验证码 | 300s |

### 5.2 发送验证码流程

```
sendCaptcha(scene, type, target)
  │
  ├─1. 冷却检查：cooldown key 存在 → 抛出 CAPTCHA_TOO_FREQUENT(637)
  │
  ├─2. 每日限额：daily key 值 >= 10 → 抛出 CAPTCHA_DAILY_LIMIT(638)
  │
  ├─3. 生成 6 位随机数字验证码
  │
  ├─4. 存入 Redis（覆盖旧验证码和失败计数）
  │
  ├─5. 设置冷却 key（TTL = cooldown.seconds）
  │
  ├─6. 每日计数 INCR（首次设置至次日 0 点 TTL）
  │
  └─7. 分发：type=PHONE → SmsService；type=EMAIL → MailService
```

### 5.3 校验验证码流程

```
verifyCaptcha(scene, type, target, code)
  │
  ├─1. 获取失败次数，>= 5 次 → 删除验证码 key → 抛出 CAPTCHA_EXPIRED(639)
  │
  ├─2. Redis GET 验证码；不存在 → 抛出 CAPTCHA_EXPIRED(639)
  │
  ├─3. 比对（不区分大小写）
  │    └─ 不匹配 → failures INCR → 抛出 CAPTCHA_ERROR(640)
  │
  └─4. 匹配 → 删除验证码 key 和失败计数（一次性）
```

## 六、忘记密码流程

### 6.1 发送忘记密码验证码

```
sendForgotPasswordCaptcha(dto)
  │
  ├─1. 查询用户是否存在（按手机号/邮箱）
  │    └─ 不存在 → 静默成功（防止用户枚举攻击）
  │
  └─2. 用户存在 → captchaService.sendCaptcha(FORGOT, type, target)
```

### 6.2 重置密码

```
resetPassword(dto)
  │
  ├─1. captchaService.verifyCaptcha(FORGOT, type, target, captcha)
  │
  ├─2. 查询用户（按手机号/邮箱）
  │    └─ 不存在 → 抛出 USER_NOT_FOUND(600)
  │
  ├─3. 更新密码
  │    └─ BCrypt encode(newPassword) → user.password
  │
  ├─4. 重置锁定状态
  │    └─ loginFailCount=0, lockTime=null
  │
  └─5. updateById(user)
```

## 七、用户登出与信息获取

### 7.1 登出流程

```
logout()
  │
  ├─1. StpUtil.getLoginIdAsLong()
  ├─2. StpUtil.logout()
  └─3. log.info("用户登出成功，userId={}", userId)
```

### 7.2 获取当前用户信息

```
getCurrentUserInfo()
  │
  ├─1. StpUtil.getLoginIdAsLong()
  ├─2. userMapper.selectById(userId) → null → 抛出 USER_NOT_FOUND(600)
  ├─3. resourceService.getUserMenuTree(userId)
  ├─4. resourceService.getUserPermissions(userId)
  └─5. 返回 LoginVO { token, user, menus, permissions }
```

## 八、密码修改流程

```
changePassword(userId, dto)
  │
  ├─1. userMapper.selectById(userId) → null → 抛出 USER_NOT_FOUND(600)
  ├─2. passwordEncoder.matches(oldPassword, user.password)
  │    └─ 不匹配 → 抛出 OLD_PASSWORD_ERROR(606)
  ├─3. passwordEncoder.matches(newPassword, user.password)
  │    └─ 相同 → 抛出 NEW_PASSWORD_SAME_AS_OLD(607)
  ├─4. user.password = encode(newPassword)
  └─5. updateById(user)
```

## 九、Sa-Token 认证架构

### 9.1 Sa-Token 配置

```java
@Configuration
@ConditionalOnProperty(name = "satoken.interceptor.enable",
                         havingValue = "true", matchIfMissing = true)
public class SaTokenConfig implements WebMvcConfigurer {

    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        var registration = registry.addInterceptor(
            new SaInterceptor(handle -> StpUtil.checkLogin())
        );
        registration.addPathPatterns("/**");

        // 注意：excludePathPatterns 相对于 context-path，不需要加 /api 前缀
        registration.excludePathPatterns(
            "/static/**", "/favicon.ico", "/common/**", "/error", "/test/**",
            "/system/auth/login",
            "/system/auth/logout",
            "/system/auth/info",
            "/system/auth/password",
            "/system/auth/graphic-captcha",
            "/system/auth/captcha",
            "/system/auth/forgot-password/**",
            "/system/config/public",
            "/files/public/**",
            "/doc.html", "/swagger-ui/**", "/swagger-ui.html",
            "/v3/api-docs/**", "/webjars/**"
        );
    }
}
```

**配置说明**：
- `matchIfMissing = true`：默认启用认证拦截
- 测试环境通过 `satoken.interceptor.enable: false` 禁用

### 9.2 测试环境配置

```yaml
# application-test.yml
satoken:
  interceptor:
    enable: false  # 禁用 Sa-Token 拦截器，避免测试时需要登录认证
```

## 十、权限校验机制

### 10.1 权限校验流程

```
Sa-Token 拦截器（登录校验）
  StpUtil.checkLogin()
    ├─ Token 有效 → 继续
    └─ Token 无效 → NotLoginException → GlobalExceptionHandler → { code: 401 }

权限注解校验（当前为预留）
  @RequirePermission("system:user:add")
    ├─ PermissionAspect 拦截
    └─ TODO: 目前 PermissionAspect 为预留，待完善
```

### 10.2 前后端权限联动

登录成功后调用 `GET /system/auth/info` 获取完整用户信息（含菜单树和权限列表），前端据此渲染路由守卫和按钮级权限。

## 十一、数据范围权限体系

### 11.1 概述

数据范围权限（Data Scope Permission）是医工宝系统中用于控制用户可见业务数据范围的机制，与接口权限（Resource/Permission）共同构成完整的 RBAC 权限体系。

| 权限类型 | 作用层级 | 控制内容 | 实现位置 |
|---------|---------|---------|---------|
| **接口权限** | 功能控制 | 用户能否访问某个功能/菜单/按钮 | `sys_resource` + `sys_role_resource` |
| **数据范围权限** | 数据可见性 | 用户能看哪些具体数据（机构/部门/医院等） | `sys_user_hospital` + 业务查询条件 |

### 11.2 数据范围类型定义

数据范围由 `DataScopeConstants` 常量类定义，共 5 种类型：

| 值 | 常量 | 说明 |
|----|------|------|
| 1 | ALL | 全部数据（系统管理员） |
| 2 | ORG | 本机构数据 |
| 3 | SELF | 仅自己的数据 |
| 4 | HOSPITALS | 医院范围数据（通过 sys_user_hospital 限制） |
| 5 | DEPT | 本部门数据 |

详细说明见 `05_用户管理功能实现方案` 六章。

## 十二、系统配置关联

鉴权模块依赖以下系统配置：

| 配置键 | 配置名称 | 默认值 | 说明 |
|-------|---------|-------|------|
| `default.password` | 默认密码 | 123456 | 新用户初始密码 |
| `login.max.failures` | 最大连续登录失败次数 | 5 | 达到此次数后锁定账户 |
| `login.lock.duration` | 登录锁定时长 | 15 | 自动解锁时间（分钟） |
| `captcha.expire.seconds` | 验证码有效期 | 300 | 短信/邮箱验证码有效时间（秒） |
| `captcha.cooldown.seconds` | 发送冷却时间 | 60 | 两次发送的最小间隔（秒） |
| `captcha.daily.limit` | 每日发送上限 | 10 | 同一目标每日最多发送次数 |
| `mail.from` | 发件人邮箱 | - | 邮件发送时的 From 地址 |

## 十三、错误码定义

| 错误码 | 枚举值 | 说明 |
|-------|--------|------|
| 401 | UNAUTHORIZED | 未登录或登录已过期，请重新登录 |
| 403 | FORBIDDEN | 没有权限访问该资源 |
| 600 | USER_NOT_FOUND | 用户不存在 |
| 602 | USER_DISABLED | 用户已被禁用 |
| 605 | PASSWORD_ERROR | 密码错误 |
| 606 | OLD_PASSWORD_ERROR | 旧密码错误 |
| 607 | NEW_PASSWORD_SAME_AS_OLD | 新密码不能与旧密码相同 |
| 608 | USERNAME_OR_PASSWORD_ERROR | 用户名或密码错误 |
| 609 | ACCOUNT_LOCKED | 账户已被锁定，请%d分钟后重试 |
| 612 | PERMISSION_DENIED | 没有权限执行该操作 |
| 637 | CAPTCHA_TOO_FREQUENT | 发送过于频繁，请稍后再试 |
| 638 | CAPTCHA_DAILY_LIMIT | 今日发送次数已达上限 |
| 639 | CAPTCHA_EXPIRED | 验证码已过期或不存在 |
| 640 | CAPTCHA_ERROR | 验证码错误 |
| 641 | CAPTCHA_TYPE_INVALID | 不支持的验证码类型 |
| 773 | CAPTCHA_GRAPHIC_EXPIRED | 图形验证码已过期，请刷新 |
| 774 | CAPTCHA_GRAPHIC_ERROR | 图形验证码错误 |

## 十四、登录日志记录

### 14.1 日志记录时机

| 时机 | login_status | fail_reason |
|-----|-------------|-------------|
| 用户不存在 | 0 | 用户不存在 |
| 用户已禁用 | 0 | 用户已禁用 |
| 账户已锁定 | 0 | 账户已锁定 |
| 密码错误 | 0 | 密码错误 |
| 图形验证码错误 | — | 抛出异常，不记录日志 |
| 登录成功 | 1 | null |

### 14.2 客户端信息获取

```java
// 获取客户端 IP（支持多级代理）
private String getClientIp() {
    String ip = request.getHeader("X-Forwarded-For");
    if (ip == null || ip.isEmpty() || "unknown".equalsIgnoreCase(ip)) {
        ip = request.getHeader("X-Real-IP");
    }
    if (ip == null || ip.isEmpty() || "unknown".equalsIgnoreCase(ip)) {
        ip = request.getRemoteAddr();
    }
    if (ip != null && ip.contains(",")) {
        ip = ip.split(",")[0].trim();
    }
    return ip;
}
```

## 十五、安全设计

### 15.1 密码安全

- BCryptPasswordEncoder 加密，强度 `$2a$10$`（默认强度 10）
- 密码校验使用 `matches()` 方法，防止时序攻击
- 不存储明文密码

### 15.2 Token 安全

- Token 存储在 HTTP Header `Authorization` 中
- Token 默认有效期 30 天（Sa-Token `timeout: 2592000`）
- 登出时调用 `StpUtil.logout()` 使 Token 失效

### 15.3 账户安全

- 连续登录失败达到阈值后自动锁定
- 锁定后基于时间自动解锁
- 登录成功自动重置失败计数

### 15.4 验证码安全

- 图形验证码一次性使用（Redis key 校验后立即删除）
- 短信/邮箱验证码有冷却保护（60s）和每日限额（10次）
- 校验失败达到 5 次后验证码自动作废
- 忘记密码接口对不存在的目标静默成功（防止用户枚举攻击）

## 十六、版本历史

| 版本 | 日期 | 修改内容 | 作者 |
|-----|------|---------|------|
| 1.0 | 2026-03-23 | 初始版本 | hanjor |
| 1.1 | 2026-03-23 | 新增数据范围权限体系章节 | hanjor |
| 2.0 | 2026-04-22 | 重构：新增 PHONE/EMAIL 登录方式、图形验证码、CaptchaService、忘记密码流程；LoginDTO 重构为 loginType/principal/credential；LoginVO.login() 仅返回 token；新增 SendCaptchaDTO、ForgotPasswordResetDTO、GraphicCaptchaVO；LoginLogEntity 新增 loginType 字段；错误码新增 637-641、773-774；更新模块结构、Sa-Token 放行路径、系统配置关联 | hanjor |
