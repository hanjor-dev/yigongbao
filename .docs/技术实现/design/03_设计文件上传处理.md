# 设计文件上传处理

> **任务序号**：3
> **状态**：已完成
> **前置依赖**：02_design模块初始化

---

## 一、任务概述

实现设计阶段三类文件的上传、删除功能：
1. **打印文件数据包**：压缩包上传 + 自动解析包内文件列表
2. **可视化模型文件**：直接上传，无数量限制
3. **设计报告**：直接上传，每工单一份

---

## 二、涉及接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/design/workorder/{orderId}/package/upload` | POST | 上传打印文件数据包 |
| `/api/design/workorder/{orderId}/package/{packageId}` | DELETE | 删除数据包 |
| `/api/design/workorder/{orderId}/visual-model/upload` | POST | 上传可视化模型 |
| `/api/design/workorder/{orderId}/visual-model/{fileId}` | DELETE | 删除可视化模型 |
| `/api/design/workorder/{orderId}/report/upload` | POST | 上传设计报告 |
| `/api/design/workorder/{orderId}/report/{fileId}` | DELETE | 删除设计报告 |

---

## 三、三类文件处理逻辑

### 3.1 打印文件数据包

**上传流程**：
1. 接收压缩包文件（支持 ZIP/RAR/7Z）
2. 调用 FileService 存储压缩包
3. 生成数据包编号：`{订单编号}-{序号}`（使用 CodeRuleConstants.DATA_PACKAGE_NO）
4. 解析压缩包内文件列表（不解压到磁盘，仅读取元数据）
5. 按扩展名白名单过滤，只保留允许的文件类型
6. 写入 design_package 和 design_package_file 表

**删除流程**：
1. 校验数据包是否存在且属于当前工单
2. 检查是否有关联的打印产品信息（design_product），若有则禁止删除
3. 删除 design_package_file 记录（逻辑删除）
4. 删除 design_package 记录（逻辑删除）
5. 删除存储的压缩包文件

**扩展名白名单配置**：
- 配置键：`design.package.allowed_extensions`
- 默认值：`.stl,.obj,.ply,.3mf,.gcode,.ctb,.cbddlp`
- 存储于 sys_config 表，运行时可调整

### 3.2 可视化模型文件

**上传流程**：
1. 接收模型文件
2. 调用 FileService 存储
3. 写入 design_model 表

**删除流程**：
1. 校验文件是否存在且属于当前工单
2. 删除 design_model 记录
3. 删除存储的文件

**说明**：无数量限制，一个工单可上传多个可视化模型。

### 3.3 设计报告

**上传流程**：
1. 接收报告文件
2. 调用 FileService.uploadAndLink 存储并关联
3. bizType = `DESIGN_REPORT`，bizId = orderId

**删除流程**：
1. 调用 FileService.deleteById

**说明**：复用现有 file_detail 表，object_type 区分，每工单一份。

---

## 四、压缩包解析

### 4.1 支持格式

| 格式 | 扩展名 | 解析库 |
|------|--------|--------|
| ZIP | .zip | java.util.zip（JDK 内置） |
| RAR | .rar | junrar（需引入依赖） |
| 7Z | .7z | commons-compress（已有） |

### 4.2 解析策略

- **不解压到磁盘**：仅读取压缩包内的文件列表元数据
- **提取信息**：文件名、文件大小、相对路径
- **过滤逻辑**：按扩展名白名单过滤，跳过目录项

### 4.3 工具类设计

```java
/**
 * 压缩包解析工具类
 */
public class ArchiveParserUtil {
    
    /**
     * 解析压缩包，返回文件列表
     * @param inputStream 压缩包输入流
     * @param fileName 文件名（用于判断格式）
     * @param allowedExtensions 允许的扩展名集合
     * @return 文件信息列表
     */
    public static List<ArchiveFileInfo> parse(
        InputStream inputStream, 
        String fileName, 
        Set<String> allowedExtensions
    );
}

/**
 * 压缩包内文件信息
 */
@Data
public class ArchiveFileInfo {
    private String fileName;      // 文件名（不含路径）
    private String filePath;      // 包内完整路径
    private Long fileSize;        // 文件大小（字节）
    private String extension;     // 扩展名
}
```

---

## 五、依赖引入

```xml
<!-- RAR 解析（pom.xml 新增） -->
<dependency>
    <groupId>com.github.junrar</groupId>
    <artifactId>junrar</artifactId>
    <version>7.5.5</version>
</dependency>
```

> 注：ZIP 使用 JDK 内置，7Z 使用 commons-compress（框架已引入）。

---

## 六、权限与校验

| 校验项 | 说明 |
|--------|------|
| 工单状态 | 仅「设计中」状态可操作 |
| 操作人 | 仅当前设计师（order.designer_id）可操作 |
| 文件格式 | 数据包必须是 ZIP/RAR/7Z，其他拒绝 |
| 文件大小 | 单文件不超过系统配置限制（默认 500MB） |

---

## 七、类清单

| 类型 | 类名 | 说明 |
|------|------|------|
| Controller | DesignFileController | 文件上传删除接口 |
| Service | DesignFileService | 文件业务逻辑 |
| ServiceImpl | DesignFileServiceImpl | 实现类 |
| Util | ArchiveParserUtil | 压缩包解析工具 |
| DTO | ArchiveFileInfo | 包内文件信息 |
| VO | DesignPackageVO | 数据包返回结构 |
| VO | DesignPackageFileVO | 包内文件返回结构 |
| VO | DesignModelVO | 可视化模型返回结构 |

---

## 八、系统配置

| 配置键 | 默认值 | 说明 |
|--------|--------|------|
| `design.package.allowed_extensions` | `.stl,.obj,.ply,.3mf,.gcode,.ctb,.cbddlp` | 数据包允许的文件扩展名 |
| `design.package.max_size_mb` | `500` | 数据包最大大小（MB） |
| `design.model.max_size_mb` | `200` | 可视化模型最大大小（MB） |
| `design.report.max_size_mb` | `50` | 设计报告最大大小（MB） |

---

## 九、完成清单

- [x] 引入 junrar 和 commons-compress 依赖
- [x] 创建 ArchiveParserUtil 工具类
- [x] 创建 ArchiveFileInfo DTO
- [x] 创建 DesignFileService 接口
- [x] 实现 DesignFileServiceImpl
- [x] 创建 DesignFileController
- [x] 创建 VO 类（DesignPackageVO、DesignPackageFileVO、DesignModelVO）
- [x] 添加设计模块错误码（ErrorCodeEnum 733-740）
- [x] 创建 DesignConfigConstants 配置常量类
- [x] 添加系统配置项初始化数据
- [x] 单元测试（27 个用例通过）

---

*文档版本：v1.2 | 最后更新：2026-04-15*
