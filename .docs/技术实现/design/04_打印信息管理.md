# 打印信息管理

> **任务序号**：4
> **状态**：已完成
> **前置依赖**：03_设计文件上传处理

---

## 一、任务概述

实现打印信息的选项数据查询、列表查询、整包保存、单条删除功能。

---

## 二、涉及接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/design/workorder/{orderId}/print-info/options` | GET | 获取打印信息选项数据（产品树、材质、颜色） |
| `/design/workorder/{orderId}/package/{packageId}/print-info` | GET | 查询数据包打印信息列表 |
| `/design/workorder/{orderId}/package/{packageId}/print-info` | POST | 保存打印信息（整包替换，空列表=清空） |
| `/design/workorder/{orderId}/package/{packageId}/print-info/{printInfoId}` | DELETE | 删除单条打印信息 |

---

## 三、业务逻辑

### 3.1 选项数据（getOptions）

返回打印信息填写所需的下拉选项：

| 字段 | 数据来源 | 说明 |
|------|----------|------|
| designMode | 订单表（暂返回 null） | 当前设计模式（预留字段） |
| products | `productService.listAllWithSpecs()` | 启用产品及其启用规格树，含 certNo |
| materials | `dictService.listByTypeCode("15")` | 材质选项，标记「15.1 树脂」为默认 |
| colorGroups | `dictService.listTreeByTypeCode("16")` | 颜色按产品大类分组 |

**产品树结构**：
```json
{
  "products": [
    {
      "id": 1,
      "productName": "PEEK骨模型",
      "category": "17.1",
      "categoryName": "模型类",
      "specs": [
        { "id": 10, "specName": "47mm", "certId": 100, "certNo": "GZZJ-2026-0001" },
        { "id": 11, "specName": "52mm", "certId": 100, "certNo": "GZZJ-2026-0001" }
      ]
    }
  ]
}
```

**颜色分组结构**：
```json
{
  "colorGroups": [
    {
      "categoryCode": "17.1",
      "categoryName": "模型类",
      "colors": [
        { "code": "16.1.1", "name": "白色", "isDefault": true },
        { "code": "16.1.2", "name": "肤色", "isDefault": false }
      ]
    },
    {
      "categoryCode": "17.2",
      "categoryName": "导板类",
      "colors": [
        { "code": "16.2.1", "name": "透明", "isDefault": true }
      ]
    }
  ]
}
```

### 3.2 级联关系

```
选择产品 → 过滤规格（只显示该产品下 status=1 的规格）
选择规格 → 自动带出注册证号（spec.certNo）
选择产品 → 按大类过滤颜色（根据 product.category 匹配 colorGroup.categoryCode）
材质 → 全量可选，默认「树脂」（dict_code=15.1）
打印模型 → 从当前数据包内文件列表多选
```

### 3.3 保存逻辑（savePrintInfo）

采用**整包替换**策略：

1. 校验订单状态（必须为「设计中」或「设计审核不通过」）
2. 校验当前用户为订单指定设计师
3. 校验 packageId 属于 orderId
4. 校验每条 item 的 packageFileId 属于该数据包
5. 校验 productId 存在且 status=1
6. 校验 specId 存在、status=1、且 spec.productId == productId
7. 删除该数据包下所有旧记录（逻辑删除）
8. 批量插入新记录
9. 空列表 = 清空该包所有打印信息

**关键**：后端从 `product_spec` 表读取 `certNo` 覆盖前端传入值，确保注册证号准确。

### 3.4 权限校验

所有写操作（save/delete）执行前校验：
- 订单状态必须为 `DESIGN_IN_PROGRESS(2020)` 或 `DESIGN_REVIEW_REJECTED(2060)`
- 当前登录用户必须是订单的 `designerId`
- 校验失败抛出 `DESIGN_OPERATOR_NOT_ALLOWED(740)`

---

## 四、数据结构

### 4.1 请求 DTO

**SavePrintInfoDTO**
```java
@Data
public class SavePrintInfoDTO {
    @NotNull
    private List<@Valid SavePrintInfoItemDTO> items;
}
```

**SavePrintInfoItemDTO**
```java
@Data
public class SavePrintInfoItemDTO {
    @NotNull private Long packageFileId;    // 数据包内文件ID
    @NotNull private Long productId;        // 产品ID
    @NotNull private Long specId;           // 规格ID
    @NotNull @Min(1) private Integer quantity; // 数量
    
    // 冗余字段（前端传入，后端可覆盖）
    private String packageFileName;
    private String productName;
    private String specName;
    private String certNo;                  // 后端从 spec 表读取覆盖
    private String materialId;              // dict_code
    private String materialName;
    private String colorId;                 // dict_code
    private String colorName;
    private Integer packQuantity;
    private String timeliness;
    private String productMark;
    private Integer sortOrder;
}
```

### 4.2 响应 VO

**PrintInfoOptionsVO**
```java
@Data
public class PrintInfoOptionsVO {
    private String designMode;                    // 设计模式（暂为 null）
    private List<PrintInfoProductVO> products;    // 产品树（含规格）
    private List<DictOptionVO> materials;         // 材质选项
    private List<ColorGroupVO> colorGroups;       // 颜色分组
}
```

**PrintInfoProductVO**
```java
@Data
public class PrintInfoProductVO {
    private Long id;
    private String productName;
    private String category;
    private String categoryName;
    private List<PrintInfoSpecVO> specs;
}
```

**PrintInfoSpecVO**
```java
@Data
public class PrintInfoSpecVO {
    private Long id;
    private String specName;
    private Long certId;
    private String certNo;
}
```

**ColorGroupVO**
```java
@Data
public class ColorGroupVO {
    private String categoryCode;           // 产品大类 dict_code（如 17.1）
    private String categoryName;           // 大类名称
    private List<DictOptionVO> colors;     // 颜色选项
}
```

**DictOptionVO**
```java
@Data
public class DictOptionVO {
    private String code;      // dict_code
    private String name;      // dict_name
    private Boolean isDefault;
}
```

**DesignProductVO**（打印信息记录）
```java
@Data
public class DesignProductVO {
    private Long id;
    private Long orderId;
    private Long packageId;
    private Long productId;
    private String productName;
    private Long specId;
    private String specName;
    private String certNo;
    private String materialId;
    private String materialName;
    private String colorId;
    private String colorName;
    private Integer quantity;
    private Integer packQuantity;
    private String timeliness;
    private String productMark;
    private Long packageFileId;
    private String packageFileName;
    private Integer sortOrder;
}
```

---

## 五、实体设计

**DesignProductEntity**（design_product 表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 主键 |
| orderId | Long | 订单ID |
| packageId | Long | 数据包ID |
| productId | Long | 产品ID |
| productName | String | 产品名称 |
| specId | Long | 规格ID |
| specName | String | 规格名称 |
| certNo | String | 注册证号 |
| materialId | String | 材质 dict_code（如 15.1） |
| materialName | String | 材质名称 |
| colorId | String | 颜色 dict_code（如 16.1.1） |
| colorName | String | 颜色名称 |
| quantity | Integer | 数量 |
| packQuantity | Integer | 包装数量 |
| timeliness | String | 时效 |
| productMark | String | 产品标识 |
| packageFileId | Long | 关联文件ID |
| packageFileName | String | 文件名称 |
| sortOrder | Integer | 排序 |

> **注意**：`materialId` 和 `colorId` 为 **String** 类型，存储字典 dict_code（如 "15.1"、"16.1.1"）。

---

## 六、错误码

| 错误码 | 枚举 | 说明 |
|--------|------|------|
| 648 | PRODUCT_NOT_FOUND | 产品不存在 |
| 740 | DESIGN_OPERATOR_NOT_ALLOWED | 非设计师或订单状态不允许操作 |
| 742 | PRODUCT_SPEC_NOT_FOUND | 产品规格不存在 |
| 400 | PARAM_ERROR | spec.productId 与 item.productId 不匹配 |

---

## 七、代码文件清单

| 类型 | 路径 |
|------|------|
| Service接口 | `yigongbao-module-design/.../service/DesignPrintInfoService.java` |
| Service实现 | `yigongbao-module-design/.../service/impl/DesignPrintInfoServiceImpl.java` |
| Controller | `yigongbao-module-design/.../controller/DesignPrintInfoController.java` |
| 请求DTO | `dto/SavePrintInfoDTO.java`, `dto/SavePrintInfoItemDTO.java` |
| 响应VO | `vo/PrintInfoOptionsVO.java`, `vo/PrintInfoProductVO.java`, `vo/PrintInfoSpecVO.java`, `vo/ColorGroupVO.java`, `vo/DictOptionVO.java`, `vo/DesignProductVO.java` |
| 实体 | `entity/DesignProductEntity.java` |
| 单元测试 | `test/.../DesignPrintInfoServiceImplTest.java`（9 条测试用例） |

---

## 八、完成状态

- [x] Controller 层实现
- [x] Service 层实现
- [x] 整包保存逻辑（删除旧记录 + 批量插入）
- [x] 权限校验（订单状态 + 设计师身份）
- [x] 单元测试（9 条）

---

*文档版本：v2.0 | 最后修订：2026-04-15*
