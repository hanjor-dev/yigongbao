# 订单修改审核实现方案

> **文档版本**：**4.0**
> **创建日期**：2026-04-08
> **作者**：hanjor
> **归属模块**：order
> **状态**：待实施

---

## 零、版本记录

| 版本 | 日期 | 修改内容 | 作者 |
|-----|------|---------|------|
| 1.0 | 2026-04-08 | 初始版本：基于4.6需求文档和业务澄清，完整设计实现方案 | hanjor |
| **2.0** | **2026-04-08** | **重大变更：移除申请编号（改用ID）；阶段限制修正为订单+设计阶段（仅item类型）；设计解耦方案（统一调用接口）；新增字典数据；错误码起始码修正为715；采用MySQL8动态索引** | **hanjor** |
| **3.0** | **2026-04-08** | **字段拆分：apply_type 拆为 apply_type_codes(VARCHAR 50)+apply_type_names(VARCHAR 200)；字典编码体系改为 group=14（14.1/14.2/14.3）；ModifyApplyTypeEnum.dictCode 更新为 14.x；sys_config JSON 顶层 key 改为 14.1/14.2/14.3；实体/Service 代码同步更新；移除 DictService 依赖；新增 HospitalMapper 依赖；修正 executeModification 中 applyTypeCodes 解析；新增 ExecuteModificationItemDTO 明确 orderItemId 语义；service 代码使用显式字段赋值代替反射** | **hanjor** |

---

## 一、文档概述

### 1.1 文档目的

本文档描述医工宝订单模块「订单信息修改申请审核」功能的技术实现方案，详细涵盖数据库设计、接口设计、业务流程、代码结构、错误码设计等内容。

### 1.2 功能概述

| 项目 | 说明 |
|------|------|
| 功能名称 | 订单信息修改申请审核 |
| 功能分类 | 订单管理 > 修改申请 |
| 需求来源 | `.docs/需求分析/v1/业务流程1_订单创建.md` 第4.6章节 |
| 核心流程 | 业务员发起申请 → 管理员审核 → 审核通过后允许修改 → 系统留痕 |
| 实现状态 | 待实施 |

### 1.3 业务规则总结

以下业务规则经需求澄清确认，作为设计方案的前提约束：

| 规则项 | 业务决策 |
|------|------|
| 功能开关 | 默认强制开启，无需配置开关 |
| 允许发起的阶段 | **仅订单阶段（phase=1）和设计阶段（phase=2）**可发起修改申请 |
| 阶段与类型限制 | 订单阶段：可申请基础信息、影像文件、重建项目；设计阶段：**仅可申请重建项目** |
| 类型允许字段 | 通过 `sys_config` 动态配置，管理员可调整 |
| 申请类型选择 | 支持多选，如同时申请修改「基础信息」和「重建项目」 |
| 审批粒度 | 必须全部审批，一次性通过/拒绝整个申请，不支持部分审批 |
| 并发控制 | 每个订单同时只能有1个有效申请（有待审核申请时不允许再发起） |
| 修改留痕 | 每次字段变更必须记录（修改字段/修改前值/修改后值/修改人/修改时间），永久留存 |
| 查询权限 | 由权限配置控制，业务层不关心具体权限判断 |
| 申请编号 | 直接使用主键ID，无需单独编码规则 |

### 1.4 相关文档

| 文档 | 说明 |
|------|------|
| `.docs/需求分析/v1/业务流程1_订单创建.md` | 业务需求文档（第4.6章节） |
| `.docs/技术实现/order/01_订单流程实现方案.md` | 模块架构、数据库设计、API设计参考 |
| `.docs/技术实现/order/02_订单模块功能实现方案.md` | 订单模块功能实现细节 |
| `.docs/接口文档/19_订单模块接口文档.md` | 订单模块接口文档 |

---

## 二、数据库设计

### 2.1 数据库表总览

| 序号 | 表名 | 说明 | 优先级 |
|------|------|------|--------|
| 1 | `order_modify_apply` | 订单修改申请表 | P0 |
| 2 | `order_modification_log` | 订单修改留痕表 | P0 |
| 3 | `sys_dict` 字典数据 | 申请类型字典 | P0 |
| 4 | `sys_config` 配置记录 | 字段配置 | P0 |

### 2.2 订单修改申请表（order_modify_apply）

```sql
CREATE TABLE `order_modify_apply` (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `order_id` BIGINT NOT NULL COMMENT '订单ID',
    `order_code` VARCHAR(50) NOT NULL COMMENT '订单编号（冗余，便于查询）',

    -- 申请类型：多选时逗号分隔字典编码，如 "14.1,14.3"
    `apply_type_codes` VARCHAR(50) NOT NULL COMMENT '申请类型字典编码（逗号分隔，如 14.1,14.3）',
    `apply_type_names` VARCHAR(200) COMMENT '申请类型中文名称冗余（如 基础信息、重建项目）',
    `apply_reason` TEXT COMMENT '申请原因',

    -- 审核信息
    `status` VARCHAR(20) NOT NULL DEFAULT 'PENDING' COMMENT '状态：PENDING-待审核，APPROVED-已同意，REJECTED-已拒绝',
    `reject_reason` TEXT COMMENT '驳回原因（审核不通过时必填）',

    -- 操作人
    `applicant_id` BIGINT NOT NULL COMMENT '申请人ID',
    `applicant_name` VARCHAR(100) COMMENT '申请人姓名',
    `auditor_id` BIGINT COMMENT '审核人ID',
    `auditor_name` VARCHAR(100) COMMENT '审核人姓名',
    `audit_time` DATETIME COMMENT '审核时间',

    -- 公共字段
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    `create_by` BIGINT,
    `update_by` BIGINT,
    `is_deleted` TINYINT DEFAULT 0,

    PRIMARY KEY (`id`),
    KEY `idx_order_modify_apply_order_id` (`order_id`),
    KEY `idx_order_modify_apply_applicant_id` (`applicant_id`),
    KEY `idx_order_modify_apply_status` (`status`),
    KEY `idx_order_modify_apply_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单信息修改申请表';

-- 并发控制：同一订单只能有1个 PENDING 申请
-- MySQL 8.0+ 函数索引，is_deleted=1 时返回 NULL，不触发唯一约束
CREATE UNIQUE INDEX uk_order_modify_apply_pending
    ON order_modify_apply ((CASE WHEN is_deleted = 0 AND status = 'PENDING' THEN order_id ELSE NULL END));
```

**设计要点**：

1. **移除 apply_code 字段**：申请编号直接使用主键ID，无需单独编码规则，简化设计
2. **apply_type 字段拆分**：`apply_type_codes VARCHAR(50)` 存储逗号分隔的字典编码（如 `"14.1,14.3"`），`apply_type_names VARCHAR(200)` 存储冗余中文名（如 `"基础信息、重建项目"`）便于查询展示；VARCHAR(50) 最长存储 `"14.1,14.2,14.3"` = 11字符，充裕
3. **并发控制函数索引**：`uk_order_modify_apply_pending` 使用 MySQL 8 表达式索引（CREATE TABLE 后追加），保证同一订单同一时间只有一条 PENDING 状态的记录，从数据库层面彻底解决并发问题
4. `is_deleted` 逻辑删除，撤回申请时标记删除
5. 公共字段（`create_time`、`update_time`、`create_by`、`update_by`、`is_deleted`）由 MyBatis-Plus 自动填充

### 2.3 订单修改留痕表（order_modification_log）

```sql
CREATE TABLE `order_modification_log` (
    `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    `order_id` BIGINT NOT NULL COMMENT '订单ID',
    `order_code` VARCHAR(50) NOT NULL COMMENT '订单编号（冗余）',
    `apply_id` BIGINT COMMENT '关联的修改申请ID',

    -- 修改字段信息
    `field_name` VARCHAR(50) NOT NULL COMMENT '修改字段名称（如 patient_name）',
    `field_label` VARCHAR(100) COMMENT '修改字段中文名（如"患者姓名"）',
    `old_value` TEXT COMMENT '修改前的值',
    `new_value` TEXT COMMENT '修改后的值',

    -- 操作人
    `modifier_id` BIGINT NOT NULL COMMENT '修改人ID',
    `modifier_name` VARCHAR(100) COMMENT '修改人姓名',

    -- 公共字段（仅 create_time）
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,

    PRIMARY KEY (`id`),
    KEY `idx_order_modification_log_order_id` (`order_id`),
    KEY `idx_order_modification_log_apply_id` (`apply_id`),
    KEY `idx_order_modification_log_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单信息修改留痕表';
```

**设计要点**：

1. 每条留痕记录对应一个字段的一次变更（1对1关系）
2. `old_value` 和 `new_value` 使用 TEXT 类型，支持长文本
3. `apply_id` 可为空（未来扩展用）
4. **不继承 BaseEntity**：留痕表为永久不可变审计记录，无软删除、无 `update_time`，仅自动填充 `create_time`
5. 永久留存，不设过期策略

### 2.4 索引命名规范

> **重要**：所有索引使用表名前缀，避免多表索引名冲突；唯一性约束使用 MySQL 8 动态表达式索引解决软删除下的唯一性问题

| 表名 | 索引前缀 | 示例 |
|------|---------|------|
| `order_modify_apply` | `idx_order_modify_apply_` | `idx_order_modify_apply_order_id` |
| `order_modification_log` | `idx_order_modification_log_` | `idx_order_modification_log_order_id` |

---

## 三、代码模块结构

### 3.1 目录结构

```
yigongbao-module-order/
└── src/main/java/com/yigongbao/module/order/
    ├── controller/
    │   └── OrderModifyApplyController.java       # 【新建】修改申请接口
    │
    ├── service/
    │   ├── OrderModifyApplyService.java          # 【新建】接口（统一调用入口）
    │   └── impl/
    │       └── OrderModifyApplyServiceImpl.java   # 【新建】实现
    │
    ├── mapper/
    │   ├── OrderModifyApplyMapper.java             # 【新建】
    │   └── OrderModificationLogMapper.java         # 【新建】
    │
    ├── entity/
    │   ├── OrderModifyApplyEntity.java            # 【新建】
    │   └── OrderModificationLogEntity.java          # 【新建】
    │
    ├── dto/modify/
    │   ├── CreateModifyApplyDTO.java              # 【新建】申请创建
    │   ├── AuditModifyApplyDTO.java               # 【新建】审核操作
    │   ├── ModifyApplyPageQueryDTO.java           # 【新建】分页查询
    │   ├── ModificationLogPageQueryDTO.java        # 【新建】留痕分页查询
    │   │   # ↑ ExecuteModificationDTO 和 ExecuteModificationItemDTO 已在 v5.0 删除
    │   │   # 执行修改接口改为 Map<String, Object> 动态传参，无需 DTO
    │   └── ModifyApplyFieldConfigDTO.java         # 【新建】字段配置（查询用）
    │
    ├── vo/modify/
    │   ├── ModifyApplyVO.java                     # 【新建】申请VO（创建后返回）
    │   ├── ModifyApplyDetailVO.java               # 【新建】申请详情VO
    │   ├── ModifyApplyListVO.java                  # 【新建】申请列表项VO
    │   ├── ModifyApplyFieldConfigVO.java           # 【新建】字段配置VO
    │   └── ModificationLogVO.java                   # 【新建】留痕记录VO
    │
    └── enums/
        ├── ModifyApplyTypeEnum.java                # 【新建】申请类型枚举
        └── ModifyApplyStatusEnum.java                # 【新建】申请状态枚举
```

### 3.2 模块依赖关系

```
OrderModifyApplyController
    └── OrderModifyApplyService (impl: OrderModifyApplyServiceImpl)
            ├── OrderModifyApplyMapper
            ├── OrderModificationLogMapper
            ├── OrderMainMapper              ← 直接读取订单，不注入 Service（规避循环依赖）
            ├── OrderItemMapper              ← 重建项目修改
            ├── OrderFileMapper              ← 影像文件修改
            ├── HospitalMapper               ← 复现 fillAreaFromHospital 逻辑（4行代码，不跨 Service 调用）
            ├── OrderDataValidator           ← 复用项目校验逻辑
            ├── FileService                  ← basic 模块（文件存在性校验）
            └── ConfigService               ← system 模块（读取字段配置）
```

### 3.3 解耦设计：统一调用接口

> **重要**：本功能可能后续被设计模块（phase=2）调用，因此 Service 层接口设计为**统一调用入口**，所有模块均通过 `OrderModifyApplyService` 的接口方法使用本功能，不直接操作 Mapper。

**对外暴露的统一接口方法**（`OrderModifyApplyService`）：

| 方法 | 用途 | 调用方 |
|------|------|--------|
| `canApplyModify(Long orderId)` | 判断订单是否可以发起修改申请（phase 限制） | Controller、前端 |
| `createApply(Long orderId, CreateModifyApplyDTO dto)` | 发起修改申请 | Controller、业务模块 |
| `withdrawApply(Long applyId)` | 撤回申请 | Controller |
| `auditApply(Long applyId, AuditModifyApplyDTO dto)` | 审核申请 | Controller |
| `executeModification(Long applyId, Map<String, Object> modifications)` | **核心方法**：执行订单修改（审核通过后），Map 动态传参 | Controller、设计模块 |
| `listMyApplies / listPendingApplies / getApplyDetail` | 查询接口 | Controller |
| `listModificationLogs / getFieldConfig` | 查询接口 | Controller |
| `validateFieldsInScope(Long applyId, List<String> fieldNames)` | 校验字段是否在申请范围内（设计模块可复用） | 设计模块 |

---

## 四、枚举定义

### 4.1 申请类型枚举（ModifyApplyTypeEnum）

```java
/**
 * 订单修改申请类型枚举
 * 对应 order_modify_apply.apply_type_codes 字段，存储字典编码
 * 字典编码：14.1（基础信息）/ 14.2（影像文件）/ 14.3（重建项目），对应 sys_dict group=14
 *
 * @author hanjor
 * @date 2026-04-08
 */
@Getter
@AllArgsConstructor
public enum ModifyApplyTypeEnum {

    INFO("14.1", "基础信息"),
    IMAGE("14.2", "影像文件"),
    ITEM("14.3", "重建项目");

    /**
     * 字典编码（存储到数据库和前端显示的值，对应 sys_dict.dict_code）
     */
    private final String dictCode;

    /**
     * 类型名称（中文描述）
     */
    private final String name;

    /**
     * 根据字典编码获取枚举
     */
    public static ModifyApplyTypeEnum getByDictCode(String dictCode) {
        if (dictCode == null) {
            return null;
        }
        for (ModifyApplyTypeEnum type : values()) {
            if (type.getDictCode().equals(dictCode)) {
                return type;
            }
        }
        return null;
    }

    /**
     * 将逗号分隔的字典编码转换为中文描述（顿号连接）
     * 例如："14.1,14.3" → "基础信息、重建项目"
     */
    public static String toNamesText(String dictCodes) {
        if (dictCodes == null || dictCodes.isBlank()) {
            return "";
        }
        return Arrays.stream(dictCodes.split(","))
                .map(String::trim)
                .map(ModifyApplyTypeEnum::getByDictCode)
                .filter(Objects::nonNull)
                .map(ModifyApplyTypeEnum::getName)
                .collect(Collectors.joining("、"));
    }
}
```

### 4.2 申请状态枚举（ModifyApplyStatusEnum）

```java
/**
 * 订单修改申请状态枚举
 * 对应 order_modify_apply.status 字段
 *
 * @author hanjor
 * @date 2026-04-08
 */
@Getter
@AllArgsConstructor
public enum ModifyApplyStatusEnum {

    PENDING("PENDING", "待审核"),
    APPROVED("APPROVED", "已同意"),
    REJECTED("REJECTED", "已拒绝");

    /**
     * 状态编码（存储到数据库的值）
     */
    private final String code;

    /**
     * 状态名称（中文描述）
     */
    private final String name;

    /**
     * 根据编码获取枚举
     */
    public static ModifyApplyStatusEnum getByCode(String code) {
        if (code == null) {
            return null;
        }
        for (ModifyApplyStatusEnum status : values()) {
            if (status.getCode().equals(code)) {
                return status;
            }
        }
        return null;
    }
}
```

---

## 五、字典数据设计

### 5.1 申请类型字典

在 `sys_dict` 表中新增「订单修改申请类型」字典数据，使用 **group=14**（当前 init.sql 最大 group 为 13）。

### 5.2 字典与枚举的对应关系

| 字典编码 | 枚举 | 中文名称 | 说明 |
|---------|------|--------|------|
| `14.1` | `ModifyApplyTypeEnum.INFO` | 基础信息 | 修改患者姓名、年龄等基础字段 |
| `14.2` | `ModifyApplyTypeEnum.IMAGE` | 影像文件 | 替换订单关联的影像数据/报告文件 |
| `14.3` | `ModifyApplyTypeEnum.ITEM` | 重建项目 | 修改重建项目明细（部位/项目/说明等） |

### 5.3 sys_dict 种子数据

```sql
-- group=14：订单修改申请类型
-- 父节点
INSERT INTO sys_dict (parent_id, dict_code, dict_name, dict_value, level, sort, status)
VALUES (0, '14', '订单修改申请类型', '', 1, 14, 1);

-- 子节点（parent_id 使用上面父节点的 id，实际插入时替换为实际 id）
INSERT INTO sys_dict (parent_id, dict_code, dict_name, dict_value, level, sort, status)
VALUES
    (@group14_id, '14.1', '基础信息', 'INFO',  2, 1, 1),
    (@group14_id, '14.2', '影像文件', 'IMAGE', 2, 2, 1),
    (@group14_id, '14.3', '重建项目', 'ITEM',  2, 3, 1);
```

> **说明**：实际插入 `init.sql` 时需明确写出 parent_id 的实际值（当前最后一个 group 父节点之后的下一个 id）。

---

## 六、系统配置设计

### 6.1 配置方案

每种申请类型允许修改的字段，通过 `sys_config` 表动态配置。管理员可在后台调整配置，无需代码修改。

### 6.2 配置键定义

**配置键**：`order.modify.field.config`

**存储位置**：`sys_config` 表

**配置值格式**：JSON

### 6.3 配置值结构

```json
{
  "14.1": {
    "name": "基础信息",
    "fields": [
      { "field": "patientName", "label": "患者姓名", "type": "text", "required": false },
      { "field": "patientAge", "label": "患者年龄", "type": "number", "required": false },
      { "field": "patientGender", "label": "患者性别", "type": "select", "required": false, "options": [{ "value": "12.1", "label": "男" }, { "value": "12.2", "label": "女" }] },
      { "field": "doctorId", "label": "关联医生", "type": "autocomplete", "required": false },
      { "field": "doctorPhone", "label": "医生电话", "type": "text", "required": false },
      { "field": "isPostal", "label": "是否邮寄", "type": "switch", "required": false },
      { "field": "postalAddress", "label": "邮寄地址", "type": "textarea", "required": false },
      { "field": "expectedDeliveryDate", "label": "期望交付时间", "type": "datetime", "required": false }
    ]
  },
  "14.2": {
    "name": "影像文件",
    "fields": [
      { "field": "imageDataFileIds", "label": "影像数据文件", "type": "file", "required": false },
      { "field": "imageReportFileIds", "label": "影像报告文件", "type": "file", "required": false }
    ]
  },
  "14.3": {
    "name": "重建项目",
    "fields": [
      { "field": "items", "label": "重建项目明细", "type": "array", "required": false, "subFields": [
        { "field": "bodyPartId", "label": "部位", "type": "select" },
        { "field": "projectId", "label": "重建项目", "type": "select" },
        { "field": "projectDesc", "label": "项目说明", "type": "textarea" },
        { "field": "formingRequirement", "label": "成形需求", "type": "textarea" },
        { "field": "otherRequirement", "label": "其他要求", "type": "textarea" }
      ]}
    ]
  }
}
```

### 6.4 字段说明

| 属性 | 类型 | 说明 |
|------|------|------|
| `field` | String | 字段名（与 `executeModification` Map key 对应；items 子字段由 subFields 定义） |
| `label` | String | 字段中文名（前端展示用） |
| `type` | String | 前端组件类型：text、number、select、switch、textarea、datetime、file、array、autocomplete |
| `required` | Boolean | 是否必填（目前暂不使用，预留） |
| `options` | Array | select 类型时的选项列表 |
| `subFields` | Array | array 类型时的子字段定义 |

### 6.5 种子数据

```sql
-- 字段配置（order.modify.field.config）
-- 顶层 key 使用字典编码 14.1/14.2/14.3，与 ModifyApplyTypeEnum.dictCode 一致
INSERT INTO sys_config (config_key, config_name, config_value, config_type, config_group, is_system, is_public, sort, status)
VALUES (
    'order.modify.field.config',
    '订单修改申请字段配置',
    '{"14.1":{"name":"基础信息","fields":[{"field":"patientName","label":"患者姓名","type":"text","required":false},{"field":"patientAge","label":"患者年龄","type":"number","required":false},{"field":"patientGender","label":"患者性别","type":"select","required":false,"options":[{"value":"12.1","label":"男"},{"value":"12.2","label":"女"}]},{"field":"doctorId","label":"关联医生","type":"autocomplete","required":false},{"field":"doctorPhone","label":"医生电话","type":"text","required":false},{"field":"isPostal","label":"是否邮寄","type":"switch","required":false},{"field":"postalAddress","label":"邮寄地址","type":"textarea","required":false},{"field":"expectedDeliveryDate","label":"期望交付时间","type":"datetime","required":false}]},"14.2":{"name":"影像文件","fields":[{"field":"imageDataFileIds","label":"影像数据文件","type":"file","required":false},{"field":"imageReportFileIds","label":"影像报告文件","type":"file","required":false}]},"14.3":{"name":"重建项目","fields":[{"field":"items","label":"重建项目明细","type":"array","required":false,"subFields":[{"field":"bodyPartId","label":"部位","type":"select"},{"field":"projectId","label":"重建项目","type":"select"},{"field":"projectDesc","label":"项目说明","type":"textarea"},{"field":"formingRequirement","label":"成形需求","type":"textarea"},{"field":"otherRequirement","label":"其他要求","type":"textarea"}]}]}}',
    'json',
    'order',
    1,
    0,
    10,
    1
);
```

---

## 七、错误码设计

### 7.1 已有错误码

以下错误码已在 `ErrorCodeEnum` 中定义，可直接使用：

| 错误码 | 枚举名 | 消息 | 说明 |
|--------|--------|------|------|
| 703 | `ORDER_MODIFY_APPLY_NOT_FOUND` | 修改申请不存在 | 申请不存在 |
| 704 | `ORDER_MODIFY_APPLY_STATUS_ERROR` | 修改申请状态不合法 | 状态不匹配 |
| 705 | `ORDER_MODIFY_APPLY_ALREADY_PROCESSED` | 该修改申请已处理 | 已审核过 |

### 7.2 新增错误码（715 起）

| 错误码 | 枚举名 | 消息 | 说明 |
|--------|--------|------|------|
| 715 | `ORDER_MODIFY_APPLY_EXISTS` | 已有待审核的修改申请，请等待处理后再发起新申请 | 并发控制 |
| 716 | `ORDER_MODIFY_FIELD_NOT_ALLOWED` | 该字段不在申请范围内，请检查申请类型 | 字段校验 |
| 717 | `ORDER_NOT_APPLICABLE_STATUS` | 当前订单状态不适用修改申请功能 | 阶段校验 |
| 718 | `ORDER_MODIFY_APPLY_NOT_MINE` | 只能撤回自己的申请 | 撤回权限 |
| 719 | `ORDER_MODIFY_REJECT_REASON_REQUIRED` | 审核拒绝时必须填写驳回原因 | 审核参数 |
| 720 | `ORDER_MODIFY_AUDIT_PERMISSION_DENIED` | 只有管理员可以审核申请 | 审核权限 |
| 721 | `ORDER_MODIFY_FIELD_CONFIG_NOT_FOUND` | 字段配置不存在，请联系管理员 | 配置缺失 |
| 722 | `ORDER_MODIFY_TYPE_NOT_ALLOWED_IN_PHASE` | 当前阶段不允许申请该类型的修改 | 阶段+类型联合校验 |

### 7.3 ErrorCodeEnum 扩展

```java
// 修改申请相关（续 703-705）
ORDER_MODIFY_APPLY_EXISTS(715, "已有待审核的修改申请，请等待处理后再发起新申请"),
ORDER_MODIFY_FIELD_NOT_ALLOWED(716, "该字段不在申请范围内，请检查申请类型"),
ORDER_NOT_APPLICABLE_STATUS(717, "当前订单状态不适用修改申请功能"),
ORDER_MODIFY_APPLY_NOT_MINE(718, "只能撤回自己的申请"),
ORDER_MODIFY_REJECT_REASON_REQUIRED(719, "审核拒绝时必须填写驳回原因"),
ORDER_MODIFY_AUDIT_PERMISSION_DENIED(720, "只有管理员可以审核申请"),
ORDER_MODIFY_FIELD_CONFIG_NOT_FOUND(721, "字段配置不存在，请联系管理员"),
ORDER_MODIFY_TYPE_NOT_ALLOWED_IN_PHASE(722, "当前阶段不允许申请该类型的修改"),
```

---

## 八、DTO 设计

### 8.1 CreateModifyApplyDTO（创建申请）

```java
/**
 * 创建订单修改申请 DTO
 *
 * @author hanjor
 * @date 2026-04-08
 */
@Data
public class CreateModifyApplyDTO implements Serializable {

    private static final long serialVersionUID = 1L;

    /**
     * 申请修改的类型（多选，逗号分隔字典编码）
     * 取值：14.1（基础信息）/ 14.2（影像文件）/ 14.3（重建项目）
     * 示例："14.1,14.3"
     */
    @NotBlank(message = "申请类型不能为空")
    private String applyTypes;

    /**
     * 申请原因
     */
    @NotBlank(message = "申请原因不能为空")
    private String applyReason;
}
```

### 8.2 AuditModifyApplyDTO（审核申请）

```java
/**
 * 审核订单修改申请 DTO
 *
 * @author hanjor
 * @date 2026-04-08
 */
@Data
public class AuditModifyApplyDTO implements Serializable {

    private static final long serialVersionUID = 1L;

    /**
     * 审核操作
     * APPROVE - 同意
     * REJECT - 拒绝
     */
    @NotBlank(message = "审核操作不能为空")
    private String action;

    /**
     * 驳回原因（审核拒绝时必填）
     */
    private String rejectReason;
}
```

### 8.3 ModifyApplyPageQueryDTO（分页查询）

```java
/**
 * 修改申请分页查询 DTO
 *
 * @author hanjor
 * @date 2026-04-08
 */
@Data
public class ModifyApplyPageQueryDTO implements Serializable {

    private static final long serialVersionUID = 1L;

    /**
     * 页码（默认1）
     */
    private Integer pageNum = 1;

    /**
     * 每页条数（默认10）
     */
    private Integer pageSize = 10;

    /**
     * 申请状态筛选（可选）
     * PENDING - 待审核
     * APPROVED - 已同意
     * REJECTED - 已拒绝
     */
    private String status;

    /**
     * 订单编号（模糊查询，可选）
     */
    private String orderCode;

    /**
     * 申请人姓名（模糊查询，可选）
     */
    private String applicantName;

    /**
     * 申请开始日期（可选）
     */
    private String startDate;

    /**
     * 申请结束日期（可选）
     */
    private String endDate;
}
```

### 8.4 执行修改传参（v5.0 改为 Map 动态传参）

> **v5.0 变更**：`ExecuteModificationDTO` 和 `ExecuteModificationItemDTO` 已删除，`executeModification()` 接口改为 `Map<String, Object>` 动态传参。
>
> 各字段语义与原 DTO 字段一致，只传要修改的字段，后端按申请类型分发处理。详见第 10.10 节和接口文档 19.21 节。

**Map key 说明（按申请类型分组）**：

| 申请类型 | key | 类型说明 |
|----------|-----|---------|
| 14.1 基础信息 | `patientName`, `patientAge`, `patientGender` | 患者信息 |
| 14.1 基础信息 | `doctorId`, `doctorPhone` | 医生信息 |
| 14.1 基础信息 | `isPostal`, `postalAddress`, `expectedDeliveryDate` | 交付信息 |
| 14.2 影像文件 | `imageDataFileIds`（List\<String\>）, `imageReportFileIds`（List\<String\>） | 文件替换 |
| 14.3 重建项目 | `items`（List\<Map\<String,Object\>\>，子字段：`orderItemId`/`bodyPartId`/`projectId`/`projectDesc`/`formingRequirement`/`otherRequirement`） | 项目替换 |



### 8.5 ModificationLogPageQueryDTO（留痕分页查询）

```java
/**
 * 修改留痕分页查询 DTO
 *
 * @author hanjor
 * @date 2026-04-08
 */
@Data
public class ModificationLogPageQueryDTO implements Serializable {

    private static final long serialVersionUID = 1L;

    /**
     * 页码（默认1）
     */
    private Integer pageNum = 1;

    /**
     * 每页条数（默认10）
     */
    private Integer pageSize = 10;

    /**
     * 修改开始日期（可选）
     */
    private String startDate;

    /**
     * 修改结束日期（可选）
     */
    private String endDate;

    /**
     * 筛选字段名（精确匹配，可选）
     */
    private String fieldName;
}
```

---

## 九、VO 设计

### 9.1 ModifyApplyVO（创建申请后返回）

```java
/**
 * 订单修改申请 VO（创建后返回）
 *
 * @author hanjor
 * @date 2026-04-08
 */
@Data
public class ModifyApplyVO implements Serializable {

    private static final long serialVersionUID = 1L;

    private Long id;
    private Long orderId;
    private String orderCode;
    private String applyTypeCodes;     // 字典编码，如 "14.1,14.3"
    private String applyTypeNames;     // 中文冗余，如 "基础信息、重建项目"
    private String applyReason;
    private String status;
    private String statusText;
    private Long applicantId;
    private String applicantName;
    private LocalDateTime createTime;
}
```

### 9.2 ModifyApplyListVO（申请列表项）

```java
/**
 * 订单修改申请列表项 VO
 *
 * @author hanjor
 * @date 2026-04-08
 */
@Data
public class ModifyApplyListVO implements Serializable {

    private static final long serialVersionUID = 1L;

    private Long id;
    private Long orderId;
    private String orderCode;
    private String hospitalName;
    private String patientName;
    private String applyTypeCodes;     // 字典编码
    private String applyTypeNames;     // 中文冗余
    private String applyReason;
    private String status;
    private String statusText;
    private String rejectReason;
    private Long applicantId;
    private String applicantName;
    private Long auditorId;
    private String auditorName;
    private LocalDateTime auditTime;
    private LocalDateTime createTime;
}
```

### 9.3 ModifyApplyDetailVO（申请详情）

```java
/**
 * 订单修改申请详情 VO
 *
 * @author hanjor
 * @date 2026-04-08
 */
@Data
public class ModifyApplyDetailVO implements Serializable {

    private static final long serialVersionUID = 1L;

    private Long id;
    private Long orderId;
    private String orderCode;
    private String hospitalName;
    private String deptName;
    private String patientName;
    private String doctorName;
    private Integer currentPhase;
    private String currentPhaseText;
    private Integer currentStatus;
    private String currentStatusText;
    private String applyTypeCodes;     // 字典编码
    private String applyTypeNames;     // 中文冗余
    private String applyReason;
    private String status;
    private String statusText;
    private String rejectReason;
    private Long applicantId;
    private String applicantName;
    private Long auditorId;
    private String auditorName;
    private LocalDateTime auditTime;
    private LocalDateTime createTime;
    private LocalDateTime updateTime;
}
```

### 9.4 ModificationLogVO（修改留痕记录）

```java
/**
 * 订单修改留痕记录 VO
 *
 * @author hanjor
 * @date 2026-04-08
 */
@Data
public class ModificationLogVO implements Serializable {

    private static final long serialVersionUID = 1L;

    private Long id;
    private Long orderId;
    private String orderCode;
    private Long applyId;
    private String fieldName;
    private String fieldLabel;
    private String oldValue;
    private String newValue;
    private Long modifierId;
    private String modifierName;
    private LocalDateTime createTime;
}
```

---

## 十、接口设计

### 10.1 接口总览

| 分类 | 接口 | 方法 | 路径 | 发起方 | 说明 |
|------|------|------|------|--------|------|
| 阶段判断 | 判断是否可申请修改 | GET | `/api/order/{id}/can-apply-modify` | 前端 | 前端用于控制申请入口是否显示 |
| 配置查询 | 查询字段配置 | GET | `/api/order/modify-apply/field-config` | 前端 | 获取各类型可修改字段 |
| 申请发起 | 发起修改申请 | POST | `/api/order/{id}/modify-apply` | 业务员 | 创建修改申请 |
| 申请撤回 | 撤回申请 | DELETE | `/api/order/modify-apply/{id}` | 申请人 | 仅撤回自己的待审核申请 |
| 申请查询 | 查询我发起的申请 | GET | `/api/order/modify-apply/my` | 业务员 | 分页列表，带状态/时间筛选 |
| 申请查询 | 查询待审核申请 | GET | `/api/order/modify-apply/pending` | 管理员 | 分页列表 |
| 申请查询 | 查询申请详情 | GET | `/api/order/modify-apply/{id}` | 双方 | 包含订单基本信息 |
| 申请查询 | 查询订单的申请记录 | GET | `/api/order/{id}/modify-applies` | 双方 | 查询某订单的所有申请 |
| 审核操作 | 审核申请 | PUT | `/api/order/modify-apply/{id}/audit` | 管理员 | 同意/拒绝 |
| 修改执行 | 执行订单修改 | PUT | `/api/order/{id}/modification` | 业务员 | 审核通过后方可调用 |
| 留痕查询 | 查询修改留痕 | GET | `/api/order/{id}/modification-logs` | 双方 | 查询订单的修改历史记录 |

### 10.2 发起修改申请

#### POST /api/order/{id}/modify-apply

**路径参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | Long | ✅ | 订单ID |

**请求体**：

```json
{
  "applyTypes": "14.1,14.3",
  "applyReason": "患者信息变更，需要更新"
}
```

**前置校验（核心逻辑）**：

| 校验项 | 错误码 | 说明 |
|--------|--------|------|
| 订单存在 | ORDER_NOT_FOUND(675) | 订单不存在 |
| 订单阶段适用修改申请 | ORDER_NOT_APPLICABLE_STATUS(717) | 仅 phase=1 或 phase=2 的订单可发起申请 |
| 设计阶段类型限制 | ORDER_MODIFY_TYPE_NOT_ALLOWED_IN_PHASE(722) | phase=2 时只能申请 item 类型 |
| 同一订单无待审核申请 | ORDER_MODIFY_APPLY_EXISTS(715) | 已有待审核申请 |
| 申请类型在字典中有效 | PARAM_ERROR(400) | 存在无效的申请类型 |
| 申请原因不为空 | PARAM_ERROR(400) | 必须填写申请原因 |

**阶段与类型联合校验逻辑**：

```java
/**
 * 校验申请类型是否在当前阶段允许
 */
private void validateTypeInPhase(OrderMainEntity order, String applyTypes) {
    Integer phase = order.getPhase();

    if (FlowPhaseEnum.ORDER.getValue().equals(phase)) {
        // 订单阶段：允许所有类型（14.1 / 14.2 / 14.3）
        return;
    }

    if (FlowPhaseEnum.DESIGN.getValue().equals(phase)) {
        // 设计阶段：仅允许 14.3（重建项目）
        for (String type : applyTypes.split(",")) {
            ModifyApplyTypeEnum typeEnum = ModifyApplyTypeEnum.getByDictCode(type.trim());
            if (typeEnum != null && typeEnum != ModifyApplyTypeEnum.ITEM) {
                log.warn("设计阶段不允许申请类型：{}", type);
                throw new BusinessException(ErrorCodeEnum.ORDER_MODIFY_TYPE_NOT_ALLOWED_IN_PHASE);
            }
        }
        return;
    }

    // 其他阶段不允许发起申请
    throw new BusinessException(ErrorCodeEnum.ORDER_NOT_APPLICABLE_STATUS);
}
```

**响应**：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 1,
    "orderId": 100,
    "orderCode": "ORD-20260408-000001",
    "applyTypeCodes": "14.1,14.3",
    "applyTypeNames": "基础信息、重建项目",
    "applyReason": "患者信息变更，需要更新",
    "status": "PENDING",
    "statusText": "待审核",
    "applicantId": 5,
    "applicantName": "张三",
    "createTime": "2026-04-08T10:30:00"
  }
}
```

### 10.3 判断是否可申请修改

#### GET /api/order/{id}/can-apply-modify

**说明**：前端用于控制「申请修改」按钮的显示/隐藏。

**响应**：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "canApply": true,
    "allowedTypes": ["14.1", "14.2", "14.3"],
    "allowedTypesText": "基础信息、影像文件、重建项目",
    "reason": null
  }
}
```

**canApply=false 时的 reason 示例**：

| reason | 说明 |
|--------|------|
| `PHASE_NOT_ALLOWED` | 当前阶段不允许申请修改 |
| `TYPE_NOT_ALLOWED` | 当前阶段不允许申请该类型 |
| `PENDING_EXISTS` | 已有待审核的修改申请 |
| `NO_PERMISSION` | 无权限 |

### 10.4 撤回申请

#### DELETE /api/order/modify-apply/{id}

**前置校验**：

| 校验项 | 错误码 | 说明 |
|--------|--------|------|
| 申请存在 | ORDER_MODIFY_APPLY_NOT_FOUND(703) | 修改申请不存在 |
| 申请状态为 PENDING | ORDER_MODIFY_APPLY_STATUS_ERROR(704) | 只有待审核的申请可以撤回 |
| 申请人是当前用户 | ORDER_MODIFY_APPLY_NOT_MINE(718) | 只能撤回自己的申请 |

**实现**：逻辑删除（`is_deleted=1`）

**响应**：`{ "code": 200, "message": "操作成功" }`

### 10.5 查询我发起的申请

#### GET /api/order/modify-apply/my

**Query 参数**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| pageNum | Integer | 否 | 1 | 页码 |
| pageSize | Integer | 否 | 10 | 每页条数 |
| status | String | 否 | - | 筛选状态 |
| orderCode | String | 否 | - | 订单编号（模糊） |
| startDate | String | 否 | - | 申请开始日期 |
| endDate | String | 否 | - | 申请结束日期 |

**数据权限**：仅返回当前用户发起的申请（`applicant_id = currentUserId`）

### 10.6 查询待审核申请（管理员）

#### GET /api/order/modify-apply/pending

**权限**：仅 `公司管理员` 或 `业务管理员`。

**Query 参数**：同 10.5

### 10.7 查询申请详情

#### GET /api/order/modify-apply/{id}

**权限**：申请人可查看自己的申请；管理员可查看所有申请。

### 10.8 查询订单的申请记录

#### GET /api/order/{id}/modify-applies

**路径参数**：订单ID

**Query 参数**：分页 + status 筛选

### 10.9 审核申请

#### PUT /api/order/modify-apply/{id}/audit

**权限**：仅 `公司管理员` 或 `业务管理员`。

**请求体**：

```json
{
  "action": "APPROVE",
  "rejectReason": ""
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| action | String | ✅ | APPROVE（同意）/ REJECT（拒绝） |
| rejectReason | String | 拒绝时必填 | 驳回原因 |

**审核权限判断**：由 `@RequirePermission` 注解或 Service 层调用权限服务校验。业务层不硬编码角色判断。

**响应**：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 1,
    "status": "APPROVED",
    "statusText": "已同意",
    "auditorId": 1,
    "auditorName": "管理员",
    "auditTime": "2026-04-08T11:00:00"
  }
}
```

### 10.10 执行订单修改

#### PUT /order/modify/execute/{applyId}

> **重要**：此接口专门用于审核通过后的修改，与原有的草稿阶段直接更新接口完全独立。
> 设计模块也通过此接口执行修改申请通过后的订单变更。

**路径参数**：`applyId`（Long，已审核通过的申请ID，无需再传 orderId，服务端从申请记录中取得）

**请求体**：`Map<String, Object>` 动态字段，只传本次要修改的字段。

```json
// 场景：混合申请（14.1 基础信息 + 14.3 重建项目）
{
  "patientName": "李四（更新）",
  "patientAge": 45,
  "items": [
    {
      "orderItemId": null,
      "bodyPartId": 1,
      "projectId": 10,
      "projectDesc": "左腿膝关节",
      "formingRequirement": "高精度打印"
    }
  ]
}
```

**前置校验**：

| 校验项 | 错误码 | 说明 |
|--------|--------|------|
| 申请存在 | 701 | 申请不存在 |
| 申请状态为 APPROVED | 717 | 申请未审核通过，不可执行修改 |
| 申请尚未执行 | 718 | 申请已执行（COMPLETED），禁止重复执行 |
| 订单存在 | 675 | 订单不存在 |

**执行流程**：

```
1. 根据 applyId 查申请，校验状态为 APPROVED（幂等保护：COMPLETED 直接报 718）
2. 从申请的 apply_type_codes 解析 allowedTypes Set（含 14.1/14.2/14.3）
3. IF allowedTypes 含 14.1（基础信息）：
   - 快照所有 INFO 字段的旧值
   - applyInfoFields()：将 modifications 中的字段值写入 order 实体
     （hospitalId/hospitalDeptId/doctorId 走 validateAndFillForModify 以同步冗余字段；其余字段直接赋值）
   - logInfoFieldChanges()：逐字段对比新旧值，有变化才插入 order_modification_log
   - 标记 infoModified=true
4. IF allowedTypes 含 14.3（重建项目）且 modifications 包含 "items" key：
   - 取旧的 order_item 列表（按 orderId 查，含 id 映射）
   - 逻辑删除旧 order_item（全量）
   - 用 orderDataValidator.validateAndFillItems() 校验并填充新明细
   - 批量插入新 order_item
   - 逐项记录留痕（新增/修改/删除各有记录）
5. IF allowedTypes 含 14.2（影像文件）：
   - 逻辑删除旧的 imageData / imageReport 类型 order_file 记录
   - 插入新的 order_file 记录
   - 各记录 1 条留痕（JSON 数组格式）
6. IF infoModified：orderMainMapper.updateById(order)（仅 14.1 类型才写回主表，避免无效 update_time 变更）
7. 将申请状态置为 COMPLETED（防止重复执行）
8. 事务提交
```

**关键设计决策：隐式字段白名单**

服务端采用"隐式白名单"方式：每个 `process*Modification` 方法只处理它认识的字段，未知字段静默忽略（不报错、不修改）。这样的设计：
- 足够安全：多余字段无法产生副作用
- 低复杂度：不需要运行时强制校验，`items` 子字段也不需要加入 sys_config 配置
- 扩展方便：`validateFieldsInScope()` 方法已预留，若未来需要强制校验可随时启用

前端仍应按申请类型只传该类型允许的字段（见 `order.modify.field.config` sys_config 配置）。

### 10.11 查询修改留痕

#### GET /api/order/{id}/modification-logs

**Query 参数**：

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| pageNum | Integer | 否 | 1 | 页码 |
| pageSize | Integer | 否 | 10 | 每页条数 |
| startDate | String | 否 | - | 修改开始日期 |
| endDate | String | 否 | - | 修改结束日期 |
| fieldName | String | 否 | - | 筛选字段名（精确匹配） |

**权限**：由权限配置控制，业务层不关心。

---

## 十一、核心业务逻辑

### 11.1 OrderModifyApplyServiceImpl 核心方法

```java
/**
 * 订单修改申请 Service 实现类
 *
 * @author hanjor
 * @date 2026-04-08
 */
@Service
@RequiredArgsConstructor
@Slf4j
public class OrderModifyApplyServiceImpl implements OrderModifyApplyService {

    private final OrderModifyApplyMapper orderModifyApplyMapper;
    private final OrderModificationLogMapper orderModificationLogMapper;
    private final OrderMainMapper orderMainMapper;
    private final OrderItemMapper orderItemMapper;
    private final OrderFileMapper orderFileMapper;
    private final HospitalMapper hospitalMapper;        // 复现 fillAreaFromHospital 逻辑
    private final OrderDataValidator orderDataValidator;
    private final FileService fileService;              // 文件存在性校验
    private final ConfigService configService;

    // ==================== 阶段判断 ====================

    /**
     * 判断订单是否可以发起修改申请
     *
     * @param orderId 订单ID
     * @return 判断结果（包含 canApply、allowedTypes、reason）
     */
    @Override
    public CanApplyModifyResult canApplyModify(Long orderId) {
        OrderMainEntity order = orderMainMapper.selectById(orderId);
        if (order == null) {
            return CanApplyModifyResult.no("ORDER_NOT_FOUND");
        }

        Integer phase = order.getPhase();
        if (!FlowPhaseEnum.ORDER.getValue().equals(phase)
                && !FlowPhaseEnum.DESIGN.getValue().equals(phase)) {
            return CanApplyModifyResult.no("PHASE_NOT_ALLOWED");
        }

        // 校验是否有待审核申请
        Long pendingCount = orderModifyApplyMapper.selectCount(
            new LambdaQueryWrapper<OrderModifyApplyEntity>()
                .eq(OrderModifyApplyEntity::getOrderId, orderId)
                .eq(OrderModifyApplyEntity::getStatus, ModifyApplyStatusEnum.PENDING.getCode())
                .eq(OrderModifyApplyEntity::getIsDeleted, 0)
        );
        if (pendingCount > 0) {
            return CanApplyModifyResult.no("PENDING_EXISTS");
        }

        // 获取允许的申请类型
        List<String> allowedTypes;
        if (FlowPhaseEnum.ORDER.getValue().equals(phase)) {
            allowedTypes = List.of(
                ModifyApplyTypeEnum.INFO.getDictCode(),
                ModifyApplyTypeEnum.IMAGE.getDictCode(),
                ModifyApplyTypeEnum.ITEM.getDictCode()
            );
        } else {
            // 设计阶段：仅允许 item（14.3）
            allowedTypes = List.of(ModifyApplyTypeEnum.ITEM.getDictCode());
        }

        return CanApplyModifyResult.yes(allowedTypes);
    }

    // ==================== 申请发起 ====================

    /**
     * 发起修改申请
     */
    @Override
    @Transactional(rollbackFor = Exception.class)
    public ModifyApplyVO createApply(Long orderId, CreateModifyApplyDTO dto) {
        log.info("发起修改申请，orderId={}, applyTypes={}", orderId, dto.getApplyTypes());
        try {
            // 1. 校验订单
            OrderMainEntity order = orderMainMapper.selectById(orderId);
            if (order == null) {
                throw new BusinessException(ErrorCodeEnum.ORDER_NOT_FOUND);
            }

            // 2. 校验订单阶段适用修改申请
            Integer phase = order.getPhase();
            if (!FlowPhaseEnum.ORDER.getValue().equals(phase)
                    && !FlowPhaseEnum.DESIGN.getValue().equals(phase)) {
                throw new BusinessException(ErrorCodeEnum.ORDER_NOT_APPLICABLE_STATUS);
            }

            // 3. 校验阶段+类型联合限制
            validateTypeInPhase(phase, dto.getApplyTypes());

            // 4. 校验申请类型有效性（枚举校验，无需 DB 查询）
            validateApplyTypes(dto.getApplyTypes());

            // 5. 校验无待审核申请（应用层校验，函数索引作为数据库层兜底）
            validateNoPendingApply(orderId);

            // 6. 构建申请实体
            OrderModifyApplyEntity apply = new OrderModifyApplyEntity();
            apply.setOrderId(orderId);
            apply.setOrderCode(order.getOrderCode());
            apply.setApplyTypeCodes(dto.getApplyTypes());
            apply.setApplyTypeNames(ModifyApplyTypeEnum.toNamesText(dto.getApplyTypes()));
            apply.setApplyReason(dto.getApplyReason());
            apply.setStatus(ModifyApplyStatusEnum.PENDING.getCode());
            apply.setApplicantId(StpUtil.getLoginIdAsLong());
            apply.setApplicantName(getCurrentUserName());
            orderModifyApplyMapper.insert(apply);

            log.info("发起修改申请成功，applyId={}", apply.getId());
            return toApplyVO(apply);
        } catch (BusinessException e) {
            throw e;
        } catch (Exception e) {
            log.error("发起修改申请异常，orderId={}", orderId, e);
            throw e;
        }
    }

    /**
     * 校验申请类型是否在当前阶段允许
     */
    private void validateTypeInPhase(Integer phase, String applyTypes) {
        if (FlowPhaseEnum.DESIGN.getValue().equals(phase)) {
            // 设计阶段：只允许 14.3（重建项目）
            for (String type : applyTypes.split(",")) {
                ModifyApplyTypeEnum typeEnum = ModifyApplyTypeEnum.getByDictCode(type.trim());
                if (typeEnum != null && typeEnum != ModifyApplyTypeEnum.ITEM) {
                    log.warn("设计阶段不允许申请类型：{}", type);
                    throw new BusinessException(ErrorCodeEnum.ORDER_MODIFY_TYPE_NOT_ALLOWED_IN_PHASE);
                }
            }
        }
    }

    /**
     * 校验无待审核申请（应用层兜底）
     */
    private void validateNoPendingApply(Long orderId) {
        Long count = orderModifyApplyMapper.selectCount(
            new LambdaQueryWrapper<OrderModifyApplyEntity>()
                .eq(OrderModifyApplyEntity::getOrderId, orderId)
                .eq(OrderModifyApplyEntity::getStatus, ModifyApplyStatusEnum.PENDING.getCode())
                .eq(OrderModifyApplyEntity::getIsDeleted, 0)
        );
        if (count > 0) {
            log.warn("订单已有待审核申请，orderId={}", orderId);
            throw new BusinessException(ErrorCodeEnum.ORDER_MODIFY_APPLY_EXISTS);
        }
    }

    /**
     * 校验申请类型有效性（枚举校验，无需 DB 查询）
     */
    private void validateApplyTypes(String applyTypes) {
        if (applyTypes == null || applyTypes.isBlank()) {
            throw new BusinessException(400, "申请类型不能为空");
        }
        for (String type : applyTypes.split(",")) {
            ModifyApplyTypeEnum typeEnum = ModifyApplyTypeEnum.getByDictCode(type.trim());
            if (typeEnum == null) {
                throw new BusinessException(400, "存在无效的申请类型：" + type);
            }
        }
    }

    // ==================== 申请撤回 ====================

    @Override
    @Transactional(rollbackFor = Exception.class)
    public void withdrawApply(Long applyId) {
        log.info("撤回修改申请，applyId={}", applyId);
        try {
            OrderModifyApplyEntity apply = orderModifyApplyMapper.selectById(applyId);
            if (apply == null) {
                throw new BusinessException(ErrorCodeEnum.ORDER_MODIFY_APPLY_NOT_FOUND);
            }
            if (!ModifyApplyStatusEnum.PENDING.getCode().equals(apply.getStatus())) {
                throw new BusinessException(ErrorCodeEnum.ORDER_MODIFY_APPLY_STATUS_ERROR);
            }
            if (!apply.getApplicantId().equals(StpUtil.getLoginIdAsLong())) {
                throw new BusinessException(ErrorCodeEnum.ORDER_MODIFY_APPLY_NOT_MINE);
            }
            orderModifyApplyMapper.deleteById(applyId);
            log.info("撤回修改申请成功，applyId={}", applyId);
        } catch (BusinessException e) {
            throw e;
        } catch (Exception e) {
            log.error("撤回修改申请异常，applyId={}", applyId, e);
            throw e;
        }
    }

    // ==================== 申请审核 ====================

    @Override
    @Transactional(rollbackFor = Exception.class)
    public void auditApply(Long applyId, AuditModifyApplyDTO dto) {
        log.info("审核修改申请，applyId={}, action={}", applyId, dto.getAction());
        try {
            OrderModifyApplyEntity apply = orderModifyApplyMapper.selectById(applyId);
            if (apply == null) {
                throw new BusinessException(ErrorCodeEnum.ORDER_MODIFY_APPLY_NOT_FOUND);
            }
            if (!ModifyApplyStatusEnum.PENDING.getCode().equals(apply.getStatus())) {
                throw new BusinessException(ErrorCodeEnum.ORDER_MODIFY_APPLY_ALREADY_PROCESSED);
            }
            String action = dto.getAction().toUpperCase();
            if (!"APPROVE".equals(action) && !"REJECT".equals(action)) {
                throw new BusinessException(400, "审核操作无效：" + dto.getAction());
            }
            if ("REJECT".equals(action) && StrUtil.isBlank(dto.getRejectReason())) {
                throw new BusinessException(ErrorCodeEnum.ORDER_MODIFY_REJECT_REASON_REQUIRED);
            }

            Long auditorId = StpUtil.getLoginIdAsLong();
            String auditorName = getCurrentUserName();
            apply.setAuditorId(auditorId);
            apply.setAuditorName(auditorName);
            apply.setAuditTime(LocalDateTime.now());
            if ("APPROVE".equals(action)) {
                apply.setStatus(ModifyApplyStatusEnum.APPROVED.getCode());
            } else {
                apply.setStatus(ModifyApplyStatusEnum.REJECTED.getCode());
                apply.setRejectReason(dto.getRejectReason());
            }
            orderModifyApplyMapper.updateById(apply);
            log.info("审核修改申请成功，applyId={}, status={}", applyId, apply.getStatus());
        } catch (BusinessException e) {
            throw e;
        } catch (Exception e) {
            log.error("审核修改申请异常，applyId={}", applyId, e);
            throw e;
        }
    }

    // ==================== 执行修改 ====================

    /**
     * 执行订单修改（审核通过后调用）
     * v5.0 改为 Map<String, Object> 动态传参，不再使用 ExecuteModificationDTO
     *
     * @param applyId       修改申请ID（路径参数，无需再传 orderId）
     * @param modifications 动态修改字段 Map（只传要改的字段）
     */
    @Override
    @Transactional(rollbackFor = Exception.class)
    public void executeModification(Long applyId, Map<String, Object> modifications) {
        log.info("执行订单修改，applyId={}", applyId);

        // 1. 校验申请状态（幂等保护：COMPLETED 直接报 718）
        OrderModifyApplyEntity apply = orderModifyApplyMapper.selectById(applyId);
        // ... 校验省略（见 OrderModifyApplyServiceImpl 源码）

        // 2. 从 apply_type_codes 解析允许的类型
        Set<String> allowedTypes = new HashSet<>(Arrays.asList(apply.getApplyTypeCodes().split(",")));

        // 3. 查订单实体（orderId 从申请中取，不需要调用方传入）
        OrderMainEntity order = orderMainMapper.selectById(apply.getOrderId());

        // 4. 分类型处理（每个 process* 方法只处理它认识的字段，未知字段静默忽略）
        boolean infoModified = false;
        if (allowedTypes.contains(ModifyApplyTypeEnum.INFO.getDictCode())) {
            processInfoModification(order, modifications, applyId, modifierId, modifierName);
            infoModified = true;
        }
        if (allowedTypes.contains(ModifyApplyTypeEnum.ITEM.getDictCode()) && modifications.containsKey("items")) {
            processItemModification(order, modifications, applyId, modifierId, modifierName);
        }
        if (allowedTypes.contains(ModifyApplyTypeEnum.IMAGE.getDictCode())) {
            processImageModification(order, modifications, applyId, modifierId, modifierName);
        }

        // 5. 仅 INFO 类型修改了 order 主表字段时才回写 DB（避免无意义的 update_time 变更）
        if (infoModified) {
            orderMainMapper.updateById(order);
        }

        // 6. 将申请状态置为 COMPLETED（防止重复执行）
        apply.setStatus(ModifyApplyStatusEnum.COMPLETED.getCode());
        orderModifyApplyMapper.updateById(apply);
    }

    // process*Modification 方法见 OrderModifyApplyServiceImpl 源码（各方法处理各自字段，Map 取值用 getLongValue/getStringValue 等辅助方法）

    /**
     * 记录修改留痕（旧值 == 新值时不记录）
     */
    private void recordModificationLog(Long orderId, String orderCode, Long applyId,
            String fieldName, String fieldLabel, Object oldValue, Object newValue,
            Long modifierId, String modifierName) {
        if (Objects.equals(oldValue, newValue)) {
            return;
        }
        OrderModificationLogEntity logEntity = new OrderModificationLogEntity();
        logEntity.setOrderId(orderId);
        logEntity.setOrderCode(orderCode);
        logEntity.setApplyId(applyId);
        logEntity.setFieldName(fieldName);
        logEntity.setFieldLabel(fieldLabel);
        logEntity.setOldValue(oldValue != null ? oldValue.toString() : null);
        logEntity.setNewValue(newValue != null ? newValue.toString() : null);
        logEntity.setModifierId(modifierId);
        logEntity.setModifierName(modifierName);
        orderModificationLogMapper.insert(logEntity);
    }
}
```

---

## 十二、与现有模块的交互

### 12.1 与设计模块（phase=2）的解耦方案

> **重要设计目标**：设计模块未来可能需要调用订单修改申请功能，因此本服务设计为**统一调用入口**，设计模块通过 Service 接口使用，不直接操作 Mapper。

**设计模块调用示例**：

```java
/**
 * 设计模块 Service
 */
@Service
@RequiredArgsConstructor
public class DesignProjectServiceImpl implements DesignProjectService {

    private final OrderModifyApplyService orderModifyApplyService;  // 统一入口

    /**
     * 设计阶段修改重建项目（通过修改申请流程）
     */
    @Override
    public void modifyRebuildProjectInDesignPhase(Long applyId, Map<String, Object> modifications) {
        // 仅允许 item 类型字段（可选：校验字段范围，静默忽略也是安全的）
        orderModifyApplyService.validateFieldsInScope(applyId, List.of("items"));
        // 执行修改（applyId 路径参数，orderId 由服务端从申请中取得）
        orderModifyApplyService.executeModification(applyId, modifications);
    }
}
```

### 12.2 与 OrderMainServiceImpl 的关系

`OrderMainServiceImpl.updateOrder()` **保持不变**，继续处理：
- 草稿阶段（phase=1, status=10）的订单更新
- 其他不经过修改申请流程的订单更新

**不将申请逻辑混入 `updateOrder()`**，保持职责单一。

### 12.3 与 Flow 模块的关系

**无依赖**。审核通过后订单状态不变，不调用 `FlowFacade`。

---

## 十三、修改申请阻断主流程机制（v4.0 新增）

### 13.1 问题描述

原有设计中 `order_modify_apply` 与 `order_main` 完全独立，存在修改申请时主流程仍可推进，导致两类问题：

| 状态 | 问题 |
|------|------|
| `PENDING`（待审核） | 管理员审批动作意义丧失：流程先行推进，申请悬空 |
| `APPROVED`（已批准未执行） | 已批准的修改内容永久丢失：流程推进后已无窗口执行修改 |

### 13.2 拦截范围

拦截 `OrderMainServiceImpl` 中以下 4 个会改变 `phase/status` 的方法（**不拦截 createOrder/createFromDraft**，`CREATE` 动作仅记录历史不流转）：

| 方法 | 动作 |
|------|------|
| `submitOrder` | SUBMIT_ORDER |
| `withdrawOrder` | WITHDRAW |
| `auditPass` | DATA_AUDIT_PASS |
| `auditReject` | DATA_AUDIT_REJECT |

### 13.3 阻断逻辑

在每个方法的订单存在性校验之后、`flowFacade.executeFlow()` 调用之前，插入：

```java
// 校验无阻断性修改申请
orderModifyApplyService.validateNoBlockingModifyApply(id);
```

`validateNoBlockingModifyApply` 实现：

1. 一次查询 `order_modify_apply`，条件：`order_id = orderId AND status IN ('PENDING', 'APPROVED') AND is_deleted = 0`
2. 无结果 → 静默返回（不阻断）
3. 有 `PENDING` → 抛 `ORDER_HAS_PENDING_MODIFY_APPLY(730)`
4. 有 `APPROVED`（无 PENDING）→ 抛 `ORDER_HAS_APPROVED_MODIFY_APPLY(731)`

> **优先级**：PENDING 优先于 APPROVED，因为 PENDING 说明申请尚未完成，更需要先处理。

### 13.4 新增错误码

| 错误码 | 枚举名 | 消息 | 触发场景 |
|--------|--------|------|----------|
| 730 | `ORDER_HAS_PENDING_MODIFY_APPLY` | 订单存在待审核的修改申请，请先处理后再操作 | 存在 PENDING 申请时尝试流转 |
| 731 | `ORDER_HAS_APPROVED_MODIFY_APPLY` | 订单存在已批准但未执行的修改申请，请先执行或撤销后再操作 | 存在 APPROVED 申请时尝试流转 |

### 13.5 清除阻断的路径

用户可通过以下操作清除阻断，使主流程恢复可流转：

| 操作 | 接口 | 效果 |
|------|------|------|
| 申请人撤销 | `DELETE /api/order/modify-apply/{applyId}` | 逻辑删除，`is_deleted=1`，不再阻断 |
| 管理员拒绝 | `PUT /api/order/modify-apply/{applyId}/audit`（action=REJECT） | `status→REJECTED`，不再阻断 |
| 业务员执行修改 | `PUT /api/order/{orderId}/modification` | `status→COMPLETED`，不再阻断 |

### 13.6 接口变更说明

`OrderModifyApplyService` 接口新增方法：

```java
/**
 * 校验订单是否存在阻断主流程的修改申请（PENDING 或 APPROVED 状态）
 * 存在时抛出 BusinessException，不存在时静默返回
 *
 * @param orderId 订单ID
 */
void validateNoBlockingModifyApply(Long orderId);
```

---

## 十四、实施清单

### 13.1 数据库层

| 序号 | 工作项 | 说明 |
|------|--------|------|
| 1 | 新建 `order_modify_apply` 表 | DDL 见 2.2 节，含动态唯一索引 |
| 2 | 新建 `order_modification_log` 表 | DDL 见 2.3 节，不继承 BaseEntity |
| 3 | 插入 `sys_dict` 字典数据 | 申请类型字典（modify_apply_type） |
| 4 | 插入 `sys_config` 配置记录 | 字段配置 JSON |

### 13.2 枚举层

| 序号 | 工作项 | 文件位置 |
|------|--------|---------|
| 5 | 新建 `ModifyApplyTypeEnum` | `order.enums.ModifyApplyTypeEnum` |
| 6 | 新建 `ModifyApplyStatusEnum` | `order.enums.ModifyApplyStatusEnum` |

### 13.3 实体层

| 序号 | 工作项 | 文件位置 |
|------|--------|---------|
| 7 | 新建 `OrderModifyApplyEntity` | `order.entity.OrderModifyApplyEntity`（继承 BaseEntity） |
| 8 | 新建 `OrderModificationLogEntity` | `order.entity.OrderModificationLogEntity`（不继承 BaseEntity） |

### 13.4 Mapper 层

| 序号 | 工作项 | 文件位置 |
|------|--------|---------|
| 9 | 新建 `OrderModifyApplyMapper` | `order.mapper.OrderModifyApplyMapper` |
| 10 | 新建 `OrderModificationLogMapper` | `order.mapper.OrderModificationLogMapper` |

### 13.5 DTO 层

| 序号 | 工作项 | 文件位置 |
|------|--------|---------|
| 11 | 新建 `CreateModifyApplyDTO` | `order.dto.modify.CreateModifyApplyDTO` |
| 12 | 新建 `AuditModifyApplyDTO` | `order.dto.modify.AuditModifyApplyDTO` |
| 13 | 新建 `ModifyApplyPageQueryDTO` | `order.dto.modify.ModifyApplyPageQueryDTO` |
| 14 | 新建 `ModificationLogPageQueryDTO` | `order.dto.modify.ModificationLogPageQueryDTO` |
| ~~15~~ | ~~新建 `ExecuteModificationDTO`~~ | **v5.0 已删除**，改为 Map 动态传参 |
| ~~15a~~ | ~~新建 `ExecuteModificationItemDTO`~~ | **v5.0 已删除** |
| 16 | 新建 `ModifyApplyFieldConfigDTO` | `order.dto.modify.ModifyApplyFieldConfigDTO` |

### 13.6 VO 层

| 序号 | 工作项 | 文件位置 |
|------|--------|---------|
| 17 | 新建 `ModifyApplyVO` | `order.vo.modify.ModifyApplyVO` |
| 18 | 新建 `ModifyApplyListVO` | `order.vo.modify.ModifyApplyListVO` |
| 19 | 新建 `ModifyApplyDetailVO` | `order.vo.modify.ModifyApplyDetailVO` |
| 20 | 新建 `ModifyApplyFieldConfigVO` | `order.vo.modify.ModifyApplyFieldConfigVO` |
| 21 | 新建 `ModificationLogVO` | `order.vo.modify.ModificationLogVO` |
| 22 | 新建 `CanApplyModifyResult` | `order.vo.modify.CanApplyModifyResult`（阶段判断结果） |

### 13.7 Service 层

| 序号 | 工作项 | 文件位置 |
|------|--------|---------|
| 23 | 新建 `OrderModifyApplyService` 接口 | `order.service.OrderModifyApplyService` |
| 24 | 新建 `OrderModifyApplyServiceImpl` | `order.service.impl.OrderModifyApplyServiceImpl` |

### 13.8 Controller 层

| 序号 | 工作项 | 文件位置 |
|------|--------|---------|
| 25 | 新建 `OrderModifyApplyController` | `order.controller.OrderModifyApplyController` |

### 13.9 常量层

| 序号 | 工作项 | 文件位置 |
|------|--------|---------|
| 26 | 新增错误码 715-722 | `common.enums.ErrorCodeEnum` |
| 27 | 新增配置键 `ORDER_MODIFY_FIELD_CONFIG` | `common.enums.SystemConfigKeyEnum`（配置 key = `"order.modify.field.config"`） |

### 13.10 测试

| 序号 | 工作项 | 说明 |
|------|--------|------|
| 27 | 新建 `OrderModifyApplyServiceImplTest` | Service 层单元测试 |
| 28 | 新建 `OrderModifyApplyControllerTest` | Controller 接口测试 |

---

## 十五、版本历史

| 版本 | 日期 | 修改内容 | 作者 |
|-----|------|---------|------|
| 1.0 | 2026-04-08 | 初始版本：基于4.6需求文档和业务澄清，完整设计实现方案 | hanjor |
| **2.0** | **2026-04-08** | **重大变更：移除申请编号（改用ID）；阶段限制修正为订单+设计阶段（仅item类型）；设计解耦方案（统一调用接口）；新增字典数据；错误码起始码修正为715；采用MySQL8动态索引** | **hanjor** |
| **3.0** | **2026-04-08** | **字段拆分：apply_type 拆为 apply_type_codes(VARCHAR 50)+apply_type_names(VARCHAR 200)；字典编码体系改为 group=14（14.1/14.2/14.3）；ModifyApplyTypeEnum.dictCode 更新为 14.x；sys_config JSON 顶层 key 改为 14.1/14.2/14.3；实体/Service 代码同步更新；移除 DictService 依赖；新增 HospitalMapper 依赖；修正 executeModification 中 applyTypeCodes 解析；新增 ExecuteModificationItemDTO 明确 orderItemId 语义；service 代码使用显式字段赋值代替反射** | **hanjor** |
| **4.0** | **2026-04-10** | **新增：修改申请阻断主流程机制。PENDING/APPROVED 状态的修改申请会阻断 submitOrder/withdrawOrder/auditPass/auditReject 的流转；新增错误码 730/731；validateNoBlockingModifyApply 接口方法；清除路径：撤销申请、管理员拒绝、执行修改** | **hanjor** |
| **5.0** | **2026-04-10** | **executeModification 接口重构：移除 ExecuteModificationDTO/ExecuteModificationItemDTO，改为 Map<String, Object> 动态传参；接口路径从 PUT /order/modify/{orderId}/execute?applyId={applyId} 改为 PUT /order/modify/execute/{applyId}；修复 orderMainMapper.updateById 无条件调用问题（仅 INFO 类型修改时才回写主表）；更新接口文档 19.21 和技术文档 10.10** | **hanjor** |
