# 产品型号管理功能实现方案

## 7.1 设计目标

产品体系采用**产品 + 规格**两层结构：
- **产品（Product）**：按大类管理，如「骨模型」「导板」
- **规格（ProductSpec）**：每个产品下可关联多条规格，每条规格独立关联注册证

这种设计支持：
1. 同一产品下多种规格共享产品名称
2. 规格独立关联注册证，满足合规追溯
3. 级联下拉：选择产品 → 过滤规格 → 带出注册证号

## 7.2 表结构

### 7.2.1 产品表（product）

```sql
CREATE TABLE product (
    id              BIGINT          NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    product_name    VARCHAR(128)    NOT NULL COMMENT '产品名称',
    category        VARCHAR(20)     COMMENT '产品大类 dict_code（如 17.1）',
    category_name   VARCHAR(64)     COMMENT '大类名称（冗余）',
    status          TINYINT         DEFAULT 1 COMMENT '状态（0=禁用，1=正常）',
    remark          VARCHAR(512)    COMMENT '备注',
    create_time     DATETIME        DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time     DATETIME        DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    create_by       BIGINT          DEFAULT NULL COMMENT '创建人ID',
    update_by       BIGINT          DEFAULT NULL COMMENT '更新人ID',
    is_deleted      TINYINT         DEFAULT 0 COMMENT '是否删除',
    PRIMARY KEY (id),
    KEY idx_product_category (category),
    KEY idx_product_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='产品表';
-- 函数唯一索引（逻辑删除兼容）
CREATE UNIQUE INDEX uk_product_name ON product ((CASE WHEN is_deleted = 0 THEN product_name ELSE NULL END));
```

### 7.2.2 产品规格表（product_spec）

```sql
CREATE TABLE product_spec (
    id              BIGINT          NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    product_id      BIGINT          NOT NULL COMMENT '关联产品ID',
    spec_name       VARCHAR(128)    NOT NULL COMMENT '规格名称',
    cert_id         BIGINT          DEFAULT NULL COMMENT '关联注册证ID（可空）',
    cert_no         VARCHAR(64)     DEFAULT NULL COMMENT '注册证号（冗余）',
    sort            INT             DEFAULT 0 COMMENT '排序',
    status          TINYINT         DEFAULT 1 COMMENT '状态（0=禁用，1=正常）',
    remark          VARCHAR(512)    COMMENT '备注',
    create_time     DATETIME        DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time     DATETIME        DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    create_by       BIGINT          DEFAULT NULL COMMENT '创建人ID',
    update_by       BIGINT          DEFAULT NULL COMMENT '更新人ID',
    is_deleted      TINYINT         DEFAULT 0 COMMENT '是否删除',
    PRIMARY KEY (id),
    KEY idx_product_spec_product_id (product_id),
    KEY idx_product_spec_cert_id (cert_id),
    KEY idx_product_spec_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='产品规格表';
-- 同一产品下规格名唯一（逻辑删除兼容）
CREATE UNIQUE INDEX uk_product_spec_name ON product_spec ((CASE WHEN is_deleted = 0 THEN CONCAT_WS('|', product_id, spec_name) ELSE NULL END));
```

## 7.3 代码结构

位于 `basic/product` 子模块：

```
basic/product/
├── controller/ProductController.java          # 产品 + 规格的 REST 接口
├── service/
│   ├── ProductService.java                    # 产品服务接口
│   ├── ProductSpecService.java                # 规格服务接口
│   └── impl/
│       ├── ProductServiceImpl.java            # 产品服务实现
│       └── ProductSpecServiceImpl.java        # 规格服务实现
├── mapper/
│   ├── ProductMapper.java
│   └── ProductSpecMapper.java
├── entity/
│   ├── ProductEntity.java                     # 产品实体
│   └── ProductSpecEntity.java                 # 规格实体
├── dto/
│   ├── CreateProductDTO.java
│   ├── UpdateProductDTO.java
│   ├── ProductPageDTO.java
│   ├── ProductListDTO.java
│   ├── ProductCategoryDTO.java
│   ├── CreateProductSpecDTO.java              # 创建规格
│   └── UpdateProductSpecDTO.java              # 更新规格
├── vo/
│   ├── ProductVO.java                         # 产品视图（含 specs 列表）
│   └── ProductSpecVO.java                     # 规格视图
└── convert/ProductConvert.java
```

## 7.4 核心接口

### 7.4.1 产品接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/basic/product/page` | 分页查询产品 |
| POST | `/basic/product/list` | 查询所有产品 |
| GET | `/basic/product/{id}` | 查询产品详情（含 specs） |
| POST | `/basic/product` | 创建产品 |
| PUT | `/basic/product/{id}` | 更新产品 |
| DELETE | `/basic/product/{id}` | 删除产品（有规格时拒绝） |
| POST | `/basic/product/list-by-category` | 按分类查询产品 |

### 7.4.2 规格接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/basic/product/{id}/spec` | 创建规格 |
| GET | `/basic/product/{id}/specs` | 查询规格列表 |
| PUT | `/basic/product/spec/{specId}` | 更新规格 |
| DELETE | `/basic/product/spec/{specId}` | 删除规格（被引用时拒绝） |

### 7.4.3 设计模块打印信息选项

```java
/**
 * 查询所有启用产品及其启用规格
 * 用于打印信息选项数据（产品树）
 */
List<ProductVO> listAllWithSpecs();
```

## 7.5 业务规则

### 7.5.1 产品删除校验

产品下存在规格时，拒绝删除，抛出 `PRODUCT_HAS_SPECS(741)`。

### 7.5.2 规格删除校验

规格被 `design_product`（打印信息）引用时，拒绝删除，抛出 `PRODUCT_SPEC_IN_USE(744)`。

该校验通过 `SpecReferenceChecker` 接口实现跨模块解耦：
- 接口定义在 `yigongbao-common` 模块
- 实现类 `SpecReferenceCheckerImpl` 在 `yigongbao-module-design` 模块
- `ProductSpecServiceImpl` 通过 `@Autowired(required=false)` 注入

### 7.5.3 级联关系

```
选择产品
  → 过滤规格列表（只显示该产品下 status=1 的规格）
  → 选择规格后
      → 自动带出注册证号（spec.certNo）
```

## 7.6 错误码扩展

```java
// 产品相关
PRODUCT_NOT_FOUND(648, "产品不存在"),
PRODUCT_EXISTS(649, "产品名称已存在"),
PRODUCT_HAS_SPECS(741, "产品下存在规格，无法删除"),

// 规格相关
PRODUCT_SPEC_NOT_FOUND(742, "产品规格不存在"),
PRODUCT_SPEC_EXISTS(743, "同一产品下规格名称已存在"),
PRODUCT_SPEC_IN_USE(744, "规格已被打印信息引用，无法删除");
```

## 7.7 代码文件清单

| 类型 | 路径 |
|------|------|
| 产品实体 | `yigongbao-module-basic/.../product/entity/ProductEntity.java` |
| 规格实体 | `yigongbao-module-basic/.../product/entity/ProductSpecEntity.java` |
| 产品Mapper | `yigongbao-module-basic/.../product/mapper/ProductMapper.java` |
| 规格Mapper | `yigongbao-module-basic/.../product/mapper/ProductSpecMapper.java` |
| 产品Service | `yigongbao-module-basic/.../product/service/ProductService.java` |
| 规格Service | `yigongbao-module-basic/.../product/service/ProductSpecService.java` |
| 产品ServiceImpl | `yigongbao-module-basic/.../product/service/impl/ProductServiceImpl.java` |
| 规格ServiceImpl | `yigongbao-module-basic/.../product/service/impl/ProductSpecServiceImpl.java` |
| Controller | `yigongbao-module-basic/.../product/controller/ProductController.java` |
| 产品VO | `yigongbao-module-basic/.../product/vo/ProductVO.java` |
| 规格VO | `yigongbao-module-basic/.../product/vo/ProductSpecVO.java` |
| 引用检查接口 | `yigongbao-common/.../service/SpecReferenceChecker.java` |
| 引用检查实现 | `yigongbao-module-design/.../service/impl/SpecReferenceCheckerImpl.java` |

---

**文档信息**

| 项目 | 内容 |
|------|------|
| 文档版本 | v2.0 |
| 创建日期 | 2026-03-24 |
| 最后修订 | 2026-04-15 |
| 作者 | hanjor |

**变更记录**

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0 | 2026-03-24 | 初始版本 |
| v2.0 | 2026-04-15 | 重构为产品+规格两层结构；新增 ProductSpecEntity/Service；新增跨模块引用检查；更新错误码 |
