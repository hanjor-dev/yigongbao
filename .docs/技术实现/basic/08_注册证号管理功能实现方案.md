# 注册证号管理功能实现方案

## 8.1 设计目标

注册证号管理，关联产品型号，支持有效期管理和过期提醒。

## 8.2 表结构

```sql
CREATE TABLE registration_cert (
    id              BIGINT          NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    cert_code       VARCHAR(64)     NOT NULL COMMENT '注册证号',
    cert_name       VARCHAR(256)   NOT NULL COMMENT '注册证名称',
    valid_from      DATE            COMMENT '有效期开始',
    valid_to        DATE            COMMENT '有效期截止',
    cert_file_url   VARCHAR(512)   COMMENT '注册证扫描件URL',
    status          TINYINT        DEFAULT 1 COMMENT '状态（0=过期，1=有效）',
    remark          VARCHAR(512)    COMMENT '备注',
    create_time     DATETIME        DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time     DATETIME        DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    is_deleted      TINYINT        DEFAULT 0 COMMENT '是否删除',
    PRIMARY KEY (id),
    UNIQUE KEY uk_cert_code (cert_code),
    KEY idx_cert_valid_to (valid_to),
    KEY idx_cert_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='注册证表';
```

## 8.3 代码结构

位于 `basic/registrationCert` 子模块：

```
basic/registrationCert/
├── controller/RegistrationCertController.java
├── service/RegistrationCertService.java
├── service/impl/RegistrationCertServiceImpl.java
├── mapper/RegistrationCertMapper.java
├── entity/RegistrationCertEntity.java
├── dto/CreateRegistrationCertDTO.java
├── vo/RegistrationCertVO.java
└── convert/RegistrationCertConvert.java
```

## 8.4 有效期自动更新

通过定时任务每日检查注册证有效期，将过期的注册证状态自动更新为 0（过期）。

```java
/**
 * 更新过期注册证状态
 * 每日凌晨执行
 */
@Scheduled(cron = "0 0 1 * * ?")
public void updateExpiredCerts() {
    registrationCertMapper.updateExpiredCerts(LocalDate.now());
}
```

## 8.5 错误码扩展

```java
CERT_NOT_FOUND(720, "注册证不存在"),
CERT_EXISTS(721, "注册证号已存在"),
CERT_EXPIRED(722, "注册证已过期");
```

## 8.6 代码文件清单

| 文件 | 路径 |
|------|------|
| 实体 | `yigongbao-module-basic/.../registrationCert/entity/RegistrationCertEntity.java` |
| Mapper | `yigongbao-module-basic/.../registrationCert/mapper/RegistrationCertMapper.java` |
| Service接口 | `yigongbao-module-basic/.../registrationCert/service/RegistrationCertService.java` |
| Service实现 | `yigongbao-module-basic/.../registrationCert/service/impl/RegistrationCertServiceImpl.java` |
| Controller | `yigongbao-module-basic/.../registrationCert/controller/RegistrationCertController.java` |
| DTO | `dto/CreateRegistrationCertDTO.java` |
| VO | `vo/RegistrationCertVO.java` |
| Convert | `convert/RegistrationCertConvert.java` |

---

**文档信息**

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.0 |
| 创建日期 | 2026-03-24 |
| 作者 | hanjor |
