# 统一编码生成器功能实现方案

## 1. 设计目标

提供统一的编码生成服务，支持可配置的编码规则、序号管理、并发安全。

---

## 2. 设计决策

| 决策项 | 选择 |
|-------|------|
| 编码格式 | 统一4位序号，如 `HOS-0001` |
| 机构编码方案 | 单规则 + 动态前缀（扩展编码器） |
| 层级编码处理 | 改为平铺编码（移除父子层级编码逻辑） |
| 部位/项目前缀 | 带前缀，如 `BP-0001`、`RP-0001` |

---

## 3. 当前编码生成器能力分析

### 3.1 支持的格式

| 组成 | 示例 | 支持状态 |
|-----|------|---------|
| 固定前缀 | `HOS-` | ✅ 支持 |
| 日期部分 | `{yyyy}{MM}{dd}` | ✅ 支持 |
| 序号部分 | `0001` | ✅ 支持 |
| 完整示例 | `HOS-20260325-0001` | ✅ 支持 |

### 3.2 不支持的格式（需扩展）

| 需求 | 示例 | 说明 |
|-----|------|------|
| 动态前缀 | `ORG-P-`、`ORG-D-` | 前缀根据业务参数变化 |

### 3.3 新增能力（v1.2）

#### 3.3.1 序号后缀生成（generateWithSeqSuffix）

**设计目标**：支持按业务标识（`bizKey`）隔离序号池，用于同一业务前缀下生成多个子编码。

**典型场景**：一个订单对应多个数据包，前缀统一采用订单编号，序号后缀表示第几个数据包。

| bizKey | 序号池 | 生成结果 |
|--------|--------|---------|
| `202603250001` | 独立序号池A | `202603250001-1`、`202603250001-2` |
| `202603250002` | 独立序号池B | `202603250002-1`、`202603250002-2` |

**数据库支持**：`sys_code_sequence` 表新增 `biz_key` 字段：

```sql
biz_key VARCHAR(64) DEFAULT NULL COMMENT '业务标识（用于按业务维度隔离序号，如订单编号，为空表示全局序号）',
UNIQUE KEY uk_seq_rule_biz_key (rule_code, biz_key)
```

> `biz_key` 为空时表示全局序号（兼容原有逻辑），非空时按 `rule_code + biz_key` 隔离序号池。

#### 3.3.2 编码规则常量（CodeRuleConstants）

**设计目标**：统一管理所有编码规则编码值，消除硬编码。

**常量类位置**：`com.yigongbao.common.constant.CodeRuleConstants`

```java
public final class CodeRuleConstants {
    private CodeRuleConstants() {}

    // ==================== 基础模块编码 ====================
    public static final String HOSPITAL_NO = "HOSPITAL_NO";
    public static final String HDEPT_NO = "HDEPT_NO";
    public static final String PROJECT_NO = "PROJECT_NO";
    public static final String PRODUCT_CODE = "PRODUCT_CODE";
    public static final String BODYPART_NO = "BODYPART_NO";
    public static final String TEMPLATE_NO = "TEMPLATE_NO";
    public static final String DOCTOR_NO = "DOCTOR_NO";

    // ==================== 订单相关编码 ====================
    public static final String ORDER_NO = "ORDER_NO";
    public static final String ORDER_ITEM_NO = "ORDER_ITEM_NO";
    public static final String DATA_PACKAGE_NO = "DATA_PACKAGE_NO";
    public static final String INSTRUCTION_NO = "INSTRUCTION_NO";

    // ==================== 其他编码 ====================
    public static final String FILE_NO = "FILE_NO";
}
```

#### 3.3.3 四种生成方式汇总

| 方法 | 签名 | 典型场景 | 生成示例 |
|------|------|---------|---------|
| `generate` | `String generate(String ruleCode)` | 通用编码生成 | `HOS-0001` |
| `generateWithBizPrefix` | `String generateWithBizPrefix(String ruleCode, String bizPrefix)` | 业务前缀拼接（占全局序号） | `ZL-ORD-000001-0001` |
| `generateWithCustomPrefix` | `String generateWithCustomPrefix(String ruleCode, String prefix)` | 动态前缀拼接（占全局序号） | `ORG-P-0001` |
| `generateWithSeqSuffix` | `String generateWithSeqSuffix(String ruleCode, String bizKey)` | 序号后缀（按bizKey隔离序号池） | `202603250001-1` |

#### 3.3.4 前缀置空支持

`sys_code_rule.prefix` 字段可置空，系统会自动跳过前缀拼接。

| 前缀 | 日期格式 | 序号长度 | 生成结果 |
|------|---------|---------|---------|
| `ORD-` | `{yyyy}{MM}{dd}` | 4 | `ORD-20260325-0001` |
| （空） | `{yyyy}{MM}{dd}` | 4 | `20260325-0001` |
| （空） | （空） | 6 | `000001` |

---

## 4. 编码规则

### 4.1 基础编码规则

| 编码类型 | 规则示例 | 重置策略 | 说明 |
|---------|---------|---------|------|
| 订单编号 | `ORD-20260324-000001` | 每日重置 | 年月日 + 6位流水 |
| 工单编号 | `WO-202603-0001` | 每月重置 | 年月 + 4位流水 |
| 指令单编号 | `ZL-ORD-20260324-000001-1` | 不重置 | 固定前缀 + 订单号 + 序号 |
| 图纸编号 | `TZ-ORD-20260324-000001` | 不重置 | 固定前缀 + 订单号 |
| 影像数据包编号 | `SJB-20260324-0001` | 每日重置 | 前缀 + 年月日 + 4位流水 |
| UDI码 | `UDI-00000001` | 不重置 | 无日期，全局序号 |

### 4.2 业务编码规则

| 编码类型 | 规则示例 | 重置策略 | 说明 |
|---------|---------|---------|------|
| 医院编码 | `HOS-0001` | 不重置 | 4位序号 |
| 医院组合模板编码 | `TPL-0001` | 不重置 | 4位序号 |
| 部门编码 | `DEPT-0001` | 不重置 | 4位序号 |
| 机构编码 | `ORG-P-0001` | 不重置 | 动态前缀 + 4位序号 |
| 重建部位编码 | `BP-0001` | 不重置 | 4位序号 |
| 重建项目编码 | `RP-0001` | 不重置 | 4位序号 |
| 产品型号编码 | `PROD-0001` | 不重置 | 4位序号 |

---

## 5. 表结构

### 5.1 编码规则表

```sql
CREATE TABLE sys_code_rule (
    id              BIGINT          NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    rule_code      VARCHAR(64)     NOT NULL COMMENT '规则编码（如：ORDER_NO）',
    rule_name      VARCHAR(128)    NOT NULL COMMENT '规则名称',
    prefix         VARCHAR(32)     COMMENT '前缀（如：ORD-）',
    date_format    VARCHAR(64)     COMMENT '日期格式（支持 {yyyy}{MM}{dd} 等）',
    seq_length     INT             DEFAULT 6 COMMENT '序号长度（不够补0）',
    reset_type     VARCHAR(32)     DEFAULT 'NEVER' COMMENT '重置类型（DAY/MONTH/YEAR/NEVER）',
    current_value  BIGINT          DEFAULT 0 COMMENT '当前序号值',
    step           INT             DEFAULT 1 COMMENT '递增步长',
    status         TINYINT         DEFAULT 1 COMMENT '状态（0=禁用，1=启用）',
    remark         VARCHAR(512)    COMMENT '备注',
    create_time    DATETIME        DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time    DATETIME        DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    is_deleted     TINYINT         DEFAULT 0 COMMENT '是否删除',
    PRIMARY KEY (id),
    UNIQUE KEY uk_rule_code (rule_code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='编码规则表';
```

### 5.2 编码序号表

```sql
CREATE TABLE sys_code_sequence (
    id              BIGINT          NOT NULL AUTO_INCREMENT COMMENT '主键ID',
    rule_code      VARCHAR(64)     NOT NULL COMMENT '规则编码',
    biz_key        VARCHAR(64)     DEFAULT NULL COMMENT '业务标识（用于按业务维度隔离序号，如订单编号，为空表示全局序号）',
    current_seq    BIGINT          DEFAULT 0 COMMENT '当前序号',
    last_date      DATE            COMMENT '上次重置日期（用于判断是否需要重置）',
    version        INT             DEFAULT 0 COMMENT '乐观锁版本号',
    create_time    DATETIME        DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time    DATETIME        DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_seq_rule_biz_key (rule_code, biz_key),
    KEY idx_seq_rule_code (rule_code),
    KEY idx_seq_last_date (last_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='编码序号表';
```

---

## 6. 代码设计

### 6.1 编码规则实体

```java
// 路径: yigongbao-module-basic/src/main/java/com/yigongbao/module/basic/code/entity/CodeRuleEntity.java

@Data
@TableName("sys_code_rule")
@EqualsAndHashCode(callSuper = false)
public class CodeRuleEntity extends BaseEntity implements Serializable {

    private static final long serialVersionUID = 1L;

    /** 规则编码（如：ORDER_NO） */
    private String ruleCode;

    /** 规则名称 */
    private String ruleName;

    /** 前缀（如：ORD-） */
    private String prefix;

    /** 日期格式（支持 {yyyy}{MM}{dd} 等） */
    private String dateFormat;

    /** 序号长度（不够补0） */
    private Integer seqLength;

    /** 重置类型（DAY/MONTH/YEAR/NEVER） */
    private String resetType;

    /** 当前序号值 */
    private Long currentValue;

    /** 递增步长 */
    private Integer step;

    /** 状态（0=禁用，1=启用） */
    private Integer status;

    /** 备注 */
    private String remark;
}
```

### 6.2 重置类型枚举

```java
// 路径: yigongbao-common/src/main/java/com/yigongbao/common/enums/CodeResetTypeEnum.java

public enum CodeResetTypeEnum {
    DAY("DAY", "每日重置"),
    MONTH("MONTH", "每月重置"),
    YEAR("YEAR", "每年重置"),
    NEVER("NEVER", "不重置");

    private final String code;
    private final String name;

    CodeResetTypeEnum(String code, String name) {
        this.code = code;
        this.name = name;
    }
}
```

### 6.3 编码生成服务接口

```java
// 路径: yigongbao-module-basic/src/main/java/com/yigongbao/module/basic/code/service/CodeGeneratorService.java

package com.yigongbao.module.basic.code.service;

import com.yigongbao.common.exception.BusinessException;

/**
 * 编码生成服务
 *
 * @author hanjor
 * @date 2026-03-24
 */
public interface CodeGeneratorService {

    /**
     * 生成编码
     *
     * @param ruleCode 规则编码（如 ORDER_NO）
     * @return 编码字符串
     * @throws BusinessException 规则不存在或已禁用
     */
    String generate(String ruleCode);

    /**
     * 生成带业务前缀的编码
     * 用于指令单编号：ZL-{订单编号}-{序号}
     *
     * @param ruleCode 规则编码
     * @param bizPrefix 业务前缀（如订单编号）
     * @return 编码字符串
     */
    String generateWithBizPrefix(String ruleCode, String bizPrefix);

    /**
     * 生成带自定义前缀的编码
     * 用于机构编码等需要动态前缀的场景
     *
     * @param ruleCode 规则编码
     * @param prefix 自定义前缀（如 ORG-P-）
     * @return 完整编码（如 ORG-P-0001）
     */
    String generateWithCustomPrefix(String ruleCode, String prefix);

    /**
     * 生成带序号后缀的编码
     * 用于同一业务前缀下生成多个子编码，如数据包编码：202603250001-1、202603250001-2
     * 每个 bizKey 有独立的序号池，互不影响
     *
     * @param ruleCode 规则编码
     * @param bizKey 业务标识（如订单编号，用于隔离序号池）
     * @return 带序号后缀的编码（如 202603250001-1）
     */
    String generateWithSeqSuffix(String ruleCode, String bizKey);

    /**
     * 预览编码格式
     *
     * @param ruleCode 规则编码
     * @return 预览编码（示例）
     */
    String preview(String ruleCode);
}
```

### 6.4 编码生成服务实现

```java
// 路径: yigongbao-module-basic/src/main/java/com/yigongbao/module/basic/code/service/impl/CodeGeneratorServiceImpl.java

@Service
@RequiredArgsConstructor
@Slf4j
public class CodeGeneratorServiceImpl implements CodeGeneratorService {

    private final CodeRuleMapper codeRuleMapper;
    private final CodeSequenceMapper codeSequenceMapper;

    @Override
    @Transactional(rollbackFor = Exception.class)
    public String generate(String ruleCode) {
        log.info("生成编码，ruleCode={}", ruleCode);
        try {
            // 1. 获取规则配置
            CodeRuleEntity rule = codeRuleMapper.selectByRuleCode(ruleCode);
            if (rule == null) {
                log.warn("编码规则不存在，ruleCode={}", ruleCode);
                throw new BusinessException(ErrorCodeEnum.CODE_RULE_NOT_FOUND);
            }
            if (rule.getStatus() != null && rule.getStatus().equals(StatusConstants.DISABLED)) {
                log.warn("编码规则已禁用，ruleCode={}", ruleCode);
                throw new BusinessException(ErrorCodeEnum.CODE_RULE_DISABLED);
            }

            // 2. 获取或创建序号记录
            CodeSequenceEntity sequence = getOrCreateSequence(ruleCode);

            // 3. 检查是否需要重置
            checkAndReset(rule, sequence);

            // 4. 递增序号
            long newSeq = sequence.getCurrentSeq() + (rule.getStep() != null ? rule.getStep() : 1);
            sequence.setCurrentSeq(newSeq);

            // 乐观锁更新（@Version 会自动在 WHERE 中追加 version 条件）
            int rows = codeSequenceMapper.updateById(sequence);
            if (rows == 0) {
                log.warn("编码序号更新冲突，重试生成，ruleCode={}", ruleCode);
                return generate(ruleCode);
            }

            // 5. 组装编码
            String code = buildCode(rule, newSeq);
            log.info("生成编码成功，ruleCode={}, code={}", ruleCode, code);
            return code;

        } catch (BusinessException e) {
            throw e;
        } catch (Exception e) {
            log.error("生成编码异常，ruleCode={}", ruleCode, e);
            throw new BusinessException(ErrorCodeEnum.CODE_GENERATE_FAILED);
        }
    }

    @Override
    public String generateWithBizPrefix(String ruleCode, String bizPrefix) {
        log.info("生成带业务前缀的编码，ruleCode={}, bizPrefix={}", ruleCode, bizPrefix);
        try {
            String baseCode = generate(ruleCode);
            CodeRuleEntity rule = codeRuleMapper.selectByRuleCode(ruleCode);
            if (rule == null) {
                log.warn("编码规则不存在，生成失败，返回基础编码，ruleCode={}", ruleCode);
                return baseCode;
            }
            String result = rule.getPrefix() + bizPrefix + "-" + baseCode;
            log.info("生成带业务前缀的编码成功，ruleCode={}, bizPrefix={}, result={}", ruleCode, bizPrefix, result);
            return result;
        } catch (Exception e) {
            log.error("生成带业务前缀的编码异常，ruleCode={}, bizPrefix={}", ruleCode, bizPrefix, e);
            throw e;
        }
    }

    @Override
    public String generateWithCustomPrefix(String ruleCode, String prefix) {
        log.info("生成带自定义前缀的编码，ruleCode={}, prefix={}", ruleCode, prefix);
        try {
            String seq = generate(ruleCode);
            String result = prefix + seq;
            log.info("生成带自定义前缀的编码成功，ruleCode={}, prefix={}, result={}", ruleCode, prefix, result);
            return result;
        } catch (Exception e) {
            log.error("生成带自定义前缀的编码异常，ruleCode={}, prefix={}", ruleCode, prefix, e);
            throw e;
        }
    }

    @Override
    @Transactional(rollbackFor = Exception.class)
    public String generateWithSeqSuffix(String ruleCode, String bizKey) {
        log.info("生成带序号后缀的编码，ruleCode={}, bizKey={}", ruleCode, bizKey);
        try {
            // 1. 获取规则配置
            CodeRuleEntity rule = codeRuleMapper.selectByRuleCode(ruleCode);
            if (rule == null) {
                log.warn("编码规则不存在，ruleCode={}", ruleCode);
                throw new BusinessException(ErrorCodeEnum.CODE_RULE_NOT_FOUND);
            }
            if (rule.getStatus() != null && rule.getStatus().equals(StatusConstants.DISABLED)) {
                log.warn("编码规则已禁用，ruleCode={}", ruleCode);
                throw new BusinessException(ErrorCodeEnum.CODE_RULE_DISABLED);
            }

            // 2. 获取或创建序号记录（按 bizKey 隔离）
            CodeSequenceEntity sequence = getOrCreateSequenceWithBizKey(ruleCode, bizKey);

            // 3. 检查是否需要重置
            checkAndReset(rule, sequence);

            // 4. 递增序号
            long newSeq = sequence.getCurrentSeq() + (rule.getStep() != null ? rule.getStep() : 1);
            sequence.setCurrentSeq(newSeq);

            // 乐观锁更新
            int rows = codeSequenceMapper.updateById(sequence);
            if (rows == 0) {
                log.warn("编码序号更新冲突，重试生成，ruleCode={}, bizKey={}", ruleCode, bizKey);
                return generateWithSeqSuffix(ruleCode, bizKey);
            }

            // 5. 组装编码：业务前缀 + "-" + 序号（序号不带补零）
            String result = bizKey + "-" + newSeq;
            log.info("生成带序号后缀的编码成功，ruleCode={}, bizKey={}, result={}", ruleCode, bizKey, result);
            return result;

        } catch (BusinessException e) {
            throw e;
        } catch (Exception e) {
            log.error("生成带序号后缀的编码异常，ruleCode={}, bizKey={}", ruleCode, bizKey, e);
            throw new BusinessException(ErrorCodeEnum.CODE_GENERATE_FAILED);
        }
    }

    @Override
    public String preview(String ruleCode) {
        log.info("预览编码格式，ruleCode={}", ruleCode);
        try {
            CodeRuleEntity rule = codeRuleMapper.selectByRuleCode(ruleCode);
            if (rule == null) {
                log.warn("编码规则不存在，ruleCode={}", ruleCode);
                throw new BusinessException(ErrorCodeEnum.CODE_RULE_NOT_FOUND);
            }
            String result = buildCode(rule, 1L).replace("1", "N");
            log.info("预览编码格式成功，ruleCode={}, preview={}", ruleCode, result);
            return result;
        } catch (BusinessException e) {
            throw e;
        } catch (Exception e) {
            log.error("预览编码格式异常，ruleCode={}", ruleCode, e);
            throw e;
        }
    }

    /**
     * 获取或创建序号记录（全局）
     */
    private CodeSequenceEntity getOrCreateSequence(String ruleCode) {
        CodeSequenceEntity sequence = codeSequenceMapper.selectByRuleCode(ruleCode);
        if (sequence == null) {
            sequence = new CodeSequenceEntity();
            sequence.setRuleCode(ruleCode);
            sequence.setCurrentSeq(0L);
            sequence.setLastDate(LocalDate.now());
            sequence.setVersion(0);
            codeSequenceMapper.insert(sequence);
        }
        return sequence;
    }

    /**
     * 获取或创建序号记录（支持业务标识隔离）
     */
    private CodeSequenceEntity getOrCreateSequenceWithBizKey(String ruleCode, String bizKey) {
        CodeSequenceEntity sequence = codeSequenceMapper.selectByRuleCodeAndBizKey(ruleCode, bizKey);
        if (sequence == null) {
            sequence = new CodeSequenceEntity();
            sequence.setRuleCode(ruleCode);
            sequence.setBizKey(bizKey);
            sequence.setCurrentSeq(0L);
            sequence.setLastDate(LocalDate.now());
            sequence.setVersion(0);
            codeSequenceMapper.insert(sequence);
        }
        return sequence;
    }

    /**
     * 检查并执行重置
     */
    private void checkAndReset(CodeRuleEntity rule, CodeSequenceEntity sequence) {
        LocalDate today = LocalDate.now();
        LocalDate lastDate = sequence.getLastDate();
        String resetType = rule.getResetType();

        boolean needReset = false;
        if (StringUtils.hasText(resetType)) {
            if (resetType.equals(CodeResetTypeEnum.DAY.getCode())) {
                needReset = lastDate == null || !lastDate.equals(today);
            } else if (resetType.equals(CodeResetTypeEnum.MONTH.getCode())) {
                needReset = lastDate == null || lastDate.getYear() != today.getYear()
                        || lastDate.getMonthValue() != today.getMonthValue();
            } else if (resetType.equals(CodeResetTypeEnum.YEAR.getCode())) {
                needReset = lastDate == null || lastDate.getYear() != today.getYear();
            }
        }

        if (needReset) {
            sequence.setCurrentSeq(0L);
            sequence.setLastDate(today);
            codeSequenceMapper.updateById(sequence);
            log.info("编码序号已重置，ruleCode={}, lastDate={}", rule.getRuleCode(), today);
        }
    }

    /**
     * 组装编码
     */
    private String buildCode(CodeRuleEntity rule, Long seq) {
        StringBuilder sb = new StringBuilder();
        // 前缀（非空才拼接）
        if (StringUtils.hasText(rule.getPrefix())) {
            sb.append(rule.getPrefix());
        }
        // 日期格式（非空才拼接）
        if (StringUtils.hasText(rule.getDateFormat())) {
            sb.append(formatDate(rule.getDateFormat()));
        }
        // 序号补零拼接
        sb.append(formatSeq(rule.getSeqLength() != null ? rule.getSeqLength() : 6, seq));
        return sb.toString();
    }

    /**
     * 格式化日期
     */
    private String formatDate(String format) {
        LocalDateTime now = LocalDateTime.now();
        return format
                .replace("{yyyy}", String.format("%04d", now.getYear()))
                .replace("{MM}", String.format("%02d", now.getMonthValue()))
                .replace("{dd}", String.format("%02d", now.getDayOfMonth()))
                .replace("{HH}", String.format("%02d", now.getHour()))
                .replace("{mm}", String.format("%02d", now.getMinute()))
                .replace("{ss}", String.format("%02d", now.getSecond()));
    }

    /**
     * 格式化序号
     */
    private String formatSeq(int length, Long seq) {
        String seqStr = String.valueOf(seq);
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < length - seqStr.length(); i++) {
            sb.append('0');
        }
        sb.append(seqStr);
        return sb.toString();
    }
}
```

### 6.5 Mapper 层

```java
// CodeSequenceMapper
public interface CodeSequenceMapper extends BaseMapper<CodeSequenceEntity> {

    @Select("SELECT * FROM sys_code_sequence WHERE rule_code = #{ruleCode}")
    CodeSequenceEntity selectByRuleCode(@Param("ruleCode") String ruleCode);

    @Select("SELECT * FROM sys_code_sequence WHERE rule_code = #{ruleCode} AND biz_key = #{bizKey}")
    CodeSequenceEntity selectByRuleCodeAndBizKey(@Param("ruleCode") String ruleCode, @Param("bizKey") String bizKey);
}

// CodeRuleMapper（使用 MyBatis-Plus BaseMapper）
public interface CodeRuleMapper extends BaseMapper<CodeRuleEntity> {

    @Select("SELECT * FROM sys_code_rule WHERE rule_code = #{ruleCode}")
    CodeRuleEntity selectByRuleCode(@Param("ruleCode") String ruleCode);
}
```

---

## 7. 编码规则初始化数据

```sql
-- ============================================================
-- 编码规则数据初始化（sys_code_rule）
-- ============================================================

-- 基础业务编码
INSERT INTO sys_code_rule (rule_code, rule_name, prefix, date_format, seq_length, reset_type, status) VALUES
-- 订单相关（未来扩展）
('ORDER_NO', '订单编号', 'ORD-', '{yyyy}{MM}{dd}', 6, 'DAY', 1),
('WORK_NO', '工单编号', 'WO-', '{yyyy}{MM}', 4, 'MONTH', 1),
('INSTRUCTION_NO', '指令单编号', 'ZL-', NULL, 4, 'NEVER', 1),
('DRAWING_NO', '图纸编号', 'TZ-', NULL, 4, 'NEVER', 1),
('IMAGE_PACKAGE_NO', '影像数据包编号', 'SJB-', '{yyyy}{MM}{dd}', 4, 'DAY', 1),
('UDI', 'UDI码', 'UDI-', NULL, 8, 'NEVER', 1),
('ATTACHMENT_NO', '附件编号', 'ATT-', '{yyyy}{MM}{dd}', 6, 'DAY', 1),

-- 基础数据编码
('HOSPITAL_NO', '医院编码', 'HOS-', NULL, 4, 'NEVER', 1),
('TEMPLATE_NO', '医院组合模板编码', 'TPL-', NULL, 4, 'NEVER', 1),
('DEPT_NO', '部门编码', 'DEPT-', NULL, 4, 'NEVER', 1),
('ORG_NO', '机构编码', NULL, NULL, 4, 'NEVER', 1),
('BODYPART_NO', '重建部位编码', 'BP-', NULL, 4, 'NEVER', 1),
('PROJECT_NO', '重建项目编码', 'RP-', NULL, 4, 'NEVER', 1),
('PRODUCT_CODE', '产品型号编码', 'PROD-', NULL, 4, 'NEVER', 1),
('HDEPT_NO', '医院科室编码', 'HDEPT-', NULL, 4, 'NEVER', 1);
```

---

## 8. 各模块改造方案

### 8.1 改造对照表

| 序号 | 模块 | 表名 | 编码字段 | 规则编码 | 新格式示例 | 改造内容 |
|-----|-----|-----|---------|---------|-----------|---------|
| 1 | 医院 | `hospital` | `hospital_code` | `HOSPITAL_NO` | `HOS-0001` | 替换 `generateHospitalCode()` |
| 2 | 机构 | `sys_org` | `org_code` | `ORG_NO` | `ORG-P-0001` | 调用 `generateWithCustomPrefix()` |
| 3 | 模板 | `hospital_group_template` | `template_code` | `TEMPLATE_NO` | `TPL-0001` | 替换 `generateTemplateCode()` |
| 4 | 部门 | `sys_dept` | `dept_code` | `DEPT_NO` | `DEPT-0001` | 替换 `generateDeptCode()` |
| 5 | 部位 | `rebuild_body_part` | `code` | `BODYPART_NO` | `BP-0001` | 完全重写，改为平铺 |
| 6 | 项目 | `rebuild_project` | `code` | `PROJECT_NO` | `RP-0001` | 完全重写，改为平铺 |

### 8.2 模块1：医院 (HospitalServiceImpl)

**改造内容：**
- 注入 `CodeGeneratorService`
- 替换私有方法 `generateHospitalCode()` 为调用编码器
- **使用 `CodeRuleConstants` 常量**

```java
// 注入
private final CodeGeneratorService codeGeneratorService;

// createHospital 方法中
String hospitalCode = codeGeneratorService.generate(CodeRuleConstants.HOSPITAL_NO);
entity.setHospitalCode(hospitalCode);

// 删除私有方法 generateHospitalCode()
```

### 8.3 模块2：机构 (OrgServiceImpl)

**改造内容：**
- 注入 `CodeGeneratorService`
- 调用 `generateWithCustomPrefix()` 支持动态前缀
- **使用 `CodeRuleConstants` 常量**

```java
// 注入
private final CodeGeneratorService codeGeneratorService;

// createOrg 方法中
String prefix = getOrgPrefixByType(dto.getOrgType());  // ORG-P- / ORG-D- 等
String orgCode = codeGeneratorService.generateWithCustomPrefix(CodeRuleConstants.ORG_NO, prefix);
entity.setOrgCode(orgCode);

// 新增私有方法
private String getOrgPrefixByType(Integer orgType) {
    return switch (String.valueOf(orgType)) {
        case "1" -> "ORG-P-";
        case "2" -> "ORG-D-";
        case "3" -> "ORG-H-";
        default -> "ORG-O-";
    };
}

// 删除私有方法 generateOrgCode()
```

### 8.4 模块3：医院组合模板 (HospitalGroupTemplateServiceImpl)

**改造内容：**
- 注入 `CodeGeneratorService`
- 替换私有方法 `generateTemplateCode()` 为调用编码器
- **使用 `CodeRuleConstants` 常量**

```java
// 注入
private final CodeGeneratorService codeGeneratorService;

// createTemplate 方法中
String templateCode = codeGeneratorService.generate(CodeRuleConstants.TEMPLATE_NO);
entity.setTemplateCode(templateCode);

// 删除私有方法 generateTemplateCode()
```

### 8.5 模块4：部门 (DeptServiceImpl)

**改造内容：**
- 注入 `CodeGeneratorService`
- 替换私有方法 `generateDeptCode()` 为调用编码器
- **使用 `CodeRuleConstants` 常量**

```java
// 注入
private final CodeGeneratorService codeGeneratorService;

// createDept 方法中
String deptCode = codeGeneratorService.generate(CodeRuleConstants.DEPT_NO);
entity.setDeptCode(deptCode);

// 删除私有方法 generateDeptCode()
```

### 8.6 模块5：重建部位 (BodyPartServiceImpl)

**改造内容：**
- 注入 `CodeGeneratorService`
- 完全重写 `generateCode()` 方法，改为平铺编码
- **使用 `CodeRuleConstants` 常量**

```java
// 注入
private final CodeGeneratorService codeGeneratorService;

// createBodyPart 方法中
String code = codeGeneratorService.generate(CodeRuleConstants.BODYPART_NO);
entity.setCode(code);

// 重写私有方法（简化版）
private String generateCode(Long parentId) {
    return codeGeneratorService.generate(CodeRuleConstants.BODYPART_NO);
}
```

### 8.7 模块6：重建项目 (RebuildProjectServiceImpl)

**改造内容：**
- 注入 `CodeGeneratorService`
- 完全重写 `generateCode()` 方法，改为平铺编码
- **使用 `CodeRuleConstants` 常量**

```java
// 注入
private final CodeGeneratorService codeGeneratorService;

// createProject 方法中
String code = codeGeneratorService.generate(CodeRuleConstants.PROJECT_NO);
entity.setCode(code);

// 重写私有方法（简化版）
private String generateCode(Long bodyPartId, Long parentId) {
    return codeGeneratorService.generate(CodeRuleConstants.PROJECT_NO);
}
```

---

## 9. 编码格式变更汇总

| 模块 | 旧格式 | 新格式 | 变化说明 |
|-----|-------|-------|---------|
| 医院 | `HOS-001` | `HOS-0001` | 序号由3位改为4位 |
| 模板 | `TPL-HOS-001` | `TPL-0001` | 移除中间层，序号由3位改为4位 |
| 部门 | `DEPT-000001` | `DEPT-0001` | 序号由6位改为4位 |
| 机构 | `ORG-P-001` | `ORG-P-0001` | 序号由3位改为4位 |
| 部位 | `BP_001_001` | `BP-0001` | 改为平铺，移除层级 |
| 项目 | `RP_1_001_001` | `RP-0001` | 改为平铺，移除层级 |

---

## 10. 向后兼容说明

- **现有编码**：已存在的编码（如 `HOS-001`）不会自动修改，继续有效
- **新编码**：新创建的记录将使用新格式（如 `HOS-0001`）
- **查询兼容**：`hospital_code` 等字段的查询逻辑无需修改
- **数据库约束**：`uk_hospital_code` 等唯一索引不受影响

---

## 11. 风险评估

| 风险项 | 级别 | 缓解措施 |
|-------|-----|---------|
| 编码格式变更 | 低 | 已有数据不受影响，新数据使用新格式 |
| 机构动态前缀 | 中 | 提供 `generateWithCustomPrefix` 方法，支持灵活前缀 |
| 层级编码移除 | 低 | `parent_id` 字段仍保留，层级关系通过数据库维护 |
| 并发安全 | 低 | 编码器已使用乐观锁保证并发安全 |

---

## 12. 错误码

```java
CODE_RULE_NOT_FOUND(678, "编码规则不存在"),
CODE_RULE_DISABLED(679, "编码规则已禁用"),
CODE_GENERATE_FAILED(680, "编码生成失败");
```

---

## 13. 代码文件清单

| 文件 | 路径 |
|------|------|
| 编码规则实体 | `yigongbao-module-basic/.../code/entity/CodeRuleEntity.java` |
| 编码序号实体 | `yigongbao-module-basic/.../code/entity/CodeSequenceEntity.java` |
| 重置类型枚举 | `yigongbao-common/.../enums/CodeResetTypeEnum.java` |
| Mapper | `yigongbao-module-basic/.../code/mapper/CodeRuleMapper.java` |
| Mapper | `yigongbao-module-basic/.../code/mapper/CodeSequenceMapper.java` |
| Service接口 | `yigongbao-module-basic/.../code/service/CodeGeneratorService.java` |
| Service实现 | `yigongbao-module-basic/.../code/service/impl/CodeGeneratorServiceImpl.java` |
| Controller | `yigongbao-module-basic/.../code/controller/CodeRuleController.java` |
| DTO | `yigongbao-module-basic/.../code/dto/CreateCodeRuleDTO.java` |
| DTO | `yigongbao-module-basic/.../code/dto/UpdateCodeRuleDTO.java` |
| VO | `yigongbao-module-basic/.../code/vo/CodeRuleVO.java` |
| Convert | `yigongbao-module-basic/.../code/convert/CodeRuleConvert.java` |

---

## 14. 实现清单

### 14.1 编码器扩展
- [ ] 在 `CodeGeneratorService` 接口新增 `generateWithCustomPrefix` 方法
- [ ] 在 `CodeGeneratorServiceImpl` 实现该方法

### 14.2 初始化数据
- [ ] 在 `sql/init.sql` 新增7个业务编码规则（HOSPITAL_NO、TEMPLATE_NO、DEPT_NO、ORG_NO、BODYPART_NO、PROJECT_NO、PRODUCT_CODE）

### 14.3 模块改造
- [ ] 医院模块：`HospitalServiceImpl` 改造
- [ ] 机构模块：`OrgServiceImpl` 改造
- [ ] 模板模块：`HospitalGroupTemplateServiceImpl` 改造
- [ ] 部门模块：`DeptServiceImpl` 改造
- [ ] 部位模块：`BodyPartServiceImpl` 改造
- [ ] 项目模块：`RebuildProjectServiceImpl` 改造

### 14.4 测试
- [ ] `CodeGeneratorServiceImplTest` 更新，测试新方法 `generateWithCustomPrefix`
- [ ] 各模块 ServiceImpl 单元测试更新
- [ ] 验证新编码格式正确生成
- [ ] 验证并发场景下编码不重复

---

## 15. 使用示例

```java
// ✅ 推荐：使用 CodeRuleConstants 常量
import com.yigongbao.common.constant.CodeRuleConstants;

// 示例1：生成医院编码
String hospitalCode = codeGeneratorService.generate(CodeRuleConstants.HOSPITAL_NO);
// 结果：HOS-0001

// 示例2：生成带业务前缀的编码（如指令单）
String instructionNo = codeGeneratorService.generateWithBizPrefix(
    CodeRuleConstants.INSTRUCTION_NO,
    "ORD-20260324-000001"
);
// 结果：ZL-ORD-20260324-000001-0001

// 示例3：生成带自定义前缀的编码（如机构编码）
String orgCode = codeGeneratorService.generateWithCustomPrefix(
    CodeRuleConstants.ORG_NO,
    "ORG-P-"
);
// 结果：ORG-P-0001

// 示例4：生成带序号后缀的编码（如数据包，同一订单下的多个数据包）
// 第一个数据包
String code1 = codeGeneratorService.generateWithSeqSuffix(
    CodeRuleConstants.DATA_PACKAGE_NO,
    "202603250001"  // 传入订单编号作为 bizKey
);
// 结果：202603250001-1

// 第二个数据包（同一个订单）
String code2 = codeGeneratorService.generateWithSeqSuffix(
    CodeRuleConstants.DATA_PACKAGE_NO,
    "202603250001"
);
// 结果：202603250001-2

// 不同订单的数据包（独立序号池）
String code3 = codeGeneratorService.generateWithSeqSuffix(
    CodeRuleConstants.DATA_PACKAGE_NO,
    "202603250002"
);
// 结果：202603250002-1

// 示例5：预览编码格式
String preview = codeGeneratorService.preview(CodeRuleConstants.HOSPITAL_NO);
// 结果：HOS-NNNN

// ❌ 错误：直接使用硬编码字符串
String hospitalCode = codeGeneratorService.generate("HOSPITAL_NO");
String code = codeGeneratorService.generateWithSeqSuffix("DATA_PACKAGE_NO", "202603250001");
```

---

## 16. 更新记录

| 版本 | 日期 | 作者 | 更新内容 |
|-----|------|------|---------|
| v1.0 | 2026-03-24 | hanjor | 初始版本 |
| v1.1 | 2026-03-25 | hanjor | 新增 generateWithCustomPrefix 方法；新增7个业务编码规则；6个模块改造方案 |
| v1.2 | 2026-03-25 | hanjor | 新增 generateWithSeqSuffix 方法（按业务标识隔离序号池）；新增 CodeRuleConstants 常量类；更新 sys_code_sequence 表结构支持 biz_key；更新所有文档和使用示例 |

---

## 17. 常见问题 FAQ

### Q1：generateWithBizPrefix 和 generateWithSeqSuffix 有什么区别？

| 对比项 | generateWithBizPrefix | generateWithSeqSuffix |
|-------|---------------------|---------------------|
| 序号池 | 全局共享（相同 ruleCode 共用序号） | 按 bizKey 隔离（不同 bizKey 各自独立） |
| 序号格式 | 补零格式（如 0001） | 无补零（如 1、2、3） |
| 拼接方式 | `前缀 + bizPrefix + "-" + 序号` | `bizKey + "-" + 序号` |
| 典型场景 | 指令单（ZL-{订单编号}-{序号}） | 数据包（{订单编号}-{1}、{订单编号}-{2}） |

### Q2：前缀可以置空吗？

可以。将 `sys_code_rule.prefix` 设置为 NULL，系统会跳过前缀拼接。例如：
- 前缀=`ORD-`，日期=`{yyyy}{MM}{dd}`，序号长度=4 → `ORD-20260325-0001`
- 前缀=空，日期=`{yyyy}{MM}{dd}`，序号长度=4 → `20260325-0001`
- 前缀=空，日期=空，序号长度=6 → `000001`

### Q3：如何新增一个编码规则？

1. 在 `CodeRuleConstants` 中添加常量（如 `DATA_PACKAGE_NO = "DATA_PACKAGE_NO"`）
2. 在 `sys_code_rule` 表中插入规则数据
3. 在业务代码中使用 `codeGeneratorService.generate(CodeRuleConstants.DATA_PACKAGE_NO)`

---

**文档信息**

| 项目 | 内容 |
|-----|------|
| 文档版本 | v1.2 |
| 创建日期 | 2026-03-24 |
| 最后更新 | 2026-03-25 |
| 作者 | hanjor |
